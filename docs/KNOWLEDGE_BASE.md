# Verified knowledge base

This document stores durable, reusable findings. Keep exact observations,
compiler observations, corroboration, inferences, and unknowns visibly
separate. Address-specific transient work belongs in `.analysis/`; the current
next action belongs in `RE_HANDOFF.md`.

## Target identity

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| TGT-001 | observed | The supported executable is 696,832 bytes with SHA-256 `bb54f6fc54f0eeffaec416ca9f64aef32b5f59b7427fa5a6579f6538e0eddc07`. | Direct hash of the supplied Japanese executable; `config/target.toml` |
| TGT-002 | observed | The executable reports `Shoot the Bullet. ver 1.02a`. | ASCII string in the verified target |
| TGT-003 | corroborated | 1.02a is the planned final update, published 2006-01-13. | ZUN's official update post linked from the README |
| TGT-004 | observed | PE image base is `0x00400000`; entry point is `0x00486A9D`; relocations are stripped. | PE headers in the verified target |
| TGT-005 | observed | `.text` spans `0x00401000..0x004942A7` by PE virtual size. | PE section table |

## Compiler and ABI

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| ABI-001 | observed | PE linker version is `7.10`. | Verified target optional header |
| ABI-002 | observed | Rich-header records include build `3077`. | Decoded verified target Rich header |
| ABI-003 | compiler-observed | The pinned tools report compiler `13.10.3077` and linker `7.10.3077`. | `archaic-msvc/msvc710` commit and binary hashes in `config/tools.lock.toml` |
| ABI-004 | inferred | Visual C++ .NET 2003 x86 is the original compiler family. | ABI-001 through ABI-003 |
| ABI-005 | compiler-observed | The ANM translation unit uses `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /Od /Ob1`; `/Gr` makes `AnmManager::ExecuteScript` fastcall and `/Ob1` reproduces its inline helpers. | Canonical ANM units under pinned VC7.1 build 3077 |
| ABI-006 | compiler-observed | The stock VC7.1 build 3077 frontend does not implement TH08's patched `#pragma var_order`; natural local declaration order and identifier allocation are codegen-visible in the 0x400-byte ExecuteScript frame. | Isolated VC7.1 local-layout probes and exact ExecuteScript unit |
| ABI-007 | compiler-observed | The ECL translation unit uses `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /Od /Ob1`; its exact `RunEcl` body has a 0x9EC-byte frame and keeps `this` at `[ebp-0x580]`. | Canonical `ecl-manager-run-ecl` unit under pinned VC7.1 build 3077 |
| ABI-008 | compiler-observed | The SoundPlayer translation unit uses `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /Od /Ob1`. Scoped identifier backing names reproduce target local allocation without a patched frontend. | Twenty-six canonical SoundPlayer units under pinned VC7.1 build 3077 |
| ABI-009 | compiler-observed | The Main translation unit uses `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /Od /Ob1`; this profile reproduces seventeen independent configuration, window, timing, chain, synchronization, and D3D functions. | Canonical Main units under pinned VC7.1 build 3077 |
| ABI-010 | compiler-observed | The radial-trail initializer requires the inlined memory wrapper to read `Alloc` and `Free` parameters through volatile lvalues. VC7.1 then emits the addressable argument homes at `[ebp-0x3C]` and `[ebp-0x40]` found in the target. | Isolated pinned-VC7.1 compiler oracle and exact `anm-initialize-pulsing-radial-trail` unit |
| ABI-011 | compiler-observed | Stock VC7.1 can reproduce local ordering that TH08 documents with unsupported `#pragma var_order` by restoring source lifetimes and target-proven identifier buckets. `Chain::ReleaseSingleChain` must preserve TH08's separate `ChainElem releaseSnapshotHead` and cursor locals; backing only the real cursor with `jLocal00` restores every target home while keeping EH cleanup bound directly to `ChainElem::~ChainElem`. Grouping head/cursor into a convenience aggregate can leave the parent body exact yet change compiler-owned unwind provenance. | Canonical `chain-run-calc`, `chain-run-draw`, `chain-release-single`; 81/81 associative EH cleanup replays |
| ABI-012 | compiler-observed / diagnostic | An isolated port of TH08's `var_order` frontend hook to VC7.1 build 3077 is a valid ordering oracle for ordinary identifiers: the micro-oracle swaps `apple/zebra` from `-0x04/-0x08` to `-0x08/-0x04` without changing source semantics. Hidden member `this` is a different compiler-owned class: member-scope instrumentation sees zero pre-existing scope entries before the first named local, ordinary var-order lists leave `this` fixed, and forcing the keyword through the VC7.1 pragma parser reaches an internal compiler error. Crucially, an isolated instrumented run of TH08's original MSVC7.0 patch shows the same language boundary: `ScanList` accepts ordinary names and then reports C4081 on keyword `this`, while `Scope::Add` never receives the hidden receiver. Historical TH08 lists ending in `this` therefore do **not** prove that the patch reordered hidden `this`; they leave receiver placement natural. | Isolated VC7.1 v3 ordinary-local oracle plus member instrumentation; isolated TH08 MSVC7.0 `ORDER`/`ADD` logging micro; stock compiler remains canonical proof |
| ABI-013 | compiler-observed / diagnostic | The current complete source graph cold-compiles to 88/88 i386 COFF objects under the two per-source canonical profiles, but a real VC7.1 `/OPT:NOREF` link fails on 515 unique decorated symbols (802 diagnostics: 290 data, 225 callable/runtime). Canonical relocation evidence maps 512 names to 277 target addresses; the three unmapped globals occur only in deferred non-exact `Controller::GetInput`. Six proxy names map to multiple target addresses, proving that blanket linker aliases are unsound. This is a production declaration/ownership gap and does not revoke function-level exact credit. | `python3 scripts/build-whole.py`; generated `build/whole-validation/report.json`, 2026-09-08 |
| ABI-014 | observed | The verified target's import descriptors are ordered DINPUT8, DSOUND, d3d8, WINMM, KERNEL32, USER32, GDI32, ADVAPI32, ole32. The whole-build audit preserves this library ordering while treating static/no-descriptor SDK libraries separately. | Verified target PE import directory after `verify-target.py`; `scripts/build-whole.py` |
| ABI-015 | corroborated / policy | TH08's earliest i386 Linux validation established the useful cross-platform pattern of compiling and linking the complete source graph, then attesting executable format, machine, and fixed layout. TH095 reuses only that validation pattern: its build audit uses the original pinned VC7.1/PE32 toolchain and is not a Linux port. | TH08 commit `da07b21c8cb63230142b0a66228d6aac66555f3f`; TH095 `scripts/build-whole.py` |
| ABI-016 | target-observed / compiler-observed / corroborated | The production Chain ABI uses `class ChainElem *` (`PAV`), an enum-returning `/Gr` callback in `CreateElem`, and `int RunDrawChain`; `Global.cpp` owns `DIFFABLE_STATIC(Chain, g_Chain)` at target address `0x004BE3C8`. Target wrappers `0x00493F30`/`0x00494210` call the exact Chain constructor/destructor on that storage, and TH08 corroborates the same TU owner. Canonicalizing this family removes seven unresolved decorated names and six target addresses (515→508 unique unresolved; 802→731 diagnostics) while all 696 exact units and all 81 EH cleanup references retain exact replay. | Hash-attested Ghidra xrefs/disassembly; pinned VC7.1 cold COFF/link audit; canonical 60/60 changed-unit replay; TH08 `Global.cpp` |
| ABI-017 | compiler-observed / diagnostic | At the current production-owner checkpoint, the complete source graph cold-compiles to 88/88 i386 COFF objects under the pinned VC7.1 profiles. The real `/OPT:NOREF` link reports 239 unique unresolved decorated symbols across 258 diagnostics: 130 data and 109 callable/runtime. Relocation evidence maps 236 names to 151 target addresses; three names have no current target mapping and four decorated names map to multiple addresses. This is the live link-coherence frontier, not a change to function-level exact credit. | Fresh `python3 scripts/build-whole.py`; generated `build/whole-validation/report.json`, 2026-09-08; commits `a3412fb`, `3872090`, `7d20869`, `d77ade1` |
| ABI-018 | compiler-observed / source-structure | One target pointer slot may have several translation-unit-local partial struct views. Runnable objects share those slots through uniquely named production owners and `TH095_RUNTIME_GLOBAL_PTR`, while `#ifndef DIFFBUILD` aliases leave exact builds on their established target-facing extern symbols and relocations. A repair is incomplete until the target-proven lifecycle path writes and clears the shared owner; zero storage alone is not runtime closure. | `src/GameplayGlobals.hpp`; pinned VC7.1 production cold build; representative Main and PhotoGameTask exact-unit builds |
| ABI-019 | target-observed / compiler-observed / source-structure | The `0x004C4E70` player/photo-game pointer slot is a real shared runtime owner. Ghidra-attested PlayerInf construction stores its receiver to the slot at `0x0042EB9C`; teardown clears it at `0x0042EEFE`. Seventeen production-only per-TU views are routed through `g_RuntimePlayerOwner`, reducing the fresh `/OPT:NOREF` link from 239 to 222 unique unresolved names (258 to 238 diagnostics; 113 data / 109 callable; 219 names mapped to 150 target addresses). Canonical probes define `TH095_MATCH_EXACT` so exact-facing proxy ABI/source shapes remain independent from production canonicalization. The separation is fully replayed, not sampled: 696/696 configured exact units strict-compare with zero failures. | Hash-attested Ghidra xrefs/disassembly for `0x004C4E70`, `0x0042EA70`, and `0x0042EE90`; fresh `python3 scripts/build-whole.py`; complete canonical replay / strict compare, 2026-09-09 |
| ABI-020 | target-observed / compiler-observed / source-structure | The `0x004BDDC0` slot is the shared TH095 enemy/runtime manager pointer. The exact enemy-manager constructor at `0x00414B90` writes its receiver to the slot at `0x00414C4C`; the exact destructor at `0x004154E0` clears it at `0x00415658` after ECL/ANM cleanup. Thirteen production-only ECL/background/enemy/photo/runtime views now route through `g_RuntimeEnemyManagerOwner`, reducing the fresh `/OPT:NOREF` link from 222 to 209 unique unresolved names and 238 to 223 diagnostics (100 data / 109 callable; 206 names mapped to 149 target addresses). The ECL runtime accessor is deliberately distinct from the historical `g_Th095Runtime` token so qualified exact references retain their original decorated relocation while production uses the shared owner. All 157 directly affected canonical units replay exact; 27 compiler-private relocation names changed only after offset/type/solved-destination audit. | Hash-attested Ghidra xrefs/disassembly for `0x004BDDC0`, `0x00414B90`, and `0x004154E0`; fresh `python3 scripts/build-whole.py`; canonical affected-source replay, 2026-09-09 |
| ABI-021 | target-observed / compiler-observed / source-structure | The `0x004C4E6C` slot is the shared TH095 PhotoInf/stage-state pointer. The exact constructor at `0x0042A8A0` publishes its receiver at `0x0042A921`; the exact destructor at `0x0042AAF0` clears the slot at `0x0042AB5D` before embedded display/photo teardown. Ten production-only ASCII/background/card/front/game/task/camera/stage/result/score/ECL views route through `g_RuntimeStageStateOwner`, reducing the fresh `/OPT:NOREF` link from 209 to 199 unique unresolved names and 223 to 211 diagnostics (90 data / 109 callable; 196 names mapped to 148 target addresses). ECL uses a distinct `TH095_ECL_STAGE_STATE` accessor so exact builds preserve the original `EclRunHigh::g_Th095StageState` relocation identity. All 144 directly affected canonical units replay exact with no private-label refresh. | Hash-attested Ghidra xrefs/disassembly for `0x004C4E6C`, `0x0042A8A0`, and `0x0042AAF0`; fresh `python3 scripts/build-whole.py`; canonical affected-source replay, 2026-09-09 |
| ABI-022 | target-observed / compiler-observed / source-structure | The `0x004BDD90` slot is the real `Background *` owner, not a separate production-only storage. Hash-attested Ghidra shows `Background::Background @ 0x004020C0` publishing `this` at `0x00402226` and `Background::~Background @ 0x00402330` clearing it at `0x00402446`. `Background.hpp` therefore declares `Background *g_Background`; ANM/ECL photo-mode views canonicalize to that owner, and the multi-target PhotoCamera edge is corrected to the real `Background::SetPhotoArea @ 0x00404950` instead of a BulletManager proxy receiver. The unused `g_RuntimeGameManagerOwner` storage was deleted to avoid duplicate logical ownership. The fresh `/OPT:NOREF` link moves 199 -> 195 unique unresolved names and 211 -> 207 diagnostics (87 data / 108 callable; 192 names mapped to 147 target addresses). The decorated `PhotoBulletManagerView::g_PhotoBulletManager` remains globally reported against both `.90/.98` only because its still-open production references are the `.98` bullet-manager uses; target relocation review separates the already-canonical `.90` call. All 403 directly affected configured units replay exact. | Hash-attested Ghidra xrefs/disassembly for `0x004BDD90`, `0x004020C0`, `0x00402330`; exact `background-set-photo-area`; fresh whole-build and complete affected-source replay, 2026-09-09 |
| ABI-023 | target-observed / compiler-observed / source-structure | The `0x004BDD98` slot is the shared BulletInf manager pointer. Hash-attested Ghidra shows `BulletInf` construction at `0x00404C80` publishing `this` to the slot at `0x00404CE0`, and destruction at `0x00404ED0` clearing it at `0x00404F3D` after Chain/ANM teardown. Production enemy-shot, ECL, item, enemy, stage, reset, and photo-game bullet views route through `g_RuntimeBulletManagerOwner`. The decorated `PhotoBulletManagerView::g_PhotoBulletManager` was explicitly treated as multi-target: its `.90` relocation/callee is the real `Background::SetPhotoArea @ 0x00404950`, while its `.98` relocations are BulletInf operations. No blanket alias is used. The fresh `/OPT:NOREF` link moves 195 -> 187 unique unresolved names and 207 -> 197 diagnostics (79 data / 108 callable; 184 names mapped to 145 target addresses); `0x004BDD98` disappears from the unresolved target set and the multi-target-name count drops 4 -> 3. All 132 directly affected exact units replay exact with no label refresh. | Hash-attested Ghidra xrefs/disassembly for `0x004BDD98`, `0x00404C80`, `0x00404ED0`; canonical relocation review for `.90/.98`; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-024 | target-observed / compiler-observed / source-structure | The `0x004C45E0` slot is the shared PhotoEffect/EffectManager pointer. Hash-attested Ghidra shows the exact manager constructor at `0x0041D580` zeroing the 0x80-byte receiver and publishing `this` at `0x0041D5AE`; the destructor at `0x0041D6D0` cuts its Chain entries, destroys/frees the effect list, then clears the slot at `0x0041D773`. Production photo-effect, camera stage-controller, stage commit/reset, ECL extended, and ECL high views route through `g_RuntimeEffectManagerOwner`. Their target methods independently land in the same receiver family (`Spawn @ 0x0041DBD0`, `CommitCapturedObjects @ 0x0041DFA0`, `ResetEnemyState/ResetForPhotoTransition @ 0x0041E010`, `CountNearbyTargets @ 0x0041E060`). The fresh `/OPT:NOREF` link moves 187 -> 180 unique unresolved names and 197 -> 190 diagnostics (72 data / 108 callable; 177 names mapped to 144 target addresses); `0x004C45E0` disappears from the unresolved target set. All 96 directly affected exact units replay exact with no label refresh. | Hash-attested Ghidra xrefs/disassembly for `0x004C45E0`, `0x0041D580`, and `0x0041D6D0`; canonical relocation review for the four method targets; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-025 | target-observed / compiler-observed / source-structure | The `0x004C45DC` slot is the shared PhotoItem/ItemInf manager pointer. Hash-attested Ghidra shows constructor `0x0041CB20` constructing the 150-entry item storage, zeroing the manager, and publishing `this` at `0x0041CB80`; destructor `0x0041CC20` cuts both Chain entries, clears the slot at `0x0041CC78`, then destroys the embedded item storage. Production `g_ItemManager`, `g_PhotoItemManager`, and photo-capture particle-spawner views now route through `g_RuntimeItemManagerOwner`; their independent relocations all call the same `Spawn @ 0x0041D460` receiver. The fresh `/OPT:NOREF` link moves 180 -> 177 unique unresolved names and 190 -> 186 diagnostics (69 data / 108 callable; 174 names mapped to 143 target addresses), with `0x004C45DC` absent from the unresolved target set. All 82 directly affected exact units replay exact with no label refresh. | Hash-attested Ghidra xrefs/disassembly for `0x004C45DC`, `0x0041CB20`, and `0x0041CC20`; canonical relocation review for `Spawn @ 0x0041D460`; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-026 | target-observed / compiler-observed / source-structure | The `0x004BDDC4` slot is the shared FrontInf/background-front manager pointer. Hash-attested Ghidra shows constructor `0x00417070` constructing six embedded ANM VMs, zeroing the 0x10D4-byte manager, and publishing `this` at `0x004170CA`; destructor `0x00417360` cuts both Chain entries, marks its ANM file VMs for deletion, clears the slot at `0x004173CD`, then destroys the six-VM array. Production `g_PhotoFrontManager` and PhotoStage supervisor partial views now route through `g_RuntimeBackgroundManagerOwner`. The fresh `/OPT:NOREF` link moves 177 -> 175 unique unresolved names and 186 -> 184 diagnostics (67 data / 108 callable; 172 names mapped to 142 target addresses), with `0x004BDDC4` absent from the unresolved target set. All 17 directly affected exact units replay exact with no label refresh. | Hash-attested Ghidra xrefs/disassembly for `0x004BDDC4`, `0x00417070`, and `0x00417360`; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-027 | target-observed / compiler-observed / source-structure | The `0x004BDEC8` slot is the shared PhotoGameTask/GameTaskInf runtime pointer. Hash-attested Ghidra shows `PhotoGameTaskView::Create @ 0x00417F80` allocating/constructing the 0x124-byte task and publishing it at `0x00417FE5` before registering two Chain entries; `PhotoGameTaskView::~PhotoGameTaskView @ 0x00417E70` destroys the owned gameplay subsystems, cuts both task Chain entries, and clears the slot at `0x00417F2D`. All other production task/global-state partial views already used `g_RuntimeGameTaskOwner`; routing Main.obj's final `g_PhotoGameTask` read view through the same owner removes the last `0x004BDEC8` unresolved without changing implementation ABI. The fresh `/OPT:NOREF` link moves 175 -> 174 unique unresolved names and 184 -> 183 diagnostics (66 data / 108 callable; 171 names mapped to 141 target addresses). Main's 48 configured exact units replay exact with no label refresh. | Hash-attested Ghidra xrefs/disassembly for `0x004BDEC8`, `0x00417E70`, and `0x00417F80`; canonical PhotoGameTask relocation tracking; fresh whole-build and Main exact replay, 2026-09-09 |
| ABI-028 | target-observed / compiler-observed / source-structure | `0x004CA1B8` is the real process-wide `AnmManager *g_AnmManager` storage owned by `AnmManager.cpp`. Hash-attested Ghidra shows exact `WinMain @ 0x00420240` publishing the newly allocated manager at `0x00420389` and clearing the same slot at `0x0042055B` after deletion; the canonical WinMain unit independently replays this lifecycle exactly. Production `EclExtended::g_AnmManager` and `EclRunHigh::g_Th095AnmManager` proxy reads now cast/view the real `::th095::g_AnmManager` rather than requiring separate storage. The fresh `/OPT:NOREF` link moves 174 -> 172 unique unresolved names and 183 -> 181 diagnostics (64 data / 108 callable; 169 names mapped to 140 target addresses), with `0x004CA1B8` absent from the unresolved target set. Both directly affected ECL sources replay 23/23 exact with no label refresh. | Hash-attested Ghidra xrefs for `0x004CA1B8` and `WinMain @ 0x00420240`; canonical exact WinMain lifecycle; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-029 | target-observed / compiler-observed / source-structure | `0x004BDD9C` is the real `PhotoCardInfoView *g_PhotoCardInfo` / CardInf pointer. Hash-attested Ghidra shows `PhotoCardInfoView::PhotoCardInfoView @ 0x00408610` clearing the 0x68-byte object and publishing `this` at `0x0040865C`; destructor `0x00408760` cuts both Chain entries, marks its two ANM VMs for deletion, and clears the slot at `0x004087BA`. PhotoStage's local `PhotoStageRuntimeView` reads only `comment @ +0x20`, exactly the target-proven `PhotoCardInfoView::text @ +0x20`, so production uses one real `g_PhotoCardInfo` definition and views it at that offset rather than creating duplicate storage. The fresh `/OPT:NOREF` link moves 172 -> 170 unique unresolved names and 181 -> 178 diagnostics (62 data / 108 callable; 167 names mapped to 139 target addresses), with `0x004BDD9C` absent from the unresolved target set. All 26 directly affected exact units replay exact with no label refresh. | Hash-attested Ghidra xrefs/disassembly for `0x004BDD9C`, `0x00408610`, and `0x00408760`; canonical CardInf layout/relocations; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-030 | target-observed / compiler-observed / source-structure | `0x004C4E74` is the real `ReplayManager *g_ReplayManager` singleton pointer. Canonical exact `ReplayManager::Initialize @ 0x004342A0` publishes `this` in record and playback modes (target writes at `0x004342C2` and `0x00434413`), while exact destructor `0x004344F0` clears the slot at `0x0043459B` only when destroying the active singleton. Production therefore defines the real named global in `ReplayManager.cpp`; no bridge storage is required. The fresh `/OPT:NOREF` link moves 170 -> 169 unique unresolved names and 178 -> 176 diagnostics (61 data / 108 callable; 166 names mapped to 138 target addresses), with `0x004C4E74` absent from the unresolved target set. All 12 ReplayManager exact units replay exact with no label refresh. | Hash-attested Ghidra xrefs/function boundaries for `0x004C4E74`, `0x004342A0`, and `0x004344F0`; canonical ReplayManager exact ledger; fresh whole-build and ReplayManager replay, 2026-09-09 |
| ABI-031 | target-observed / compiler-observed / source-structure | `0x004BDDC8` is one shared 0x100-byte replay-path buffer, not a pointer owner. Hash-attested Ghidra shows `ReplayBrowserView::Update @ 0x0044DCA0` writing the storage at `0x0044E2BF`; canonical relocations bind both `g_SelectedReplayPath` (ReplayBrowser/front-end writers using `strcpy`/`sprintf`) and PhotoGameTask's read-only `g_ReplayPath` view to the same target address. Production therefore owns one real `char g_SelectedReplayPath[0x100]` and maps `g_ReplayPath` to that buffer only outside DIFFBUILD. The fresh `/OPT:NOREF` link moves 169 -> 167 unique unresolved names and 176 -> 173 diagnostics (59 data / 108 callable; 164 names mapped to 137 target addresses), with `0x004BDDC8` absent from the unresolved target set. The two directly affected sources replay 14/14 exact with no label refresh. | Hash-attested Ghidra xrefs/function boundary for `0x004BDDC8` / `0x0044DCA0`; canonical replay-browser/front-end/photo-task relocation evidence; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-032 | target-observed / compiler-observed / source-structure | PlayerInf callable identity is one runtime receiver even though exact translation units historically decorated it through several local proxy classes. Hash-attested Ghidra bounds `AngleFromPoint @ 0x004303E0` to 108 bytes, `CheckBulletCollision @ 0x00430450` to 212 bytes, and `Die @ 0x004306D0` to 241 bytes; disassembly fixes receiver fields at player position `+0x1E30`, hurtbox min/max `+0x03A8/+0x03B4`, mode `+0`, and the exact collision/laser calls into `Die` at `0x00430514`/`0x004306B1`. Production therefore centralizes those methods on one offset-asserted `PhotoPlayerRuntimeView` over `g_RuntimePlayerOwner`, while DIFFBUILD retains the original ECL/Bullet/Enemy proxy receiver spelling. This removes five angle and two collision unresolved names without wrappers, aliases, or duplicate storage: fresh `/OPT:NOREF` moves 167 -> 160 unique unresolved and 173 -> 166 diagnostics (59 data / 101 callable; 157 names mapped to 135 target addresses). Six affected sources replay 82/82 exact units; 17 BulletManager `$L` labels changed only after structural and solved-destination audit. | Hash-attested Ghidra function/disassembly/xref evidence for `0x004303E0`, `0x00430450`, `0x004306D0`; canonical exact `photo-player-angle-from-point`, `photo-player-check-bullet-collision`, `photo-player-die`; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-033 | target-observed / compiler-observed / source-structure | The ANM callable cluster at `0x00445060`, `0x00445110`, `0x00445170`, `0x004451B0`, `0x004451F0`, `0x004452F0`, and `0x00445360` is one canonical `AnmLoaded` / `AnmManager` / four-byte `AnmVmId` ABI despite historical PhotoCamera, PhotoStage, EclExtended, and EclRun proxy receiver names. Hash-attested Ghidra bounds the functions to 169/90/50/60/64/51/69 bytes respectively. Production ECL declarations are exposed only when `TH095_MATCH_EXACT` is absent, so exact preprocessing retains the original proxy names. Receiver provenance is concrete: the enemy runtime slot at `+0x4DF8` is an `AnmLoaded *`, while BulletInf `+0x27C5B0` is assigned from `g_AnmManager->LoadAnm(6, "bullet.anm")`. The fresh `/OPT:NOREF` link moves 160 -> 144 unique unresolved and 166 -> 150 diagnostics (59 data / 85 callable; 141 names mapped to 128 target addresses); every address in this cluster leaves the unresolved set. Four affected sources replay 40/40 exact units with no private-label refresh. | Hash-attested Ghidra function bounds for the seven addresses; production `AnmLoaded` ownership in Background/Enemy/Bullet reconstruction; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-034 | target-observed / source-structure | `0x004453B0` is the canonical 136-byte `AnmTextureEntryView::Clear` receiver. The HelpMenu and MusicRoom production views each described the same 0x10-byte texture entry under a local proxy class solely to call this method. Production now uses `AnmTextureEntryView *` directly; exact branches remain isolated in their existing `TH095_MATCH_EXACT` snapshots. Fresh `/OPT:NOREF` moves 144 -> 142 unique unresolved and 150 -> 148 diagnostics (59 data / 83 callable; 139 names mapped to 127 target addresses), and the address leaves the unresolved set. HelpMenu/MusicRoom replay 5/5 exact units. | Hash-attested Ghidra function bound at `0x004453B0`; canonical `SceneTexture.cpp` implementation; fresh whole-build and exact replay, 2026-09-09 |
| ABI-035 | target-observed / source-structure | `0x00404C60` is one canonical 21-byte `__fastcall i32(int)` photo-bullet script-base accessor, not separate ECL/effect helper implementations. The exact implementation reads `g_PhotoBulletScriptBases[type]`; production EclExtended and PhotoEffect callers canonicalize to `th095::GetPhotoBulletScriptBase`, while DIFFBUILD/exact retains the historical local helper decorations. | Hash-attested `scripts/ghidra.py query ... function 0x00404C60`; canonical `photo-bullet-script-base` exact unit; fresh 88-TU build; 56/56 affected exact replay. |
| ABI-036 | target-observed / source-structure | `0x0041DBD0` is the canonical 432-byte `PhotoEffectManagerView::Spawn(i32, void *)` `__thiscall` body. Production now exposes the real 0x80-byte manager ABI through `PhotoEffectRuntime.hpp`, shared by the implementation and ECL consumers; EclExtended casts only its Spawn operation because it still needs a richer local field-view for exact list inspection, while EclRun's production owner is directly typed as `PhotoEffectManagerView *`. The runtime header depends on `ZunTimer` and forward declarations rather than canonical `AnmManager.hpp`, because ECL owns independent exact-facing ANM declarations and importing both type universes causes VC7.1 redefinitions. | Hash-attested Ghidra function query at `0x0041DBD0`; canonical `photo-effect-manager-spawn`; fresh 88-TU build; 57/57 affected exact replay. |
| ABI-037 | target-observed / compiler-observed / source-structure | `0x00406CC0` is one canonical BulletInf `SpawnBulletPattern` method, not separate EnemyShot and ECL implementations. Hash-attested Ghidra bounds it to the 181-byte `__thiscall(this, short *)` body at `0x00406CC0..0x00406D74`; it computes the target angle through PlayerInf and drives the shared bullet-spawn loop, and Ghidra reports three real callers at `0x004062B0`, `0x00408E70`, and `0x00412670`. The existing exact `PhotoBulletManagerView::SpawnBulletPattern` source is the canonical implementation. Production EnemyShot and EclRun cast only this call to that real receiver/signature while DIFFBUILD preserves their historical proxy decorations. The fresh `/OPT:NOREF` link moves 123 -> 121 unique unresolved names and 127 -> 125 diagnostics; data stays 48 while callable/runtime drops 75 -> 73, and `0x00406CC0` leaves the unresolved target set. Both directly affected exact units replay 2/2 exact with no private-label refresh. | Hash-attested Ghidra function/decompile/caller evidence for `0x00406CC0`; canonical `PhotoBulletManagerView::SpawnBulletPattern`; fresh whole-build and exact replay, 2026-09-09 |
| ABI-038 | target-observed / compiler-observed / source-structure | `0x00436DD0` is the canonical 598-byte `ScreenEffect::RegisterChain(ScreenEffectType, int, int, int, int, int)` static helper, not separate ECL and PhotoStage functions. Hash-attested Ghidra bounds `0x00436DD0..0x00437025` as a six-argument `__fastcall` routine returning the newly allocated 0x34-byte effect record; decompilation shows effect-type dispatch to calc/draw callbacks and Chain registration. The canonical exact unit is `screen-effect-register-chain`. Production EclExtended and PhotoStage therefore call `ScreenEffect::RegisterChain` directly (`SCREEN_EFFECT_SHAKE_ENVELOPE` and `SCREEN_EFFECT_FULL_FADE_OUT` respectively), while DIFFBUILD/PhotoStage exact snapshots retain the historical `DispatchExtendedValue` and `SpawnPhotoStageEffect` decorations. The fresh `/OPT:NOREF` link moves 121 -> 119 unique unresolved names and 125 -> 123 diagnostics; data stays 48 while callable/runtime drops 73 -> 71, and `0x00436DD0` leaves the unresolved target set. Both affected sources replay 28/28 exact units with no private-label refresh. | Hash-attested Ghidra function/decompile evidence for `0x00436DD0`; canonical `screen-effect-register-chain`; fresh whole-build and affected-source replay, 2026-09-09 |
| ABI-039 | target-observed / compiler-observed / source-structure | The historical `PhotoResetTargetView::ResetForPhotoTransition` decoration was not one semantic method: target relocation evidence splits it across three canonical receivers. `0x004081B0..0x00408215` is the 102-byte `PhotoBulletManagerView::DespawnAllBullets`, which walks the 0x640 bullet slots and despawns active non-state-3 bullets; `0x00416810..0x004168CB` is the 188-byte static `PhotoEnemyManagerView::ResetNonPhotoTargetsAndPhotoTargetEcls`, which walks 128 enemies and restarts the eight photo-target ECL contexts; `0x0041E010..0x0041E055` is the 70-byte static `PhotoEffectManagerView::DrawSecondary`, which walks the effect list, invokes the secondary virtual draw/cleanup path for eligible entries, and returns 1. Production PhotoGame and EclRun now route each call to its real canonical method instead of preserving the ambiguous proxy receiver. DIFFBUILD and exact snapshots retain their historical decorated relocations. The fresh `/OPT:NOREF` link moves 119 -> 116 unique unresolved names and 123 -> 120 diagnostics; data remains 48 while callable/runtime drops 71 -> 68, all three target addresses leave the unresolved set, and the multi-target-decorated-name count falls 1 -> 0. Because `PhotoCamera.hpp` gained the already-existing canonical `DespawnAllBullets` declaration, all 88 canonical source objects were replayed; the final strict comparator is 696/696 exact. The rebuild changed 38 compiler-private `$L` names across four units only after structural bytes, relocation offset/type, and solved target destinations were proved unchanged. | Hash-attested Ghidra function/decompile evidence for `0x004081B0`, `0x00416810`, `0x0041E010`; canonical exact units `photo-bullet-despawn-all`, `enemy-reset-non-photo-and-photo-ecls`, `photo-effect-manager-draw-secondary`; full exact replay/strict compare, 2026-09-09 |

| ABI-040 | target-observed / compiler-observed / source-structure | The photo-capture target pipeline uses the canonical BulletInf and PhotoEffect manager receivers, not the historical PhotoCamera/PhotoStage proxy signatures. Ghidra bounds BulletInf `CapturePhotoTargets @ 0x00407820` as a `__thiscall(this, vector *, vector *)`, `ClearCapturedBullets @ 0x00407C90` as a single-manager routine, and `CountNearbyTargets @ 0x00408220` as `__thiscall(this, vector *, float)`. It likewise bounds PhotoEffect `CheckCollisionStored @ 0x0041DFA0` as a single-manager fastcall/static routine, `CountPhotoTargets @ 0x0041DF10` as `__thiscall(this, vec *, vec *)`, and `CountNearbyTargets @ 0x0041E060` as `__thiscall(this, vec *, float)`. Production PhotoCamera now uses canonical `PhotoBulletVector` member signatures and the real `PhotoEffectManagerView`; production PhotoStage commits captured effects via `CheckCollisionStored` and clears bullets through the real BulletInf receiver. TH095_MATCH_EXACT/DIFFBUILD retain the historical const-`Float3` and local proxy decorations. The fresh `/OPT:NOREF` link moves 116 -> 110 unique unresolved names and 120 -> 114 diagnostics; data remains 48 while callable/runtime drops 68 -> 62, and all six target addresses leave the unresolved set. The only canonical sources that consume `PhotoCamera.hpp` are PhotoCamera plus the PhotoGame/PhotoStage exact snapshots; all 39 configured units replay exact with no private-label refresh. | Hash-attested Ghidra function evidence for `0x00407820`, `0x00407C90`, `0x00408220`, `0x0041DFA0`, `0x0041DF10`, `0x0041E060`; canonical BulletManager/PhotoEffect exact units; fresh whole-build and 39-unit affected replay, 2026-09-09 |

| ABI-041 | target-observed / compiler-observed / source-structure | PhotoGameTask subsystem lifecycle calls must use the real manager classes rather than per-TU `*TaskView` proxy decorations. Ghidra independently bounds the canonical factories/destructors/restart routines at Background `Create @ 0x004024A0`, BulletInf `Create/Destroy @ 0x00404F80/0x004050C0`, EnemyInf `Destroy/RestartPhotoTargetEcls @ 0x00414B30/0x00416E30`, ItemInf `Create/Destroy @ 0x0041CCC0/0x0041CE00`, PhotoEffect `Create/Destroy @ 0x0041D790/0x0041D8D0`, PlayerInf `Create/Destroy @ 0x0042EFB0/0x0042F130`, and ReplayManager `Create/Destroy @ 0x004345B0/0x004347D0`. `0x00402620` is deliberately different: tracking and Ghidra identify it as a 95-byte compiler-generated Background scalar-deleting destructor that calls the exact authored destructor at `0x00402330` and frees the object. Production therefore uses ordinary `delete Background *`, not a fake `Destroy` declaration or wrapper. PhotoGameTask keeps already-resolved Front/Overlay/Pause and Enemy-create paths unchanged. The fresh `/OPT:NOREF` link moves 110 -> 96 unique unresolved names and 114 -> 100 diagnostics; callable/runtime drops 62 -> 48 while data remains 48, and all fourteen lifecycle target addresses leave the unresolved set. The production branch is excluded by `TH095_MATCH_EXACT`; all ten PhotoGameTask configured exact units replay exact with no private-label refresh. | Hash-attested Ghidra function/decompile evidence for the fourteen lifecycle targets; canonical exact ledgers for thirteen authored methods plus compiler-origin ledger for `0x00402620`; fresh whole-build and PhotoGameTask exact replay, 2026-09-09 |

| ABI-042 | target-observed / compiler-observed / source-structure | PhotoGameTask initialization must use the canonical `FileSystem::CheckIfFileAlreadyExists` and `GameConfiguration::Initialize` methods rather than TU-local proxy class names. Ghidra bounds `0x0041ABA0..0x0041AC4E` as the single-argument file-existence helper and `0x00418720..0x004188F6` as the single-receiver configuration initializer. `Main.hpp` already owns the real `GameConfiguration` layout at exactly 0xC8 bytes, matching PhotoGameTask +0x34 storage, and exposes the real FileSystem function. Production therefore embeds `GameConfiguration` directly, explicitly calls `runtimeConfig.Initialize()` before the historical whole-task memset, copies `g_Supervisor.config` during subsystem initialization, and calls `FileSystem::CheckIfFileAlreadyExists` directly. The fresh `/OPT:NOREF` link moves 96 -> 94 unique unresolved names and 100 -> 98 diagnostics; callable/runtime drops 48 -> 46 while data remains 48, and both target addresses leave the unresolved set. The production branch remains excluded by `TH095_MATCH_EXACT`; all ten PhotoGameTask configured units replay exact with no private-label refresh. | Hash-attested Ghidra function evidence for `0x0041ABA0` and `0x00418720`; canonical Main/FileSystem source; fresh whole-build and PhotoGameTask exact replay, 2026-09-09 |

| ABI-043 | target-observed / source-structure | Target slot `0x004C4A7C` is the canonical `g_Supervisor.currentState` field, not independent PhotoGameTask/ResultScreen globals. The hash-attested Supervisor base is `0x004C4670`; both canonical runtime layouts place `currentState` at +0x40C. Ghidra xrefs show cross-subsystem writes from Main/FrontEnd/Result code, and decompilation of `0x00423D60` initializes the slot to -1 while `0x00445E80` writes scene states 1/3/6/7 as the front-end state machine transitions. Production `g_PhotoNextState` and `g_ResultSceneState` therefore route to `g_Supervisor.currentState`; DIFFBUILD retains their historical exact-facing symbols. The fresh `/OPT:NOREF` link moves 94 -> 92 unique unresolved names and 98 -> 96 diagnostics; data drops 48 -> 46 while callable/runtime remains 46, and `0x004C4A7C` leaves the unresolved set. PhotoGameTask plus ResultScreen replay 34/34 exact units with no private-label refresh. | Hash-attested Ghidra xrefs/decompile for `0x004C4A7C`, `0x00423D60`, `0x00445E80`; canonical Supervisor +0x40C layout; fresh whole-build and affected exact replay, 2026-09-09 |
| ABI-044 | target-observed / compiler-observed / source-structure | The `0x26AE30` EnemyInf task shell and the exact `PhotoEnemyManagerView` lifecycle/update implementation are the same runtime receiver, not two managers. Ghidra decompilation of factory `0x004149F0` shows `operator new(0x26AE30)` followed by direct calls to ctor `0x00414B90` and `LoadResources @ 0x004153D0`, with failure cleanup through dtor `0x004154E0` and `free`; task callback `0x00416290` forwards the same pointer to canonical `PhotoEnemyManagerView::OnUpdate @ 0x00415970`. Production `EnemyManagerTask.cpp` therefore keeps its exact-facing task shell for `Create/Draw/callback` ownership but casts lifecycle/update calls to a same-size canonical `PhotoEnemyManagerView` partial receiver. DIFFBUILD retains the original task-view decorations. The fresh `/OPT:NOREF` link moves 92 -> 88 unique unresolved names and 96 -> 92 diagnostics; data remains 46 while callable/runtime drops 46 -> 42, all four addresses leave the unresolved set, and all five task-shell exact units replay exact with no label refresh. | Hash-attested Ghidra decompile for `0x004149F0` and `0x00416290`; canonical exact ctor/load/dtor/update units in `EnemyManagerUpdate.cpp`; fresh whole-build and 5/5 `EnemyManagerTask.cpp` exact replay, 2026-09-09 |
| ABI-045 | target-observed / compiler-observed / source-structure | The remaining EnemyInf spawn/reset proxy names are receiver spelling differences over existing exact `PhotoEnemyManagerView` methods. Ghidra bounds `Spawn @ 0x004156C0` as a 350-byte seven-argument `__thiscall` whose last argument controls the X-mirror bit, and `SpawnWithContext @ 0x00415820` as a 336-byte seven-argument `__thiscall` whose final pointer is copied as a 0x80-byte ECL context block. `ResetNonPhotoTargets @ 0x00416DD0` is a 92-byte single-manager `__fastcall` sweep that deactivates all 128 non-photo-target enemies. Production EclExtended casts its unused-return spawn edge to canonical `PhotoEnemyManagerView::Spawn`; production EclRun casts the runtime owner to canonical `SpawnWithContext` and `ResetNonPhotoTargets`, explicitly preserving its local `Enemy *` return view. DIFFBUILD retains `ExtendedPhotoEnemyManagerView::Spawn` and `Th095RuntimeManager::SpawnEnemy/ResetEnemies`. The fresh `/OPT:NOREF` link moves 88 -> 85 unique unresolved names and 92 -> 89 diagnostics; data remains 46 while callable/runtime drops 42 -> 39, all three addresses leave the unresolved set, and EclExtended/EclRun replay 23/23 exact units with no label refresh. | Hash-attested Ghidra function/decompile evidence for `0x004156C0`, `0x00415820`, `0x00416DD0`; canonical exact EnemyManager units; fresh whole-build and 23/23 affected exact replay, 2026-09-09 |
| ABI-046 | target-observed / compiler-observed / source-structure | Four EclExtended helper proxies are target-identical to already exact canonical helpers: `ExtendedAnmSpawner::InitializeVm -> AnmLoaded::InitializeVm @ 0x00404B80`, `AnmManagerLookupView::ExecuteScript -> AnmManager::ExecuteScript @ 0x0043A600`, `ExtendedPhotoCameraView::CountPhotoTargets -> PhotoCameraState::CountPhotoTargets @ 0x004339F0`, and `EclExtended::PhotoToScreen -> th095::PhotoToScreen @ 0x004186D0`. Ghidra confirms `0x00404B80` as an ANM-loaded `__thiscall` VM initializer, `0x004339F0` as a three-argument `__thiscall` camera scan, and `0x004186D0` as the two-pointer `__fastcall` coordinate transform; canonical exact units establish all four identities including the large `0x0043A600` script executor. The ECL production partial header therefore declares `AnmLoaded::InitializeVm` and static `AnmManager::ExecuteScript` only outside `TH095_MATCH_EXACT`; EclExtended uses canonical helpers while DIFFBUILD retains historical proxy decorations. The local camera view and canonical camera state are both `0xBDC` bytes with viewfinder position at `+0xBC4`. The fresh `/OPT:NOREF` link moves 85 -> 81 unique unresolved names and 89 -> 85 diagnostics; data remains 46 while callable/runtime drops 39 -> 35, all four addresses leave the unresolved set, and the shared-header regression replay covers all 88 configured sources / 696 canonical units with no exact failure or label refresh. | Hash-attested Ghidra function/decompile evidence for `0x00404B80`, `0x004339F0`, `0x004186D0`; canonical exact ledgers for all four addresses including `0x0043A600`; fresh whole-build and complete 696-unit exact replay, 2026-09-09 |
| ABI-047 | target-observed / compiler-observed / source-structure | The ECL interpreter's `CallEclSub @ 0x00408DE0` and `RunEcl @ 0x00408E70` use the global target `::ZunResult` enum in their VC7.1 decorated return type; `th095::ZunResult` is a distinct ABI identity even though both enums encode success/error as 0/-1. The canonical RunEcl relocation ledger directly resolves `?CallEclSub@EclManager...AW4ZunResult@@...` to `0x00408DE0`, overturning the earlier “no target mapping” hypothesis. Ghidra confirms the 140-byte `0x00408DE0` body is a `__thiscall` context initializer reading the manager subroutine table at `+0x4`; its writes at context `+0`, `+0x98`, and `+0x22C` match both `EnemyEclContext` and the historical photo-context view. Production therefore keeps `::ZunResult` available, uses it for `EclRunResult`, defines the same source body as canonical `EclManager::CallEclSub`, and routes PhotoEnemy/EclDependencies/PhotoRuntime context calls plus photo-enemy RunEcl calls through canonical `EclManager` receiver prefixes. DIFFBUILD retains the exact-facing `PhotoEnemyEclManagerView::InitializeContext` declaration/body and relocations; no duplicate body/storage is introduced. The fresh `/OPT:NOREF` link moves 81 -> 79 unique unresolved names and 85 -> 83 diagnostics; data stays 46, callable/runtime drops 35 -> 33, no-target drops 3 -> 2, and the full shared-header replay remains 696/696 exact with no label refresh (429/429 first 48 sources + 267/267 remaining 40). | Canonical EclRun relocation ledger for `0x00408DE0`/`0x00408E70`; hash-attested Ghidra function/decompile evidence for `0x00408DE0`; matching context-layout assertions; fresh whole-build and complete 696-unit exact replay, 2026-09-09 |
| ABI-048 | target-observed / compiler-observed / source-structure | The six remaining Main-side MIDI calls were ABI mismatches over already exact `MidiOutput` methods, not missing implementations. Canonical exact symbols at `ReadFileData @ 0x004221B0`, `ParseFile @ 0x00422300`, `Play @ 0x00422600`, `StopPlayback @ 0x00422660`, `UnprepareHeader @ 0x004226E0`, and `SetFadeOut @ 0x004227B0` encode the global retail `::ZunResult` return enum; `ReadFileData` encodes `const char *`. Hash-attested Ghidra independently bounds those bodies to 114/547/81/122/199/79 bytes. Production Main had retained an `i32/void` + mutable-`char *` partial declaration, while production `Midi.hpp` selected the distinct `th095::ZunResult` enum. `MidiRuntime.hpp` now provides the production pointer-only canonical method view, `Main.hpp` keeps its historical view only under `TH095_MATCH_EXACT`, and production `MidiResult` uses `::ZunResult`, matching the implementation to the target ABI without wrappers or aliases. Fresh `/OPT:NOREF` moves 79 -> 73 unique unresolved and 83 -> 77 diagnostics; data remains 46, callable/runtime drops 33 -> 27, all six addresses leave the unresolved set, and the complete 696-unit canonical strict compare remains exact with zero failures and no private-label refresh. | Canonical exact MIDI ledgers; hash-attested Ghidra function evidence for all six addresses; fresh whole-build; complete 696-unit exact replay/strict compare, 2026-09-09 |


## Analysis control plane

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| RE-001 | observed | Ghidra 12.1.3 with JDK 21 can import and analyze the target headlessly. | Pinned local bootstrap and attested import |
| RE-002 | policy | Ghidra function extents and names are provisional until reconciled against target control flow and raw bytes. Secondary disassembler observations do not supersede the attested Ghidra/target workflow. | `AGENTS.md` and `RE_WORKFLOW.md` |
| RE-003 | policy | Mapping, origin, source presence, semantic acceptance, and exactness are separate ledgers. | Config schemas and tracking validator |
| RE-004 | observed | The initial attested import exported 1,830 `.text` function candidates. | `scripts/ghidra.py import`; generated ledgers |
| RE-005 | tool-observed | Objdiff 3.8.0 renders an empty zero-unit project as 100%; this is not progress. | Target-independent empty-project smoke report |
| RE-006 | target-observed | The architecture export contains 1,830 metric rows and 3,873 direct call edges. | Attested read-only `scripts/ghidra.py architecture` export on 2026-08-29 |
| RE-007 | superseded tool-observed | An independently attested TH095 IDA database existed on 2026-08-30, but IDA is not the live project backend. A connection to another executable is never TH095 evidence. | Historical `get_metadata`/`get_entry_points` record; superseded by the Ghidra-primary workflow |
| RE-008 | tool-observed / policy | The private Ghidra 12.1.3 project is the primary semantic backend and is independently re-attested before each headless operation by SHA-256, MD5, module, image base, entry function, and six distributed 16-byte `.text` samples read from the canonical executable. The wrapper requires an exact identity-bearing success marker because a deliberate wrong-hash script failure showed that `analyzeHeadless` itself may still exit zero; mismatch handling also sets headless `ABORT`, and a deliberate byte mismatch proved that the following query script produced no output. | `scripts/ghidra.py`; `scripts/ghidra/VerifyTarget.java`; positive check on 2026-09-08 plus prior wrong-hash and mapped-byte-negative tests |
| RE-009 | target/library-observed | Direct-call/prologue-only discovery is insufficient. The reproducible follow-up checks conditional branches, code-address immediates, aligned/unaligned pointers, incomplete decodes, overlaps, and embedded tables. Earlier gap counts and the old Joystick symbol identity at 0x00497EEC are superseded by the reviewed evidence in BOUNDARY_AUDIT.md; a clean authored gate is not a completeness certificate. | scripts/audit-authored-boundary.py; docs/BOUNDARY_AUDIT.md |
| RE-010 | library-observed | Several post-authored `FUN_xxx` rows are pinned-library code rather than missed TH095 source. VC7.1 `d3dx8.lib` reproduces `D3DXMatrixLookAtLH @ 0x00459E49`, `D3DXMatrixPerspectiveFovLH @ 0x00459F91`, and five texture/surface entrypoints at `0x0045BCBF..0x0045CE67` byte-for-byte outside relocation fields. VC7.1 `libcmt.lib` independently reproduces `__ftol2`, `__CIsqrt`, `__CIcos`, `__CIsin`, `__CIatan2`, `__CIfmod`, `__mkdir`, `__CItan`, `_floor`, `__CIatan`, and `__CIacos`. This audit also catches a real Ghidra boundary failure: `_floor @ 0x004865C0` is only 64 bytes, while the distinct `__floor_pentium4 @ 0x00486600` is a 225-byte function that Ghidra did not inventory at all; its target body is 197/197 non-relocation bytes exact against the pinned CRT object. `PrepareReplayDirectory` was a reconstruction-only unresolved alias for target CRT `__mkdir`, not an authored helper. | Extracted pinned `d3dx8.lib` / `libcmt.lib` COFF members and target structural comparisons in `.analysis/gpt-web-boundary-libprovenance-20260907` |
| RE-011 | target/compiler-observed | 49 missing EH handlers were recovered from 57 canonical references and classified as compiler exclusions. Their 72 FuncInfo cleanup targets were then bounded from associative `.text$x` COFF function symbols and replayed through 81 parent/action references using only handler-relative or pre-existing canonical relocation anchors; 81/81 are provenance-exact and all 72 are now compiler exclusions. Parent-body exactness alone is therefore insufficient evidence for EH action provenance. | `audit-authored-boundary.py --verify-eh-coff`; `audit-eh-cleanups.py --require-all`; `test_eh_cleanups.py`; BOUNDARY_AUDIT.md |
| RE-012 | target/library-observed | Target 0x00497EEC is SDK c_dfDIJoystick2: state size 272, 164 objects, stride 16. Pinned dilib4 defines this format; legacy Joystick in dilib3 has state size 80 and 44 objects. Main and its relocation now name Joystick2. Numeric relocation equality alone cannot establish symbolic provenance. | SDK header/library; main-setup-dinput; test_authored_boundary.py |
| RE-013 | target/compiler-observed | Four ANM operand-resolver tables (192 bytes) were wrongly counted as authored; retain them with compare_size while crediting bodies of 398/186/152/137 bytes. Best-shot reset instead needs all 189 instruction bytes, including the complete RET 4. Net -190 bytes in authored total and exact credit; no comparison range or exact-function count was reduced. | Target control flow; full canonical replay; reviewed-extent tests; BOUNDARY_AUDIT.md |
| RE-014 | target/library/compiler-observed | The frozen 1,000-row post-authored review cohort is closed: 908 candidates are library-owned and 92 are compiler-generated, leaving zero review rows. Direct parsing of pinned VC7.1 D3DX8/CRT archives and the target PE proves 932 origins exactly (3 imports, 793 ordinary COFF functions, 47 associative functions, 89 candidates inside exact contributions). Attested Ghidra control flow plus pinned shapes closes the remaining 68 at high confidence, including the D3DX dispatch initializer, short dispatch thunks, EH funclets, and 26 static initializer/terminator bodies at the end of `.text`. Candidate classification changes neither the 697 authored-function denominator nor byte-exact credit. | `audit-runtime-origins.py`; `apply-runtime-origin-review.py`; `test_runtime_origins.py`; `BOUNDARY_AUDIT.md`; evidence ID `runtime-origin-boundary-review-2026-09-08` |

## Runtime architecture

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| ARCH-001 | target-observed | CRT entry `0x00486A9D` calls `WinMain` at `0x00420240`; the latter owns configuration, window/D3D, sound-worker, ANM allocation, message/frame loop, restart, and teardown. | Target call/control flow and system API xrefs |
| ARCH-002 | corroborated | The TH095 `GameWindow`, `Supervisor`, `SoundPlayer`, `AnmManager`, and ECL layers share source ancestry with TH08. | Matching responsibilities, call shapes, strings, and multiple equal function extents; TH08 exact source |
| ARCH-003 | exact | `0x00408E70` is the 27,091-byte `EclManager::RunEcl` dispatcher with 50 internal callees. Its COFF extent additionally owns 656 bytes of switch tables. | Canonical `ecl-manager-run-ecl` unit and architecture metrics |
| ARCH-004 | exact | `0x0043A600` is the 17,018-byte `AnmManager::ExecuteScript` ANM opcode dispatcher called by 22 target functions. Its COFF auxiliary extent additionally owns 408 bytes of switch tables. | Canonical `anm-execute-script` unit and architecture metrics |
| ARCH-005 | target-observed | TH095 uses a dedicated SoundPlayer worker thread; `0x00437790` creates it and `0x004377F0`/`0x00437810` request and join shutdown. | CreateThread state and WaitForSingleObject/CloseHandle control flow |
| ARCH-006 | inferred | `0x00430AB0` is a target-specific photography hub and `0x00447D00` is a target-specific scene-selection hub; neither may inherit a TH08 class name without further proof. | TH095-only state machines, camera/photo/scene evidence, and unresolved original owning types |
| ARCH-007 | exact | TH095 splits SFX loading between a producer worker at `0x00437CD0` and the `InitializeDSound` consumer path. Thirty-seven owned file-data slots begin at `SoundPlayer+0x5230`; `LoadSound` waits for a slot, parses RIFF/WAVE data, then creates the DirectSound buffer. | Canonical worker, `InitializeDSound`, `LoadSoundData`, and `LoadSound` units |
| ARCH-008 | target-observed | A boundary, call-graph, global-xref, and string audit identifies 32 additional authored functions totaling 55,476 bytes: an eleven-function photography/camera cluster rooted at `0x00430AB0`, an eleven-function replay/menu/best-shot cluster rooted at `0x00426BF0`, and a ten-function gameplay/resource cluster rooted at `0x00447D00`. These subsystem labels describe target-proven responsibilities; they are not recovered original class names. | Attested cluster decompiles, terminal-instruction/padding audit, private caller/callee edges, and target strings including `th95_%.2d.rpy` and `bestshot/bs_*.dat` |
| ARCH-009 | exact | `0x00426BF0` is the member update dispatcher for the target-proven `ResultScreen` layout. Its 45-byte chain wrapper at `0x0042A6B0` calls it directly, while independently exact cursor, timer, replay-slot, and VM-offset dependencies establish the owning type. | Canonical `result-screen-on-update` relocation plus six adjacent exact units and the attested dispatcher body |
| ARCH-010 | exact | `0x00429C80` is the `ResultScreen` member draw path. Its 45-byte suppression wrapper at `0x0042A6E0` calls it directly; the draw body covers best-shot statistics, twenty replay-slot rows, replay-name metadata, and the 6x16 keyboard. | Canonical `result-screen-on-draw` relocation and attested target draw body |
| ARCH-011 | exact | The five-function cluster at `0x004364F0..0x00436751` implements TH095 scene progression: twelve scene groups index 120 persistent score records, and group availability can be satisfied by total score plus the preceding group's unlock score, global captures, or preceding-group captures. The descriptive `SceneSaveDataView` name is target-local repository terminology, not a recovered original class name. | Five canonical VC7.1 units totaling 586 bytes with thirteen relocations replayed and compile-time layout assertions |
| ARCH-012 | exact | The intrusive calc/draw Chain core at `0x00418900..0x00419399`, plus its destructor at `0x0041BD20`, is exact end to end. It owns priority insertion, callback result dispatch, synchronized unlink, reentrant snapshot release, and heap-node lifetime. TH095 stops the replay-scan worker before releasing both lists and directly allocates Chain nodes rather than routing them through TH08's `ZunMemory` registry. | Thirteen canonical Chain units totaling 2,631 authored bytes; both adjacent switch tables and all 127 relocation fields replayed |
| ARCH-013 | exact | `ZunMemory::~ZunMemory @ 0x0041BD70` frees every non-null registry slot only when the registry-active byte is set. | Canonical 84-byte `zun-memory-destructor` unit with `_free` relocation replayed |

## Reconstructed ECL dependency lane

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| ECL-001 | exact | `ConfigurePolarMotion @ 0x00411150` and `ConfigureRelativeMotion @ 0x00411390` are the TH095 movement setup helpers called by the exact low-opcode dispatcher. TH095 keeps their movement-control word at enemy `+0x2BF4`: movement mode is bits 10..11, easing is bits 12..14, and X mirroring is bit 16. | Two canonical VC7.1 units totaling 1,083 bytes with fourteen relocations; TH08 ancestry plus exact TH095 operand resolvers and RunEcl call sites. |
| ECL-002 | exact | The interpolation dependency cluster at `0x004115A0..0x004119F7` contains linear and cubic-Hermite callbacks, the one-shot interpolation operation, and the eight-slot installer. The installer selects callbacks through the table at `0x004A4250`. | Four canonical units totaling 1,095 authored bytes with thirty relocations. The Hermite callback reproduces TH08 `var_order` with eight real scalar locals; `bufferLocal04` is a verified eighth stock-VC7.1 identifier bucket. |
| ECL-003 | exact | `CompareOperands @ 0x00411A00` implements opcodes 40..51 and resets the complete context `ZunTimer` on a taken branch, not only `time.current`. Its authored body ends at `0x00411F35`; `0x00411F36..0x00411F65` is the compiler-owned twelve-entry switch table, not a function. | Canonical 1,334-byte authored unit with `compare_size=0x566`; body plus 48-byte table reproduce 1,382 bytes and all 37 relocations. |
| ECL-004 | exact | `CallSubOnEnemy @ 0x00411F70`, `PopEclContext @ 0x00412060`, and `SetPrimaryAnmScripts @ 0x00412190` close the low-ECL call/return/script dependency spine. Call-stack suppression is bit 24 of enemy `+0x2BF4`; sub-call parameters copy from the TH095 ECL manager `+0x168`; return cleanup frees enemy allocation slot `+0x2CAC[index]`; the six primary ANM scripts live at `+0x2C0E/+0x2C14/+0x2C16/+0x2C10/+0x2C12/+0x2C18`, with direction byte `+0x2C0A`. | Three canonical units totaling 621 bytes with six relocations; exact aggregate context copies and target-local bounded ABI views. |

## Reconstructed controller input

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| INPUT-001 | exact | The five controller helpers at `0x004193A0..0x00419ADB` reproduce 1,820 authored bytes and all 67 relocation fields. They cover joystick discovery, per-slot controller mapping, both button-bit helpers, and raw controller-state acquisition. | Five canonical `controller-*` units under pinned VC7.1 build 3077 |
| INPUT-002 | target-observed | TH095 owns `0x8E`-byte input slots at `0x004BE218`. Each slot begins with current, previous, repeat, pressed, and released masks plus sixteen held-frame counters; joystick bindings for shoot, bomb, focus, and menu are at `+0x58..+0x5E`. | Exact controller units, target field accesses, and compile-time layout assertions |
| INPUT-003 | exact | `Controller::GetControllerInput @ 0x00419410` selects WinMM or DirectInput through runtime flag bit 11, applies three independently configured bindings, and adds X/Y axis directions. TH095 selects one of two devices for each input slot instead of TH08's single-controller path. | Canonical 1,030-byte unit with 37 replayed relocations and TH08 source-shape corroboration |
| INPUT-004 | compiler-observed | `Controller::GetInput @ 0x00419AE0` is source-present for the complete TH095 keyboard and dual-controller aggregation path. The target-proven identifier buckets are now canonical: all 212 observed EBP-relative operands land exactly at the target homes, with the seven real values occupying `buttons/inputSlot/result/bitIndex/repeatMask/currentBits/inputIndex @ -0x104..-0x11C`. The remaining 2,655-versus-2,662 residual is register scheduling plus a target-only unreferenced tail: target reserves `-0x120/-0x124`, while both bodies retain all 579 mnemonics. A phase-only diagnostic reaches 2,573/2,574 comparable bytes with all 22 relocations solved but leaves the `0x11C` frame; an eight-byte hidden-return diagnostic reaches the target `0x124` frame but moves all seven `inputIndex` references to `-0x124`. The former uses nine inert expressions and the latter dead temporary storage, so neither is canonical evidence. Declaration order, declaration initializers, scalar-vs-array global views, bitfield vs shift/mask spelling, `GetKeyboardState` address forms, comma/ternary condition variants, real-operation-owned 8-byte phase probes, natural block-lifetime variants for the HRESULT/loop locals, and syntactically live empty-UDT member/default-reference variants do not close both effects; initializing real UDT state instead grows the body to 2,693 bytes. | Attested 2,662-byte target, canonical exact-home source, TH08 exact `GetInput` object, pinned-VC7.1 register/operand crosswalks, complete `p2-*` canonical rescore, and bounded source-shape probes |
| INPUT-005 | exact | `Controller::GetControllerState @ 0x00419910` expands 32 WinMM buttons or copies 128 DirectInput buttons. The original DirectInput path discards `GetDeviceState`'s result and rechecks the earlier `Poll` result; the reconstruction preserves this behavior. | Canonical 460-byte unit with 23 replayed relocations |
| INPUT-006 | exact | `Controller::ResetKeyboard @ 0x0041A550` clears the high bit of all 256 Win32 keyboard-state bytes while preserving toggle bits, then writes the state back. | Canonical 110-byte unit with both Win32 import relocations replayed |
| INPUT-007 | exact | Each `0x8E`-byte controller input slot owns three nine-entry `ControllerMapping` profiles at `+0x58`: joystick defaults, Win32 virtual-key defaults, and DirectInput scan-code defaults. `ControllerInputSlotView::ControllerInputSlotView @ 0x0041BC00` initializes all 27 signed 16-bit bindings, and the target's compiler-generated static vector initializer constructs three consecutive slots at `0x004BE218`. | Canonical relocation-free 278-byte constructor; attested `0x00493F70` vector-initializer loop classified as compiler-owned |
| INPUT-008 | target-observed / production | The runtime input globals are views into the same storage rooted at `0x004BE218`, not independent production objects. `ReplayManager::ProcessFrame @ 0x00434830` passes that base to exact `ReplayInputSource::Update @ 0x004353B0`, shifts `historyCurrent/historyPrevious @ +0x2C/+0x2E` (`0x004BE244/246`), and copies the pressed snapshot from `+0x06` (`0x004BE21E`) into history-pressed `+0x32` (`0x004BE24A`). Frontend result/menu input observes `+0x04`, frontend pressed input observes `+0x06`, and replay released history is `+0x34` (`0x004BE24C`). The historical multi-target `g_PhotoInput` token must still be classified by use: ordinary reads use current history, while the capture edge uses pressed history. | Hash-attested Ghidra xrefs/decompile for `0x004BE218/21C/21E/244/246/24A/24C` and `0x00434830`; exact `ReplayInputSource` layout; fresh 88-TU whole build; 696/696 canonical strict replay |

## Reconstructed file system

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| FILE-001 | exact | `FileSystem::Decrypt` and `Encrypt @ 0x0041A5C0/0x0041A790` copy at most `maxBytes` into a temporary buffer, apply the odd/even chunk permutation and advancing XOR key back into the original allocation, free the temporary, and return the original pointer. This differs from TH08's allocate-and-return codec shape. | Two canonical 464-byte VC7.1 units with all four CRT relocations replayed; TH08 used only as a source-shape contrast |
| FILE-002 | exact | `FileSystem::OpenFile @ 0x0041A960` serializes through critical section 2, strips path components for archive lookup, allocates and decompresses archive entries, or falls back to sequential loose-file I/O. Every exit decrements the active-count byte at `0x004C4D7E`. | Canonical 576-byte unit with all 37 archive, allocator, string, debug, Win32, and global relocations replayed |
| FILE-003 | exact | `FileSystem::CheckIfFileAlreadyExists @ 0x0041ABA0` performs a read-only loose-file probe under the same critical section and active-count protocol. | Canonical 175-byte unit with all 14 relocations replayed |
| FILE-004 | exact | `WriteDataToFile @ 0x0041AC50`, `OpenWriteFile @ 0x0041ADC0`, `WriteToOpenFile @ 0x0041AF80`, and `CloseWriteFile @ 0x0041B090` form the serialized write lane. Open increments the critical-section-2 active count and intentionally keeps the section held across subsequent writes; close releases it. `0x0041ADC0/0x0041AF80/0x0041B090` are also reached through `ReplayFile` and `ScoreFileWriter` semantic aliases, but each target address receives authored credit only once. | Four canonical units totaling 804 bytes plus exact caller relocations from Main, ReplayManager, and ScoreData. |
| FILE-005 | exact | The shared open-file HANDLE is `0x004A4328`. Write/create uses `GENERIC_WRITE | FILE_SHARE_READ | CREATE_ALWAYS`; replay read-open uses `GENERIC_READ | FILE_SHARE_READ | OPEN_EXISTING | FILE_FLAG_SEQUENTIAL_SCAN`. Failed opens call `FormatMessageA` with flags `0x1300` and language `0x400`, log the formatted system error, and balance critical section 2. | Canonical open/create units and solved CreateFileA, FormatMessageA, LocalFree, debug-string, handle, critical-section, and active-count relocations. |
| FILE-006 | exact | `ReplayFile::Read @ 0x0041B020` mallocs the requested byte count and reads through the shared handle. A real two-field aggregate `{data, bytesRead}` is required for target homes `EBP-0x08/-0x04`. `WriteDataToFile` analogously uses `{errorMessage, handle, bytesWritten}` to reproduce `EBP-0x0C/-0x08/-0x04`. These are live semantic aggregates, not frame padding. | Canonical 107- and 362-byte units; every aggregate field is referenced by target instructions. |

## Reconstructed error logging core

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| ERROR-001 | exact | `GameErrorContext::Log/Fatal @ 0x0041B0F0/0x0041B280` are variadic member-`__cdecl` functions: `this` is stack-passed, not a normal thiscall register argument. Both append formatted text to the 8 KiB context buffer under critical section 3; `Fatal` uses a 512-byte temporary and sets `showMessageBox @ +0x2004`, while `Log` uses an 8 KiB temporary. `bufferEnd` is at `+0x2000`. | Two canonical units totaling 777 authored bytes and 19 relocations; target stack frames and `Global.hpp` layout agree. |
| ERROR-002 | exact | With `/Oi`, the natural TH08-style `vsprintf` + `strlen` + `strcpy` source expands to the target's inline string scan/copy loops. Critical-section-3 activity is tracked by byte `0x004C4D7F`. | Exact logger/fatal code plus solved critical-section, active-count, `_vsprintf`, and stack-probe relocations. |

## Reconstructed music room

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| MUSIC-001 | exact | `MusicRoomView::UpdateMusicRoom @ 0x00450FC0` is the complete TH095 music-room state machine embedded in the shared title controller. It reads `sprt/musiccmt.txt`, supports at most 32 tracks with a 64-byte path/title and eight 64-byte comment lines each, stages the title/comment VMs, uses the shared menu cursor, starts the selected track, and restores `bgm/th095_00.wav` on exit. The exact source has a fully live 12-byte parser aggregate `{fileSize, trackCount, fileCursor}`, lexical loop locals, handle-member `GetVm` calls, shared final return, and an exit label inside the Back-input body. Source-local inline helpers split temporary allocation phases: fixed VM creators and the owned comment buffer live in inline call-site phases, while dynamic VM creators remain outer-function return objects. A direct-return `changed && current % 2 == 0` helper makes VC7.1 synthesize the target deep boolean homes at `EBP-0xAC/-0xB0`; naming the helper result moves them into the wrong allocation phase. | Canonical 2,872-byte VC7.1 unit with all 76 relocations replayed; the target `0xB4` frame has no unused dword slot, and both exact CR/LF parser helpers replay unchanged |
| MUSIC-002 | exact | `SkipMusicCommentLine` and `ReadMusicCommentLine @ 0x00451B00/0x00451B90` handle CR, LF, and CRLF input while decrementing the caller-owned remaining-byte count; the reader terminates the source line in place and copies it to the fixed destination. | Two canonical relocation-free VC7.1 units totaling 374 bytes |
| MUSIC-003 | compiler-observed | `SceneValueQueue::Pop @ 0x00450F60` removes the first of sixteen integer values by shifting the complete array and is called by four target scene/front-end hubs. Its natural VC7.1 body is 93 bytes versus the 91-byte target solely because it saves/restores `esi`. | Attested target body and four-caller graph; non-canonical pinned-VC7.1 probe |

## Reconstructed Help viewer

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| HELP-001 | exact | `HelpMenuView::UpdateHelpMenu @ 0x00451C80` is the complete five-state TH095 Help viewer. It owns nine `help_%.2d.anm` pages, uses texture slot 13, gates input for twenty timer ticks, and recreates the nine selection VMs when a page closes. Source-local force-inline helpers keep each `CreateVm` return temporary and the page-data ownership pointer in call-site allocation order; lexical loop indices, cursor getters, timer comparison, and the shared `load_page` body reproduce the target without padding. | Canonical 2,358-byte authored unit with a 2,378-byte compare extent, including the five-entry compiler switch table, and all 97 relocations replayed |
| HELP-002 | exact | `LoadHelpAnm @ 0x004525D0` loads the requested Help ANM through the shared archive-or-disk `FileSystem::OpenFile`, moves the owner state to 3, clears the active flag, and sets the completion flag. | Canonical 88-byte VC7.1 unit with all four relocations replayed |
| HELP-003 | target-observed | The shared title-controller layout places the cursor at `+0x20`, VM handle array at `+0xBF4`, page-selection handles at `+0xE38`, transition handle at `+0x6100`, state at `+0x610C`, path buffer at `+0x6408`, data size at `+0x650C`, and owned file data at `+0x6510`. | Target field accesses and compile-time layout assertions |

## Reconstructed front-end controller

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| FRONT-001 | exact | `SceneSelectControllerView::Draw @ 0x00452630` is canonical exact for all 1,851 authored bytes and 78 relocations. State 2 renders total/high scores, capture count, slow/success rates, and the cleared-scene marker; state 3 renders four pages of twenty numbered/user replay records. The exact `0x98` frame is fully live: a bounded force-inlined state-2 helper moves its six `Float3` plus two scalar locals into the target deep allocation phase, four replay page/selection reads use a force-inlined identity snapshot, and the two mutually exclusive replay-format branches share one scene buffer, one level buffer, and one user-id buffer. Target-proven VC7.1 identifier buckets order only these real locals; no inert storage or padding is present. | Canonical `scene-controller-draw` unit: 1,851/1,851 bytes, all 446 instruction mnemonics, all 78 relocation fields, and the exact `0x98` frame replay against the attested target |
| FRONT-002 | exact | The update/draw chain wrappers at `0x00445E40/0x00445E60` are each 19 bytes and call `SceneSelectControllerView::Update @ 0x00445E80` and `Draw @ 0x00452630` respectively. The update target fans out to scene selection, replay browser, options, Music Room, and Help viewer. | Two canonical VC7.1 units with their sole REL32 relocations replayed; attested target call graph |
| FRONT-003 | target-observed | Scene-score entries store slow rate at `+0x48`, success rate at `+0x4C`, and flags at `+0x50`. The shared front-end controller stores replay page/selection at `+0x20/+0xF8`, selected scene score at `+0xBF0`, eighty replay pointers at `+0xEA8`, outer state at `+0x6110`, and rate-display flag bit 3 at `+0x6120`. | Target field accesses and compile-time layout assertions; thirteen affected canonical units replayed unchanged |
| FRONT-004 | exact | `SceneSelectControllerView::Update @ 0x00445E80` is the complete ten-state shared coordinator. It clears the 256x192 title texture in 16- or 32-bit form, creates the common title VMs, routes every front-end page, creates game/replay managers, completes their transitions, synchronizes paired wraparound VMs, and ticks both timers. The three transition VM creations must assign the returned four-byte `SceneAnmVmId` wrapper as a whole into the representation-compatible transition handle. Extracting and assigning only `.value` makes VC7.1 load `this` before materializing the return, rotating the later allocator chronology and producing the former one-byte net residual across three global loads and two `Float3` copies. | Canonical 2,969-byte authored body plus the adjacent 40-byte ten-entry switch table; all 101 call/global/constant/table relocations replayed. The exact unit preserves the `0xC0` frame, all 714 instructions, all 57 calls, and compile-time layouts through `+0x6124`. |
| FRONT-005 | exact | `UpdateMainMenu @ 0x00446A50` is the complete outer-state-1 title menu. It creates three six-VM layers, implements wrapped input and twenty-tick gating, launches one of three idle demo replays after 1,800 frames, drains both scene queues before Game Start, and routes Replay/Music/Options/Help/Quit. Both queue drains use the same source-local `FrontEndDrainQueueValue` phase: the real inline `Size()` result remains in the caller, while a four-byte compiler-storage reservation is owned by the direct `Pop()`/free expression. This places the two target queue phases at `EBP-0x144/-0x150`; storing the Pop return in a named local is a four-byte negative oracle. | Canonical 3,299-byte authored body plus the adjacent 24-byte six-entry switch table, with all 68 relocation fields and all 3,323 compared bytes replayed exactly. |
| FRONT-006 | exact | `CloseMainMenu @ 0x00447750` sends interrupt 1 to the twenty title-menu VMs in target order; `UpdateMainMenuSelection @ 0x004479E0` sends interrupt 2/3 to eighteen VMs according to the selected row. | Canonical 648- and 787-byte VC7.1 units with all 76 relocations replayed |
| FRONT-007 | target-observed | The coordinator identifies `UpdateSceneSelect @ 0x00447D00` as outer state 2; it is the 16,066-byte root of the existing exact scene-unlock/preview/detail dependency cluster. | Unique target dispatch call site and state-dependent VM/resource behavior |
| FRONT-008 | exact | The shared TH095 front-end owner is a `0x6514`-byte object. Its constructor at `0x00445440` naturally constructs timers at `+0x08/+0x14`, replay cursors at `+0x20/+0xF8`, twelve scene cursors from `+0x1D0`, and nine sixteen-entry queues from `+0x6128` through `+0x6368`, then clears the complete object and publishes the singleton. `Initialize`, `LoadThread`, `LoadResources`, `ReleaseResources`, `Create`, and `Destroy` close title ANM, mission-message, sound, photo-resource, Chain, worker, and allocation ownership. | Seven canonical VC7.1 units totaling 1,994 authored bytes with all 95 relocations replayed; target-local member-construction order and compile-time layout assertions |
| FRONT-009 | exact / compiler-observed | `FrontEndLifecycleView::~FrontEndLifecycleView @ 0x00445AA0` performs the complete cleanup: it commits pending best-shot state, deletes eighty replay managers, unlinks both Chains, clears the singleton, frees replay/message data, drains both preview-data queues, and frees six pending preview buffers. The former compiler-local-home probe is superseded by the canonical exact unit. | Canonical `front-end-lifecycle-destructor`: 503/503 bytes with all nineteen relocation destinations replayed |

## Reconstructed main family

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| MAIN-001 | target-observed | `GameWindow` timer fields used by Render are at `+0x34`, `+0x3C`, and `+0x44`; QPC frequency/start are at `+0x14` and `+0x1C`. | Target instructions and exact Render relocations/calls |
| MAIN-002 | target-observed | `Supervisor::config.frameskipConfig` is at Supervisor `+0x1CC`; fog-state cache is at `+0x768`; seven critical sections begin at `+0x664`. | Exact Render DIR32 addends and init/delete loops |
| MAIN-003 | exact | The configuration file is 200 bytes and validates version `0x95001`; missing or invalid data is reinitialized and `./thbgm.dat` selects wave or MIDI mode. | Canonical `main-load-config` unit at `0x00424D30` |
| MAIN-004 | compiler-observed | The canonical Main/D3D family compiles with `/MT /EHsc /Gs /DNDEBUG /Zi /Gy /GF /Oi /Gr /Od /Ob1`. | Forty-eight canonical `main-*` VC7.1 units plus the exact viewport translation unit |
| MAIN-005 | exact | `InitD3DRendering` at `0x00420E20` and `ResetRenderState` at `0x00421380` reproduce the complete D3D device-creation and fixed-function state paths. | Canonical `main-init-d3d-rendering` and `main-reset-render-state` units |
| MAIN-006 | exact / compiler-observed | `WinMain @ 0x00420240` is canonical exact for the complete process lifecycle. The natural source already had all six allocation/delete `$T` homes exact but placed the `new AnmManager`, `delete AnmManager`, and `delete MidiOutput` merge-result family one dword shallow. The target-attested four-byte class belongs to the smallest real ownership frontier: publishing the newly constructed `AnmManager` into `g_AnmManager`. A source-local force-inline identity carrying exactly four bytes moves only that lifecycle merge family to target homes; no-phase is 782/790, eight bytes moves the same family one dword too deep, and function-scope four bytes moves unrelated homes. | Canonical `main-winmain`: 1,326/1,326 authored bytes, 790/790 non-relocation comparable bytes, all 134 relocations; all 47 sibling `Main.obj` units replay exact after a destination-audited private-label refresh in `UpdateSceneState` |
| MAIN-007 | exact | Serialized controller configuration contains six packed 0x12-byte bindings. Runtime controller state holds them as two groups of three separated by 0x58 bytes; initialization and validated loading copy the groups in opposite directions. | Exact `main-config-initialize` and `main-load-config` field accesses and relocations |
| MAIN-026 | target-observed / production | The 0xC8-byte `GameConfiguration` is the real `Supervisor::config` at `0x004C4670 + 0x11C = 0x004C478C`; `0x004BE270` is a distinct process-lifetime `ControllerMapping` POD. `GameConfiguration::Initialize @ 0x00418720` copies the two three-binding groups from `.BE270` into config, validated `Supervisor::LoadConfig @ 0x00424D30` copies them back, and `PhotoGameTaskView::InitializeSubsystems @ 0x00417A70` snapshots all 50 dwords from `.478C`. The exact Options tail independently writes config at `0x00450B3A`, reads it at `0x00450B65`, and persists the binding at `0x00450B6B`; the historical `g_OptionsGameConfig` spelling therefore denotes two target objects at different expressions and must never be blanket-aliased. | Hash-attested Ghidra xrefs/decompilation for `0x004C478C`, `0x004BE270`, `0x00418720`, `0x00424D30`, `0x00417A70`, and Options tail xrefs; exact Options relocation map; fresh 88-TU build; 59/59 affected exact replay |
| MAIN-008 | exact | `Supervisor::RegisterChain` creates one calc and three draw elements at priorities 0, 0, 0x17, and 0x1E. Target behavior corrects the inherited callback labels: `OnDraw2 @ 0x004235D0` owns the priority-0 prompt/surface path, while `DrawFpsCounter @ 0x00423790` owns priority `0x17`. The two synchronization loops each cover seven critical sections, and `ReleaseSurfaces` covers 32 D3D surface slots. | Canonical `main-register-chain`, critical-section, and release-surface units |
| MAIN-009 | exact | `Supervisor::DrawFpsCounter @ 0x00423790` calls the member FPS calculator, colors values below 30 red and values below 40 pale red, then queues `%2.1ffps` at `(288, 0, 0)` through the regular ASCII formatter and restores white. The measured FPS field is at `Supervisor+0x79C`. | Canonical 170-byte VC7.1 unit with all ten timing, constant, and ASCII relocations replayed |
| MAIN-010 | exact | `Supervisor::OnDraw2 @ 0x004235D0` applies gameplay viewport 1, optionally clears with the color at `Supervisor+0x7B8`, pulses “Press Shot Button” through the regular ASCII queue using state `+0x714`, draws the regular queue, and copies or releases ANM surface 8. The target's longer constant-index surface load is naturally reproduced by a tiny `__forceinline` out-parameter helper that writes the selected `IDirect3DSurface8 *` into the real local; no assembly or padding is required. | Canonical 442-byte VC7.1 unit with all 23 viewport, ASCII, ANM-surface, copy, and release relocations replayed |
| MAIN-011 | exact | `Supervisor::CalculateFps @ 0x00424720` samples `GameWindow::GetTimestamp` every half second and derives FPS from the unsigned frame count at `+0x788`. Values above 65 trigger a staged clock repair and disable QPC after four consecutive anomalies. When six gameplay flags are clear it accumulates the replay/photo slow-rate ratio at `+0x78C/+0x794`, capping the numerator contribution at 60 above 57 FPS, then clears the one-sample reset flag. | Canonical 603-byte VC7.1 unit with all 43 timer, gameplay-state, and slow-rate relocations replayed |
| MAIN-012 | exact | The priority-`0x1E` callback at `0x00423840` is target-local `Supervisor::FinalizeFrame`, not the inherited TH08 `DrawLoadingVms` label. It flushes the ANM vertex buffer, enters critical section 5, increments `criticalSectionLockCounts[5]`, adds `frameskipConfig + 1` to the FPS frame count at `+0x788`, leaves the section, and releases that lock user. `InitializeInput @ 0x004238E0` clears and republishes availability bits 10/11 from the keyboard/controller pointers at `+0x10/+0x14` around `SetupDInput`. | Two canonical VC7.1 units totaling 274 bytes and 17 relocations, plus exact `RegisterChain` callback ownership |
| MAIN-013 | exact | `Supervisor::SetupDInput @ 0x00423960` owns TH095's complete keyboard/game-controller setup. Unless configuration option bit 3 disables DirectInput, it creates and acquires the foreground nonexclusive keyboard, enumerates the first attached game controller, installs the joystick format and background nonexclusive mode, records a 44-byte `DIDEVCAPS` at `Supervisor+0x18`, and maps every axis to `[-1000, 1000]`. The five diagnostic strings are the target's Japanese TH095 text rather than TH08's inherited English text. | Three canonical VC7.1 units totaling 866 bytes and 34 relocations; attested vtable slots, globals, strings, and field addends |
| MAIN-014 | exact | `StartInputWorker @ 0x00423CE0` registers `InitializeInput` with the generic worker at `0x004C4658`; this independent function-pointer use proves that `InitializeInput` has the static fastcall callback ABI, while its corrected 127-byte unit remains exact. `ReleaseGameManagers @ 0x00423D00` destroys and clears the active front-end and photo-game owners at `Supervisor+0x780/+0x784`. | Two canonical units totaling 114 bytes and six relocations, plus exact replay of the corrected input callback unit |
| MAIN-015 | exact | `Supervisor::AddedCallback @ 0x00423E70` is the complete startup coordinator. It initializes the two viewports and both RNG seeds, loads the archive and score state, stages surface 8 from the inherited `title/th08logo.jpg`, optionally validates presentation timing, starts the sound and DirectInput workers, loads `nowloading.anm`, creates the ANM vertex/text buffers and loading VMs, then starts the main startup worker. Exact addends establish `suppressFpsDisplay +0x428`, `loadingAnm +0x440`, `totalPlayTime +0x448`, and `startupThreadState +0x660`. | Canonical 320-byte VC7.1 unit with all 36 startup, resource, layout, and worker relocations replayed |
| MAIN-016 | exact | `Supervisor::CheckFps @ 0x00424050` is the TH095-specific startup refresh-rate sampler: it renders up to 600 frames, measures eight samples with `GameWindow::GetTimestamp`, accepts windows from 0.5 to 0.7 seconds and samples at least 57 FPS, then forces the 60-frame path when the average reaches 65. `LoadDat @ 0x00423FB0` loads `th095.dat` and requires `th095_0102a.ver`, publishing the version blob and size at Supervisor `+0x778/+0x774`. Its authored buffer remains the natural 128-byte `versionFileName`; the decisive VC7.1 source-shape is the real `fileSize` scalar's identifier bucket. Mapping that semantic local to the already-proven shallow hash bucket moves `fileSize` to `EBP-0x04` and the buffer to `EBP-0x88`, reproducing all 151 bytes without padding or dead storage. | Canonical 600-byte CheckFps unit with all 28 relocations plus canonical 151-byte LoadDat with all 15 relocations |
| MAIN-017 | exact | `StartupThread @ 0x004242B0` completes TH095's asynchronous startup by preloading `text.anm`, registering ASCII, loading `bgm/thbgm.fmt`, publishing signed configuration volumes, deriving the quartic BGM attenuation, optionally starting the dummy MIDI timer, and closing the replay-scan worker. Its exact source gives only the `new DummyMidiTimer` expression a tagged eight-byte construction phase; the ordinary allocation and post-construction homes stay shallow while the constructor result and worker receiver move to the target deep homes. `DeletedCallback @ 0x004244D0` is the exact inverse ownership spine across both workers, managers, subsystem resources, ANM/ASCII/sound/text, DirectInput, PBG, and the dummy MIDI timer. | Canonical 532-byte StartupThread unit with all 57 relocations plus canonical 550-byte DeletedCallback with all 43 relocations; Main TU replay remains exact after compiler-local label renumbering only. |
| MAIN-018 | exact / compiler-observed | `Supervisor::ThreadClose @ 0x00425150` closes and clears the replay-scan handle under critical-section lane 6, clears its active field, and balances `criticalSectionLockCounts[6]`. Together with the exact timestamp and frame-finalization paths this proves that Supervisor `+0x70C..+0x712` is a seven-byte lock-user array, not a single timestamp-user field. The adjacent 19-byte `DummyMidiTimer` destructor calls only its base destructor; stock VC7.1 naturally emits an additional derived-vptr publication, so the complete source remains non-exact without artificial bytes. | Canonical 155-byte ThreadClose unit with all three imports; hash-attested 19-byte destructor and pinned 28-byte natural compiler probe |
| MAIN-019 | exact | TH095 replaces TH08's synchronous screenshot writer with a two-part asynchronous capture lane. `TakeScreenshot @ 0x00424A00` waits for the previous writer, captures the 640x480 X8R8G8B8 backbuffer, flips rows while converting four-byte pixels to 24-bit BGR, and publishes a CRT worker; the 16-bit path only reports the unsupported format. `ScreenshotThread @ 0x00424980` writes the packed 14-byte BMP file header, 40-byte info header, and `0xE1000` pixel payload before freeing the capture buffers and clearing the thread field. Exact field addends establish the worker at Supervisor `+0x528`, file header `+0x52C`, info/pixel owners `+0x53C/+0x540`, and path `+0x544`. | Two canonical units totaling 940 bytes with all 37 D3D, file-I/O, CRT, allocation, string, and Supervisor relocations replayed |
| MAIN-020 | exact | The adjacent Supervisor music router at `0x004251F0..0x004254C2` selects MIDI mode 2 or streamed-WAV mode 1. MIDI loads, parses, plays, stops, and fades through `MidiOutput`; WAV playback uses SoundPlayer commands 1/2/3/4/5 for load, play, stop, release, and fade. The preload option selects release versus direct stop, while WAV fade duration is divided by game speed only in the `(0, 1]` interval. The render-state wrappers at `0x004254D0..0x004255A4` flush pending ANM vertices before changing fog or arbitrary D3D state and avoid redundant fog calls through the cache at Supervisor `+0x768`. | Seven canonical VC7.1 units totaling 902 authored bytes with all 56 MIDI, SoundPlayer, speed, ANM, D3D, string, and Supervisor relocations replayed |
| MAIN-021 | exact | TH095's loading animation uses three four-byte `AnmVmId` handles at `0x004C4648..0x004C4650`, not TH08's three inline VMs. `SetupLoadingVms @ 0x00425660` creates scripts 0/1/2 on render layer 7 and positions them through the shared manager. `HideLoadingVms` and `BeginLoadingCompletion @ 0x00425730/0x004257E0` send interrupt 1/2, clear all three handles through natural `AnmVmId` temporaries, and move Supervisor `+0x714` to state 0/2; both also release the target's screen-effect owner at `0x004C4654`. | Three canonical VC7.1 units totaling 522 bytes with all 43 creation, interrupt, handle, manager, position, and screen-effect relocations replayed |
| MAIN-022 | exact | Supervisor owns two `0xF0` viewport configurations at `+0x1E4/+0x2D4`, with the active pointer/index at `+0x3C4/+0x3C8`. `InitializeViewports @ 0x00425CC0` establishes the 384-by-448 inset configuration and the 640-by-480 full configuration, their camera vectors, `pi/6` field of view, depth ranges, and mode words. `ApplyGameplayViewport @ 0x00425910` derives the centered 2D camera and `pi/10` projection; `ApplyBackgroundViewport @ 0x00425AA0` builds the 3D view from position/look-at/up, refreshes the right-vector basis, and publishes configuration `+0xE8/+0xEC` to AnmManager `+0x20/+0x24`. Both flush buffered vertices before changing transforms. | Three canonical VC7.1 units totaling 1,484 authored bytes with all 69 Supervisor, ANM, D3D, D3DX, math, and layout relocations replayed |
| MAIN-023 | exact | `Supervisor::UpdateSceneState @ 0x00425EF0` is TH095's front-end/photo-game coordinator. It creates the title controller, starts or destroys `GameTaskInf`, returns from replay through front-end modes 1/2, restarts result and retry flows, and tears down both owners on failure. The prior state is retained at `Supervisor+0x410`; the active owners are at `+0x780/+0x784`. Result restart sets controller latch `0x200`, while retry sets Supervisor flag `0x1000`. The original exit-from-photo branch returns 4 without balancing critical section 5; the exact reconstruction preserves that observable target behavior. | Canonical 938-byte authored body plus all 88 bytes in three compiler switch tables; all 46 code/table relocations replayed; after the WinMain closure all 47 other Main units revalidate |
| MAIN-024 | exact | `Supervisor::OnUpdate @ 0x00423440` is the priority-zero calc callback that closes the frame-reset/input/state-routing spine. A close request exits only after replay scanning becomes inactive. Each live frame updates sound fades, polls input slot zero, clears the ANM sprite/texture/render caches and debug counters, zeroes screen shake, services pending ANM loads, and then routes startup or `UpdateSceneState`. Vertex-shader state is deliberately cleared only after pending loads succeed. | Canonical 400-byte VC7.1 unit with all 17 SoundPlayer, Controller, ANM, and scene-state relocations; exact target order and 0x30-byte frame reproduced by the natural replay/result aggregate and inline manager helpers |
| MAIN-025 | exact | The global Supervisor lifecycle at `0x00426350/0x00426450` is exact for 339 authored bytes. Construction first initializes `GameConfiguration @ +0x11C`, iterates two empty `0xF0` viewport subobjects at `+0x1E4`, constructs the timer at `+0x3F4`, and constructs two `0x18` replay-worker-shaped members at `+0x648/+0x7A0`; only then does the TH08-ancestral body clear the `0x7BC` prefix and set flags `0x40` and `0x100`. Destruction tears the workers down in reverse order. | Canonical ctor/dtor units with thirteen relocation fields. Both worker constructor calls resolve to `0x00454E50`, the same body currently named `PbgArchive::PbgArchive`; this is a linker-folded four-dword-zeroing alias and must not receive duplicate authored-byte credit. |
| MATCH-001 | exact | Forty-eight `main-*` units reproduce 16,993 authored bytes with 1,189 explicitly replayed COFF relocations. | Canonical `main-*` units under pinned VC7.1 build 3077 |

## Reconstructed ANM VM

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| ANM-001 | exact | `AnmManager::ExecuteScript` at `0x0043A600` reproduces its complete 0x427A-byte authored body. The canonical comparison extends to 0x4412 and reproduces all 333 relocation fields and all bytes in the three compiler-owned switch tables. | `anm-execute-script`; pinned VC7.1 build 3077 |
| ANM-002 | target-observed | The main opcode dispatch is dense across 89 entries; the update tail has separate six-entry easing and seven-entry interpolation-type switches. | Target table extents `0x0043E87A..0x0043EA11`; exact DIR32 replay |
| ANM-003 | exact | `AnmVm` is 0x2CC bytes for the reconstructed lane; `Initialize`, the four operand resolvers, and `AnmLoaded::SetSprite` are independently exact. | Seven canonical ANM units and compile-time layout assertions |
| ANM-004 | compiler-observed | The exact source requires a 0x400-byte frame, the VM home at `[ebp-0x17C]`, distinct interpolation and mesh-loop indices, and declaration-only mesh locals followed by target-order initialization. | Zero-difference VC7.1 instruction/relocation comparison |
| ANM-005 | exact | `Rng::GetRandomU32InRange` must inline as `range != 0 ? GetRandomU32() % range : 0`; the mesh-closing path copies two opening vertices but writes both final V-coordinate updates through the first closing pointer. | Exact target bytes and compiler oracle |
| MATCH-002 | exact | `AnmManager::ExecuteScript` contributes 17,018 authored exact bytes; 17,426 total bytes are compared so the 408 compiler-owned table bytes cannot be silently omitted or credited as authored code. | Optional `compare_size` manifest contract and canonical replay |
| ANM-006 | exact | `AnmLoaded::SetAndExecuteScript` at `0x0043A0C0` reproduces all 234 bytes and all five relocations. Its explicit timer scratch preserves the target's VC7.1 local allocation while retaining the recovered setup semantics. `AnmLoaded::InitializeVm @ 0x00404B80` is also exact for all 222 bytes: the three real zero `Float3` assignments keep their nine shallow compiler homes unchanged, while only the final `SetAndExecuteScript` call expands through an eight-byte source-local phase so the receiver moves from `EBP-0x28` to target `-0x30`. Wrapping the entire initializer in that phase is a negative oracle because it incorrectly shifts all nine vector temporaries. | Canonical `anm-set-and-execute-script` and `anm-loaded-initialize-vm` units; all seven relocations replayed |
| ANM-007 | exact | `ClearVertexBuffer` resets the buffered range and sprite count; `FlushVertexBuffer` submits two triangle-list primitives per sprite and resets the range. Both reproduce all 258 target bytes, including eight D3D-device relocations in the flush path. | Canonical `anm-clear-vertex-buffer` and `anm-flush-vertex-buffer` units |
| ANM-008 | exact | The target VM is naturally modeled as `AnmVmBase` plus derived `AnmVm`. This makes VC7.1 emit the exact 90-byte VM constructor, 43-byte destructor, array-vector helpers, and outer-manager EH cleanup without manual code bytes. | Compiler oracle plus canonical `anm-manager-constructor` and `anm-manager-destructor` units |
| ANM-009 | exact | `AnmManager` is `0x38314C` bytes: its primary VM is at `+0xF0C`, inline untextured vertices at `+0x1774`, buffered textured vertices at `+0x17C8`, VM list head/tail at `+0x381814/+0x381818`, and nine preallocated VMs at `+0x38181C`. | Compile-time layout assertions and exact lifecycle units |
| ANM-010 | exact | TH095 ANM render mode 10 allocates a `0x4B0`-byte radial-trail payload: 33 `AnmVertex` values at `+0x000`, 33 radii at `+0x39C`, 33 radial velocities at `+0x420`, and UV velocity at `+0x4A4`. Its initializer installs update and draw callbacks at `0x00441A10` and `0x00441D70`. | Canonical 754-byte `anm-initialize-pulsing-radial-trail` unit with all 29 relocations |
| ANM-011 | exact | The radial-trail update scrolls both U and V by `uvVelocity.x`, shifts all 33 coordinates by one when a coordinate becomes negative, expands 31 radial vertices, copies vertex 1 through the advanced pointer to close vertex 32, and makes the radial vertices transparent while preserving the center color. The draw callback submits all 33 vertices through the distinct `AnmManager::DrawTriangleFan @ 0x00441480` path; the adjacent `DrawVertices @ 0x00441330` is the triangle-strip submitter used by render mode 10. | Canonical 850-byte update and 45-byte draw units with all 11 relocations |
| ANM-012 | exact | `AnmVm::Draw @ 0x004452D0` is a 26-byte forwarding wrapper that submits its VM to `AnmManager::Draw`. Nine target callers use it, including the photography viewfinder renderer. | Canonical `anm-vm-draw` unit with both relocations replayed |
| MATCH-005 | exact | The 1,005-byte manager constructor and 176-byte destructor reproduce every authored byte and all 96 relocations, including ten chain registrations and the vector constructor/destructor helpers. | Canonical `anm-manager-constructor` and `anm-manager-destructor` units |

## Reconstructed photography camera

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| PHOTO-001 | target-observed | The TH095 gameplay controller at `0x004C4E70` stores player position at `+0x1E30` and an embedded `0xBDC`-byte photography state at `+0x1E3C`. The photography state owns eleven VM handles, four inline `0x2CC` ANM VMs, charge/timers, photo counters, flags, and viewfinder position/size. | Main/helper target field accesses and compile-time layout assertions |
| PHOTO-002 | exact | `PhotoGameStateView::AngleToPoint @ 0x00430370` measures from the player position, returns π/2 for a coincident point, and otherwise uses `atan2f`. `PhotoDistance2D @ 0x00434220` is the corresponding two-dimensional Euclidean distance helper. | Canonical 108-byte and 77-byte VC7.1 units with all five relocations |
| PHOTO-003 | exact | `PhotoCameraState::BeginCapture @ 0x00432730` transitions tracking to charging, resets the mode timer, creates scripts `0x18..0x1C`, removes stale charge VMs, and queues SFX `0x2C` outside replay suppression. | Canonical 393-byte unit with all 17 relocations |
| PHOTO-004 | exact / compiler-observed | `UpdatePhotoCamera @ 0x00430AB0` is canonical exact for the complete five-state photography loop. The authored body is 7,271 bytes and VC7.1 owns an adjacent 20-byte switch table, so the canonical compare extent is 7,291 bytes. The former 6,399/6,415 residual was an allocation-chronology error, not missing camera semantics. An exact `PhotoGameUpdateView::UpdateMainState` sibling proves that `PhotoAnmVmIdValue(0)` equality can place its RHS temporary in the deep compiler class while leaving a following `CreateVm` sret shallow. With that `IsZero` frontend on both camera comparisons and the old late effect-start eight-byte reservation removed, every deep/inline home becomes uniformly one qword shallow. `/FAsc` places the affected `$T696` family frontier at the first real tracking subtraction, `enemy.position - player.position`; a force-inline value producer carrying exactly eight bytes moves the whole family to target without changing shallow CreateVm homes. Four- and twelve-byte controls score 5,976/6,415, while eight bytes gives 6,415/6,415. The independent modeTimer reset retains its target-proven `0x2C` phase. | Canonical `photo-camera-update`: 7,271/7,271 authored bytes, 7,291/7,291 body-plus-table bytes, all 1,565 authored mnemonics and all 219 relocations replayed; all ten pre-existing `PhotoCamera.cpp` units cold-replay exact, with only destination-audited private-label renumbering in `UpdateViewfinder` |
| PHOTO-004A | target-observed | Preserve the target's non-obvious camera semantics: lower viewfinder clamps are strict `<` tests; angle tracking uses one-argument `NormalizeAngle`; target-positive frame colors are `(R,G,B)=(0xFF,0x20,0x20)` and do not rewrite alpha; capture recovery resets the timer through integer assignment order; and the low-charge color path first writes `(32*slowRate)+0x60` to the red byte, immediately overwrites red with `(64*slowRate)+0x40`, then writes green and blue before copying the whole dword. The redundant first red write and untouched alpha byte are target-authored behavior, not permission to introduce inert source. | Exact target instructions at `0x00431741..0x004317C8`, `0x00430E9F..0x00430F08`, `0x004319A8..0x00431B50`, `0x004320D1..0x00432157`, and `0x00432301..0x00432338` |
| PHOTO-005 | exact | `PhotoCameraState::UpdateViewfinder @ 0x004328C0` decodes the eight movement directions through a 16-bit input-mask helper, clamps the viewfinder to the playable camera bounds, derives its charge-scaled 4:3 frame, positions the four corner VMs, and scales/positions the center VM. | Canonical 1,059-byte body plus 32-byte switch table with all 48 relocations replayed |
| PHOTO-006 | exact | `PhotoCameraState::TakePhoto @ 0x00432D10` removes the active frame VMs, collects bullet/stage/runtime targets, builds an eight-word photo metadata block, saves either the numbered or temporary photo, consumes charge, advances or closes the photo session, restores game speed, and switches SFX. | Canonical 738-byte unit with all 41 relocations replayed |
| PHOTO-007 | exact | `PhotoCameraState::CancelCapture @ 0x00433000` removes the same five frame VMs, zeroes the eight-word metadata block before saving the temporary slot, restores half charge and normal game speed, clears alternate-capture state, returns to recovery, and stops the charge SFX. | Canonical 318-byte unit with all 16 relocations replayed |
| PHOTO-008 | exact / compiler-observed | `PhotoCameraState::CalculatePhotoScore @ 0x00433140` exactly reproduces the complete TH095 scoring pipeline: bullet-size and photo-scale base points, runtime/stage targets, enemy distance and boss-rate multipliers, full-player containment for the self-shot flag, two-shot/empty-photo handling, nearby-target bonus, seven physically unrolled bullet-color bonuses, colorful/rainbow bonuses, stage multiplier, and final ten-point rounding. The target source shape uses contiguous live scoring locals and direct native-bitfield assignment for camera bit 0 and score bits 0/3; replacing those assignments with mask/or expressions preserves semantics but rotates registers and grows the body by three bytes. | Canonical 2,219-byte VC7.1 unit with all 36 relocations replayed; 642/642 instructions and 2,075/2,075 non-relocation bytes exact |
| PHOTO-009 | exact | `PhotoCameraState::CountPhotoTargets @ 0x004339F0` scans eight runtime enemy slots, rejects hidden/inactive and offscreen enemies, requires the entire 8-by-8 subject box to lie inside the viewfinder, counts valid subjects, returns the nearest viewfinder distance, and reports the highest active boss-rate ratio. | Canonical 793-byte VC7.1 unit with all 23 relocations replayed |
| PHOTO-010 | exact | `PhotoCameraState::UpdateCharge @ 0x00433D10` implements TH095's focus-to-charge loop: five held frames activate focus and SFX `0x2A`; normal charge accelerates with timer subframes; focused charge accelerates through frame 70; script `0x124` pulses after frame 60 or on even auxiliary-timer advances; release or full charge clears focus and stops the SFX. | Canonical 982-byte VC7.1 unit with all 37 relocations replayed |
| PHOTO-011 | exact | `PhotoCameraState::Draw @ 0x004340F0` draws the four inline viewfinder VMs when global suppression bits 0 and 2 are clear, then sets or clears bit 1 on the first nine handle-backed camera VMs. | Canonical 296-byte VC7.1 unit with all 12 relocations replayed |
| PHOTO-012 | exact | `PhotoGameUpdateView::Update @ 0x0042FF60` is the outer four-state photo-game coordinator. It moves the player from Y=480 to Y=400 over the 60-frame entrance, runs the main gameplay and camera updates, raises completion flags after 30 frames, resets runtime/bullet/stage subsystems at frames 4 and 15 of shutdown, then executes the inline ANM VM and advances the shared timer every frame. | Canonical 405-byte authored body plus its 16-byte switch table with all 23 relocations replayed |
| PHOTO-013 | exact / compiler-observed | `PhotoStageStateView::Update @ 0x0042AD60` is canonical exact for the complete stage-side photography state machine. Truthful raw crop snapshots, the eleven-row `0x2214` display view, calibrated stock-VC7.1 live-local buckets, the by-value frame-35 Y frontend, the initial VM-zero frontend, and a fully live fade-alpha aggregate reproduce all 1,172 target mnemonics. The final allocation barrier is an operation-owned eight-byte current-entry interrupt phase: one force-inlined frontend receives the real `PhotoStageStateView *` and a reference to the live entry index, computes `GetPhotoIndex()-1`, then interrupts that indexed entry VM. Exact `UpdatePhotoResultScreen` independently proves both an eight-byte current-photo interrupt phase and a broader previous-photo getter+interrupt phase, so the reservation is tied to a repeated semantic frontend rather than padding. Global-state indexing is one byte short, moving the phase after the getter leaves only the getter temporary two bytes wrong, and VM-id/member or nested-game-getter replacements regress the allocation lane. | Canonical `photo-stage-update`: 5,309/5,309 authored bytes, 4,789/4,789 non-relocation comparable bytes, all 130 relocations, and all 413 paired EBP operands replayed under stock VC7.1 build 3077; four pre-existing PhotoStage sibling units cold-replay exact after integration |
| PHOTO-014 | exact | `PhotoStageStateView::SavePhoto @ 0x0042C450` atomically begins a capture, stores the requested world position and dimensions in the stage header, chooses a player-side photo placement, derives one of ten entry positions, and dispatches the slot's display builder. `UpdatePhotoStage @ 0x0042C3E0` suppresses the state machine while global flag bit 2 is set. | Canonical 356-byte and 45-byte VC7.1 units with all ten relocations replayed |
| PHOTO-015 | exact | `PhotoStageStateView::CapturePhotoPixels @ 0x0042E7B0` allocates the staged best-shot pixel buffer, locks the selected capture texture, and copies every row as RGB24 when the component count is three or as two-byte ARGB4444 with the alpha nibble forced opaque otherwise. | Canonical 689-byte VC7.1 unit with all 21 global/allocation relocations replayed |
| PHOTO-016 | exact / compiler-observed | `PhotoStageDisplayView::Build @ 0x0042C5C0` is canonical exact for the complete six-digit score and twenty-row photo-bonus HUD builder. All thirteen shallow semantic dwords remain live at the target homes through `EBP-0x34`. The decisive source correction is the real postfix counter expression: every emission passes `&g_PhotoStageState->displayVms[displayVmCount++]` directly to `InitializePhotoStageDisplayVm`. Stock VC7.1 then materializes all 92 per-emission pointers as compiler `tv` values after hidden `this`, exactly at `this @ -0x140` followed by pointer homes `-0x144..-0x2B0`; naming the pointer in the inline producer rotates `this` to the deep end even though all 92 pointer addresses remain correct. The target also repeats the independently exact `PhotoFrontManagerView::Initialize` shallow-to-hidden-`this` `0x108` allocation boundary. In Build that proven interval must occupy two VC7.1 allocation classes (`0x100` plus `4`) before the first real display emission: a single `0x108`, `0x104`, `0x100`, or `0xFC` class preserves extent but leaves the receiver/pointer class wrong. | Canonical `photo-stage-display-build`: 8,560/8,560 authored bytes, 7,288/7,288 non-relocation comparable bytes, all 2,234 target mnemonics, and all 318 relocations replayed; the five pre-existing `PhotoStage.cpp` units cold-replay exact from the same rebuilt object. |
| PHOTO-017 | exact | `InitializePhotoStageDisplayVm @ 0x0042E730` initializes script `0x23`, selects the requested sprite, copies the display position to `AnmVm::positionOffset`, stores the row render mode in `intVar0`, and marks the VM active through `counterVar1`. | Canonical 118-byte VC7.1 unit with both global references and both ANM calls replayed |
| PHOTO-018 | exact | `PhotoGameUpdateView::UpdateMainState @ 0x0042F190` is the complete TH095 live-play player loop. It decodes eight movement directions, disables movement during camera charging, creates or interrupts the slow-focus VM, selects normal/focused and axis/diagonal speeds, applies the extra `0.22f` slow modifier, switches the four player animation scripts, floors coordinates to hundredths, clamps movement to `[-184,184] x [32,436]`, rebuilds three collision/photo bounds, moves the focus VM to screen coordinates, and advances a sixteen-position history. Two source-local force-inline helpers keep the real focus-zero comparison and focus-clear assignment out of the outer allocation phase, so the `CreateVm` sret and six `Float3` return objects occupy the target `EBP-0x20..-0x68` lane before the compare/snapshot/clear value homes at `-0x6C/-0x70/-0x74`. | Canonical 3,463-byte authored unit with a 3,527-byte compare extent covering both adjacent eight-entry switch tables; all 63 relocations replayed. Ordinary named zero locals, identifier renaming, equality-by-reference, and a 12-byte focus aggregate are negative oracles because they keep scalar storage in the shallow outer allocation phase. |
| PHOTO-019 | exact | The `PlayerInf` lifecycle at `0x0042EA70..0x00430252` owns a `0x2A40`-byte object with one player-effect VM, sixteen position-history entries, 128 frame/timer slots, the embedded photo camera, and three Chain registrations. The 331-byte constructor is now exact: member construction remains in the original outer phase, while the real `DebugPrint -> memset -> publish g_PhotoGame` body expands through one 12-byte source-local phase, moving only the body receiver from `EBP-0x34` to target `-0x40`. `PhotoGameUpdateView::Initialize @ 0x0042ECA0` is exact for all 392 bytes: a source-local `ZunTimer` assignment phase carries the target-attested `0x2C` reservation immediately before the hidden timer `this` home, reproducing `timer-this @ EBP-0x30` and outer `this @ -0x34` while leaving all sixteen relocations and the three half-size calculations unchanged. Its create spine registers calc/player-draw/camera-draw callbacks at priorities `0x0B/0x0B/0x12` and performs full failure cleanup; the exact SHT loader rebases power-level descriptor pointers and derives diagonal speeds as `cos(pi/4)` times the two axis speeds. | Thirteen canonical lifecycle units totaling 1,897 authored bytes with all 76 relocation fields replayed; exact 331-byte constructor and 392-byte initializer plus target `initialize/shutdown PlayerInf` strings and sole gameplay lifecycle callers |
| PHOTO-020 | exact / compiler-observed | `PhotoGameUpdateView::OnUpdate @ 0x00430180` and `PhotoCameraState::Initialize @ 0x004307D0` are canonical exact. Camera initialization clears the complete `0xBDC` state, creates scripts `0x16/0x17`, initializes four script-`0x24` viewfinder VMs with sprites `0x10/0x0F/0x0F/0x19`, installs their `(-36,-27,-18,-9),-12` positions, and starts with half charge plus a capture request. The target `0xEC` frame is four repeated operation-owned `InitializeVm` phases: one force-inlined frontend takes only the real `AnmVm *`, owns the independently proven `0x2C` VC7.1 phase, and reads the ANM owner plus fixed script `0x24` inside the frontend. Four call sites therefore contribute exactly `4*0x2C` beyond the natural `0x3C` frame. Exact PhotoItem and PhotoBullet spawn paths independently prove the same `InitializeVm` phase. ABI controls are decisive: two/three-argument free frontends contribute `0x30` per call, while no-argument member frontends let VC7 discard the phase entirely. | Canonical `photo-camera-initialize`: 733/733 authored bytes, 649/649 non-relocation comparable bytes, target frame `0xEC`, and all 21 CreateVm/InitializeVm/SetSprite/global relocations replayed; all 22 `PhotoGame.cpp` exact units cold-replay after refreshing only compiler-private switch-label names in two existing manifests |
| PHOTO-021 | exact | The adjacent PlayerInf collision/death cluster is exact. `AngleFromPoint @ 0x004303E0` supplies the reverse point-to-player angle used by ECL and projectile logic. `CheckBulletCollision @ 0x00430450` tests an incoming axis-aligned rectangle against the live hurtbox; `CalcLaserHitbox @ 0x00430530` rotates the player into a laser-local rectangle. Both reject captured/recovering states and converge on `Die @ 0x004306D0`, which enters state 2, places one script-`0x121` VM and thirty-two script-`0x122` particles at the player, resets the completion timer, conditionally plays sound 4, and sets game speed to `0.5`. | Four canonical VC7.1 units totaling 962 authored bytes with all 27 relocation fields replayed; five target callers of the reverse-angle helper and four collision callers independently confirm the interfaces |
| PHOTO-022 | exact / compiler-observed | `PhotoBulletManagerView::Update @ 0x00405120` is the complete TH095 projectile loop. It scans 1,600 live `0x65C`-byte bullets, honors both global update-freeze bits, advances spawn/active/despawn states, executes ten transform families, applies game-speed-scaled movement and player collision, culls sprite-sized projectiles outside `[-192,192] x [0,448]`, executes each bullet VM, rebuilds six ordered draw buckets, and ticks every retained bullet. The target's formerly unexplained 16-byte interval is two repeated eight-byte allocation phases owned by the two real post-playfield `PhotoBulletView::Deactivate()` calls: exact `ClearCapturedBullets` independently proves the same Deactivate frontend can own an eight-byte phase. A single 16-byte phase on the playfield predicate reaches 1,704/1,715 but places the interval before the three width/height/position argument temps; two 8-byte Deactivate phases place those temps at `-0xB0/-0xB4/-0xB8`, the 16-byte interval at `-0xBC..-0xC8`, and outer `this @ -0xCC` exactly. | Canonical `photo-bullet-manager-update`: 1,835/1,835 authored bytes and 30/30 relocations; all 33 pre-existing BulletManager units cold-replayed, with only three compiler-private `$L` names refreshed after structural zero-diff audits |
| PHOTO-023 | exact / compiler-observed | `PhotoEnemyManagerView::OnUpdate @ 0x00415970` is the complete 128-slot TH095 enemy loop: timelines/ECL, lifecycle, movement, attached/photo-marker VMs, collision/culling, four draw groups and timer advancement. The attached-VM smoothing must be `(screen - attached) * 0.07f + attached` to preserve target x87 chronology. The final `0x20` compiler allocation interval belongs to the real movement frontier as a whole, not one call: Integrate-only, Clamp-only, pre-Clamp and pair placements all compile to the same zero-difference body. The size is strict (`0x20` = 1697/1697 comparable; `0x1C`/`0x24` = 1637/1697). TH08's exact ancestral source and patched-MSVC7.0 listing independently preserve Clamp/Integrate/Clamp as one contiguous movement block, supporting that frontier ownership without claiming a same-size TH08 reservation. | Canonical `enemy-manager-on-update`: 1,853/1,853 authored bytes, all 448 target mnemonics, all 39 relocations and zero non-relocation differences. All twenty existing `EnemyManagerUpdate.obj` units cold-replay exact; two timeline switch-table `$L` symbols were refreshed only after offset/type/destination audit proved no relocation target changed. |
| PHOTO-023A | exact | The `0x26AE30` EnemyManager task shell is exact at `0x004149F0/0x004161F0/0x00416230/0x00416290/0x004162F0`. `Create` allocates the complete manager, loads resources, registers calc priority 12 and draw priority 10, and performs full destructor/delete cleanup on failure. `Draw` traverses four heads at manager `+0x4DC0`; `DrawGroup` projects enemy `position +0x28A0` into `AnmVm::positionOffset` and submits `vm +0x08`. The update gate shares one return-1 tail for photo-global bits 0/1/2; the draw gate suppresses only bit 2. | Five canonical VC7.1 units totaling 591 authored bytes with all 29 allocation, Chain, projection, ANM-draw, global-gate, and update relocations replayed |
| PHOTO-024 | exact | `PhotoEnemyView::IntegrateMovement @ 0x004160B0` stores the D3DX position delta and prior position before applying game-speed-scaled XYZ velocity, with flags1 bit 16 reversing X. `ClampPosition @ 0x00416320` uses flags1 bit 17 to constrain XY against the four bounds at enemy `+0x2C3C..+0x2C48`. | Canonical 308-byte and 208-byte VC7.1 units; all four game-speed relocations replayed |
| PHOTO-025 | exact | `PhotoEnemyView::UpdatePhotoMarkerPulse @ 0x00416770` decrements the timer at `+0x2BFC` and clears flags2 bit 6 on expiry. `Deactivate @ 0x00416E80` retires the photo-pulse and attached VM handles, frees sixteen ECL argument allocations at `+0x2CAC`, then zeroes the complete `0x4CC0` enemy object. | Canonical 101-byte and 175-byte VC7.1 units with all six relocations replayed |
| PHOTO-026 | exact | `PhotoToScreen @ 0x004186D0` is the shared TH095 world-to-screen bridge: it adds 320 to X and 16 to Y while preserving Z. | Canonical 71-byte VC7.1 unit with the `128.0f`, `192.0f`, and `16.0f` constant relocations replayed; fifteen target callers |
| PHOTO-027 | exact | The enemy ECL reset family at `0x004167E0`, `0x00416810`, `0x00416DD0`, and `0x00416E30` proves that manager `+0x26AE00` is an eight-entry photo-target table. The combined reset deactivates all 128 enemies whose photo-target bit is clear, then reinitializes each tracked target with its alternate subroutine at enemy `+0x289A`; the dedicated restart path uses the main subroutine at `+0x2898`. | Four canonical VC7.1 units totaling 401 bytes with all six relocations replayed |
| PHOTO-028 | exact | `PhotoEnemyView::UpdateScheduledEclCalls @ 0x00416F30` scans ten frame/subroutine pairs at `+0x2C54/+0x2C7C`, dispatches due calls into the main ECL context at `+0x2DC`, frees sixteen argument allocations, restores `0x210` bytes of manager template state at `+0x298C`, and clears the `+0x2BC8` latch. | Canonical 309-byte VC7.1 unit with all five relocations replayed. The only prior residual was the exchange of the real `argumentIndex` and `currentFrame` homes; the established shallow identifier-hash buckets restore their target order without padding or source-semantic changes. |
| PHOTO-029 | exact | `PhotoEnemyTimelineView::Run @ 0x004163F0` is canonical exact for all 818 authored bytes; its configured compare extent is 882 bytes because the compiler emits an adjacent sixteen-entry 64-byte switch table. The interpreter dispatches fixed-position, random-range, random-width, and extended spawns with ordinary/X-mirrored variants, opcode 8 writes the selected timeline enemy state, and opcode 10 stalls while that enemy remains active. TH08 supplies the ancestral raw `i32 *` argument shape. The target-observed range case requires the real inline `Rng::GetRandomF32InRange`, while case 11/12 uses one fully-live 20-byte `{Float3 position; PhotoEnemyView *enemy; i32 *args;}` record so VC7.1 lays out `position/spawned/args` exactly. | Canonical `enemy-timeline-run` unit: 818/818 authored bytes, 882/882 body-plus-table bytes, all 254 authored instruction mnemonics, and all 37 code/table relocations replay against the attested target. The former “882 versus 818” residual was a boundary-accounting error: 882 is the full symbol extent, not the authored body size. |
| PHOTO-030 | exact | `PhotoEnemyManagerView::Spawn @ 0x004156C0` scans 128 enemy slots, copies the manager's leading `0x4CC0`-byte spawn template into the first inactive slot, records the slot index at `+0x2968`, sets the X-movement mirror bit, applies position and optional life, and bootstraps the main ECL context. Failed first-run ECL deactivates the slot; success snapshots VM color1 at `+0x2988`, sign-extends the item type into `+0x2BD8`, applies optional score, and copies life into maximum/phase-starting life. | Canonical 350-byte VC7.1 unit with all three ECL/deactivation relocations replayed; exact TH08 ancestral source shape revalidated against the TH095 target |
| PHOTO-031 | exact | `PhotoEnemyManagerView::SpawnWithContext @ 0x00415820` shares the exact 128-slot/template/ECL spawn spine but accepts a caller-supplied context block instead of the X-mirror variant. After initializing the main ECL context at enemy `+0x2DC`, it copies exactly `0x80` bytes into `+0x2F4` before the first `RunEcl`, then follows the same failure cleanup and success initialization path. | Canonical 336-byte VC7.1 unit with all three ECL/deactivation relocations replayed; two target calls from `EclManager::RunEcl` |
| PHOTO-032 | exact | `PhotoEnemyView::~PhotoEnemyView @ 0x004152D0` and `PhotoEnemyManagerView::~PhotoEnemyManagerView @ 0x004154E0` are both canonical. The manager teardown logs shutdown, cuts both Chain nodes, frees sixteen ECL allocations in each of 128 enemies, deletes the ECL manager/file, releases ANM 8 or retires VMs according to Supervisor flag 9, clears the global owner, destroys the 128-enemy vector, and then destroys the direct spawn-template member. The child destructor must be defined inline in the class: VC7.1 still emits its 52-byte callable COMDAT for vector teardown but expands the direct spawn-template cleanup into the manager destructor. A source-local ownership helper that accepts the real enemy and argument index delays only the real ECL-argument pointer local to its call-site phase, restoring the target delete-expression homes without dummy storage. | Canonical 52-byte child and 480-byte manager destructor units; all 23 `_free`, Chain, ECL, ANM, vector-destruction, and EH relocations replayed |
| PHOTO-033 | exact | The TH095 enemy ECL manager is `0x188` bytes: the TH08-derived file/subroutine pointers and `0x160`-byte timeline state occupy `+0x000..+0x167`, while four integer and four float call parameters are TH095-specific members at `+0x168..+0x187`. `Load @ 0x00408CE0` validates version `0x800` and rebases both file-relative tables; `InitializeContext @ 0x00408DE0` selects a subroutine and resets both timers. `PhotoEnemyManagerView::LoadResources @ 0x004153D0` loads the selected scene's enemy ANM/ECL pair and naturally constructs this manager layout. | Three canonical VC7.1 units totaling 647 bytes with all 19 relocations replayed; target accesses to manager `+0x168..+0x184` independently prove the call-parameter tail |
| PHOTO-034 | exact / compiler-observed | `PhotoEnemyEclContextView::PhotoEnemyEclContextView @ 0x00415310` exactly constructs the two context timers and eight interpolation-slot timers in a `0x230`-byte context. `PhotoEnemyView::PhotoEnemyView @ 0x00415040` exactly constructs the embedded ANM VM, seventeen ECL contexts, nine independent timers, the `0x210`-byte bullet descriptor, 96 trail samples, and 194 textured trail vertices in the target-proven `0x4CC0`-byte enemy. Its three embedded VM ids at `+0x2C1C/+0x2C20/+0x4CBC` are four-byte POD storage, not non-trivial `AnmVmId` members; update code applies source-local typed views only at use sites. The enclosing `PhotoEnemyManagerView::PhotoEnemyManagerView @ 0x00414B90` naturally constructs the spawn template, sixteen timelines, and 128-enemy pool, then establishes the TH095 trail/photo-target/ECL defaults. Its former stack-home residual is superseded by the canonical exact constructor. | Canonical 1,196-byte `enemy-manager-constructor` with all eleven relocations; canonical context and enemy constructors totaling 822 authored bytes with seven relocations |
| PHOTO-035 | exact | `PhotoEnemyManagerView::Destroy @ 0x00414B30` is the 95-byte self-destruction entry used by the game-task shutdown coordinator. It performs the full manager destructor and then releases the manager allocation. | Canonical VC7.1 unit with exact destructor and `operator delete` relocations; sole target caller at `0x00417E70` |
| PHOTO-036 | exact | `PhotoStraightLaserView::Update @ 0x0041E2C0` implements TH095's straight photography laser. It grows toward the requested maximum length, advances its tail and world position, shortens at a terminal distance, culls both endpoints against the expanded playfield, checks the player against the laser interior, scales the body VM from the loaded sprite, and advances body/tail ANM scripts. | Canonical 1,029-byte VC7.1 unit with all 27 relocations replayed; exact `Float3::FromAngleMagnitude`, `PhotoGameUpdateView::CalcLaserHitbox`, and `AnmManager::ExecuteScript` dependencies |
| PHOTO-037 | exact | `PhotoRotatingLaserView::Update @ 0x0041F550` is the TH095-specific rotating/lifecycle laser. It grows and rotates with game speed, optionally anchors to photo target zero, applies packet velocity, transitions through startup/growth/sustain/fade states, interpolates width with the shared timer, enables collision only while growing or sustained, and drives its body/tail VMs. Manager `+0x26AE00` is independently reused as the eight-entry photo-target table. | Canonical comparison reproduces all 1,062 authored bytes plus the adjacent 16-byte switch table; all 23 code/table relocations replayed |
| PHOTO-038 | exact | The straight and rotating laser initializers at `0x0041E0C0/0x0041F380` copy their complete `0x28/0x48`-byte spawn packets, choose color/type ANM scripts, configure additive body/tail VMs, and seed their distinct movement/lifecycle state. Both targets independently allocate the same two additive-blend frontend phases: each phase owns a `0x2C` compiler-storage reservation followed by the hidden `AnmVm *this` home, yielding active homes at `EBP-0x30/-0x60` and outer `this @ -0x64`. One source-local `__forceinline` additive phase reproduces both 500/450-byte bodies without changing the emitted instruction or relocation topology. Their paired draw callbacks at `0x0041E6D0/0x0041F990` convert world position to screen space, rotate the body VM by `pi/2`, draw it, and draw the tail only while its offset is zero. | Four canonical initializer/draw units totaling 1,203 bytes; all 28 relocations replayed. The initializer pair is a repeated-target compiler-phase oracle, not a general license for function-scope padding. |
| PHOTO-039 | exact | `PhotoEffectManagerView::Update @ 0x0041D930` traverses the intrusive effect list while preserving the next pointer, implements the two-pass deferred-deletion counter at effect `+0x48`, removes state-1 or completed effects through virtual cleanup, unlink, and delete, and otherwise advances the effect timer. `Remove @ 0x0041DA50` decrements manager count `+0x54`, repairs both links, and updates tail `+0x50`. | Two canonical VC7.1 units totaling 373 bytes; all seven update relocations and the relocation-free unlink body replayed |
| PHOTO-040 | exact | The PhotoEffect manager's primary/secondary draw, photo-target count, stored collision, and nearby-target count fanouts at `0x0041DAB0/0x0041DF10/0x0041DFA0/0x0041E010/0x0041E060` all preserve the next link across virtual dispatch and skip state-1 effects. `CountPhotoTargets` caches its bounds at manager `+0x5C/+0x68`; the next collision fanout reuses them. `OnUpdate @ 0x0041DB00` suppresses updates for global flags 0/2/10 and temporarily sets game speed to zero for flag 1; `OnDraw @ 0x0041DBA0` suppresses primary drawing for flag 2. | Seven canonical VC7.1 units totaling 668 bytes; all eleven callback relocations and five relocation-free traversal bodies replayed |
| PHOTO-041 | exact | `PhotoEffectManagerView::Spawn @ 0x0041DBD0` allocates nonzero IDs, limits the list to 256 effects, creates the `0x610` straight or `0x630` rotating laser, appends it to the intrusive tail, increments count, and invokes virtual initialization. The two target cases independently split the inline `Append` ownership chronology: the straight previous-tail local owns an eight-byte compiler-storage phase, while the rotating previous-tail local owns a twelve-byte phase. Keeping `new` and virtual `Initialize` unchanged reproduces all 432 bytes. Both concrete constructors remain canonical exact. | Canonical 432-byte factory with all eight EH/allocation/constructor relocations plus two exact concrete constructors totaling 389 bytes; full PhotoEffect TU replay green. |
| PHOTO-042 | exact | The laser target helpers sample along the beam at 12-world-unit spacing. `CountPhotoTargets @ 0x0041E750` is exact for the half-size capture AABB scan and is identical-linker-folded for the rotating override; `CountNearbyTargets @ 0x0041F280` tests an angle-local rectangle and returns weight two; `DrawSecondary @ 0x0041F140/0x00420100` places script-`0x126` VMs at the same spacing, propagates packet color, and retires the effect. The target-count source reuses the exact straight-collision local rank: `step / sample-count / hit-count / minimum / sample` occupy the shallow lane, the inner half-size vector is reused as maximum after minimum materialization, and `step.z` is explicitly initialized before `FromAngleMagnitude`. | Four canonical units total 1,484 authored bytes with all twenty-five relocations replayed. This is a positive oracle for reusing an already-exact sibling's live-local ownership instead of introducing padding or a monolithic vector aggregate. |
| PHOTO-043 | exact | The concrete collision handlers at `0x0041E9C0/0x0041FA10` implement TH095's photograph-cut laser behavior and are both canonical exact. Both sample the beam every 12 world units, mark samples inside the capture rectangle, create color-propagating script-`0x126` VMs, and optionally emit capture particles. The shared source shape uses the established shallow scan-state rank, a fully-live three-int `GapScanState`, one reused half-size/maximum vector, scalar-first vector scaling that reproduces the target x87 conversion reuse, and `initialLength -> maximumLength` packet publication. The rotating target additionally proves the scanner CFG: after a nonzero leading captured run it clears `length/gapLength` and jumps into the common subsequent-gap scanner; after a zero-leading run, only a still-live sample range computes the initial shortened length before entering that same scanner. One `scan_more:` label and one common `finish:` return make stock VC7.1 emit target `jge near finish` instead of the former `jl short + jmp near` pair. The old one-byte branch-hard diagnosis was therefore a misplaced shared-tail source bug, not an irreducible compiler quirk. | Canonical straight/rotating collision units total 3,677 authored bytes with all 36 relocations replayed; rotating alone replays 1,767/1,767 bytes and seventeen relocations. All 34 `PhotoEffect.cpp` units remain exact after the closure and destination-audited compiler-local label refresh. |
| PHOTO-044 | exact | The TH095 `ItemInf` manager contains 150 inline `0x2F8` photo-charge items. `Update @ 0x0041CE60` is canonical exact for all 1,235 bytes: a fully-live 44-byte `{boundsMin, boundsMax, index, item, direction}` aggregate explains the target frame, including the unused z fields of two real Float3 bounds. `(f32)timer`, scalar-first vector scaling, `NormalizeAndScaleItemVelocity(direction, velocity, acceleration)`, source-local charge helpers, and one shared `tick:` tail restore target VC7.1 lifetime/evaluation order without padding. `Spawn @ 0x0041D460` is also exact: a positive `index < 150` body selects target near-`jge`, the real item pointer uses the calibrated `averagedPanLocal12` bucket, and a source-local `0x2C` phase owns only the late VM initialization/color setup. The target repeats the laser/AdvanceTransformProgram shallow-to-hidden-`this` phase boundary; timer placement is a negative control. | Twelve canonical lifecycle/update/draw/callback units totaling 2,585 authored bytes; Spawn adds eight exact relocations without inert function-scope storage |
| PHOTO-045 | exact | `LaserInf` is a `0x80`-byte manager containing a full `0x50`-byte `PhotoEffectBaseView` as its intrusive-list sentinel, followed by tail/count/id/collision-cache/ANM/Chain fields. The base vtable at `0x004958B0` proves default initialize, cleanup, target-count, collision, and nearby-target handlers; identical zero-return methods are linker-folded. Manager initialization loads `bullet.anm` in slot 6 and points the tail at the inline sentinel. The factory registers update/draw callbacks at priority 13, while teardown cuts both chains and cleans, unlinks, and frees every remaining laser. | Ten canonical VC7.1 units totaling 893 bytes with all 40 relocations replayed; target vtable slots independently prove the composition layout and identical-linker-folded aliases |
| PHOTO-046 | exact | `PhotoGameTaskView::InitializeSubsystems @ 0x00417A70` is the live photography-game lifecycle hub. It copies the `0xC8` runtime snapshot, creates replay state, validates the selected scene's normal/extra best-shot record, clears stale score fields, and creates background, front, bullet, photo-overlay, player, enemy, item, pause, and laser managers in target order before starting scene music. `PhotoGameTaskView::~PhotoGameTaskView @ 0x00417E70` destroys the same ownership graph, releases optional ECL state, cuts both Chain nodes, conditionally stops audio, and selects the final fade color. The containing task is `0x124` bytes with runtime configuration at `+0x34`, best-shot index at `+0x100`, completion-state timer at `+0x108`, Chain nodes at `+0x118/+0x11C`, and replay mode at `+0x120`. | Two canonical VC7.1 units totaling 953 bytes; all 63 global/call relocations replay to the hash-attested target, including every subsystem factory/destructor edge |
| PHOTO-047 | exact | `PhotoGameTaskView::Update @ 0x00418100` owns the live scene exit/replay gates, Help closure, captured-photo VM execution, completion countdown, second-boundary SFX, photo-target ECL restart, and stage timer. The photographed-image count at runtime `+0x29E4` and capacity at `+0x29EC` form a 12-byte counter whose integer conversion returns the capacity. `DrawHud @ 0x00418420` suppresses itself during loading/capture, derives text alpha from runtime `+0x1E34`, draws high/current scores and photo usage, and selects normal `%2d-%d` or extra `EX-%d` scene labels. | Two canonical VC7.1 units totaling 1,407 bytes; all 64 global/call/constant relocations replayed, including both `AnmManager::ExecuteScript` and photo-target ECL restart edges |
| PHOTO-048 | exact | The remaining `GameTaskInf` lifecycle is closed. The constructor at `0x004179D0` constructs the stage timer, runtime configuration, and nested completion timer before clearing the complete object. `Load @ 0x00417D20` waits for both capture slots and the global load gate, creates the subsystem graph, resolves music-controller flags, and publishes success or failure. `Create @ 0x00417F80` allocates the task, registers calc/draw Chain nodes, and launches the asynchronous replay scan; `Destroy @ 0x004180A0` proves `0x00417E70` is the C++ destructor rather than a plain shutdown method. The `0x00418690/0x004186B0` callbacks forward to update and HUD draw. | Six canonical VC7.1 units totaling 900 bytes with all 51 relocations replayed; together with PHOTO-046/047 the ten-unit task cluster is 3,260 exact bytes with 178 relocations |
| PHOTO-049 | exact | `FrontInf` is a `0x10D4`-byte manager containing six inline `AnmVm` instances followed by the loaded `front.anm` and calc/draw Chain nodes. `Update @ 0x004175B0` renders the completion timer as three seconds digits plus two hundredths digits, executes all six VMs, fades their alpha over runtime values 64..128 when no alpha interpolation owns VM zero, and sends interrupt 7 or 8 when the countdown crosses 10 or 5 seconds. `Initialize @ 0x004170F0` is now exact for all 521 bytes and 25 relocations. Its target has shallow CreateVm/VM-id homes through `EBP-0x28`, a completely unreferenced `0x108` interval at `-0x2C..-0x130`, and hidden `this @ -0x134`. The independent `PhotoStageDisplayView::Build` target repeats the same shallow-to-hidden-`this` boundary with shallow homes through `-0x34`, an unreferenced `-0x38..-0x13C` `0x108`, and `this @ -0x140`. The accepted source-local `__forceinline` phase owns the real first direct `InitializeVm(&vms[0], 4)` after all CreateVm return temporaries; moving the same phase to the first CreateVm is a 524-byte negative control. | Eleven canonical VC7.1 units totaling 2,329 bytes; the initializer replays all 421 comparable bytes plus all 25 relocation fields exactly, and all ten pre-existing FrontInf units remain exact after integration |
| PHOTO-050 | exact | The TH095 projectile spawn/transform core uses a `0x210` spawn descriptor, eighteen `0x18` transform records, and a `0x65C` live bullet with seven `0x2C` transform states at `+0x520`. `SpawnSingleBullet @ 0x00405A30` is now canonical exact for all 2,133 authored bytes plus its adjacent 36-byte nine-entry jump table. Preserve the fully live 20-byte `{transformFlags, angle, bullet, i, speed}` aggregate and the two target-specific ternary join expressions. The formerly unexplained `EBP-0x54..-0x7C` `0x2C` reservation has direct repeated-phase provenance: exact `PhotoItemManagerView::Spawn` independently assigns the same stock-VC7 phase to the real late `InitializeVm` frontend using the same bullet ANM implementation. A source-local `PhotoBulletSpawnVmSetupPhase` containing only that real `InitializeVm` reproduces target frame `0xE4`, all 529 authored mnemonics, and all 278 observed EBP operands. Controls that additionally include draw-bucket and transform-sound publication are byte-identical, proving the owner has already converged to `InitializeVm` itself rather than arbitrary neighboring statements. `AdvanceTransformProgram @ 0x004062B0` remains independently exact for 2,479 authored bytes plus its 84-byte switch table. | Canonical Spawn unit: 2,133/2,133 authored and 2,169/2,169 compared bytes with all 41 relocations; canonical Advance unit: 2,479/2,479 authored and 2,563/2,563 compared bytes with all 23 relocations; all 33 `BulletManager.cpp` units cold-replayed exact |
| PHOTO-051 | exact / compiler-observed | The complete per-frame bullet transform movement family at `0x00406D80..0x00407795` is source-present. Deceleration uses a sixteen-frame five-unit curve without multiplying its final velocity by game speed. TH095 vector acceleration uniquely advances both scalar speed and the acceleration vector by game speed and refreshes angle through `atan2`; polar and all three direction modes frame-scale their deltas but pass raw speed to velocity reconstruction. Boundary and wrapping coordinates use centered X bounds `-192..192`, Y bounds `0..448`, and shifted wrap flags `0x100000/0x200000`. `UpdateBoundaryBounce @ 0x00407440` is canonical exact for 469 bytes: TH08 exact ancestry performs `bounceSpeed -> this->speed -> magnitude`, proving two semantic publication roles. TH095 collapses the physical owner to one four-byte local, represented by overlapping `bounceSpeedBits/magnitudeBits/magnitude` union views; stock VC7.1 consequently emits the target member-to-local load, seven-byte local-to-the-same-home publication, and final argument reload without spelling an inert self-assignment. | Nine canonical VC7.1 movement units total 2,514 authored bytes; BoundaryBounce itself replays 401/401 comparable bytes plus all 17 relocations. The overlap representation is compiler-observed lineage recovery, not proof that the original source literally used a union and not a license for arbitrary self-copies. |
| PHOTO-052 | exact | TH095's bullet photography lane stores the last camera rectangle at manager `+0x34/+0x40`. `CapturePhotoTargets @ 0x00407820` excludes states zero and three plus the capture-disable flag, then builds the temporary chain through bullet `+0x35C`, distinct from draw link `+0x358`. `CountNearbyTargets @ 0x00408220` samples the minimum/maximum corners plus two Y-shifted inner samples and awards `1/1/4/10` for loaded-sprite widths up to `8/16/32/64`. `ClearCapturedBullets @ 0x00407C90` is also canonical exact: it moves both per-bullet bounds vectors to the sibling-proven function scope, reuses the same calibrated six-local rank, places its additional live VM pointer in the `bgmFormatIndexLocal05` bucket, and gives only the real `Deactivate()` operation an eight-byte source-local phase. The phase is independently supported by exact `AnmLoaded::InitializeVm`, whose target repeats the same shallow-homes / two-unused-dwords / hidden-`this` boundary; bounds and CreateVm placements are strong negative controls. | Canonical 1,122-byte capture, 1,300-byte clear, and 1,006-byte nearby units replay all 36 relocations. All four bounds vectors are real and function-scoped; no inert local or function-scope padding is used. |
| PHOTO-053 | exact | `CardInf @ 0x00408610..0x00408CDC` is a standalone TH095 photograph-card text/fade component, not a continuation of BulletManager. Its `0x68`-byte object owns two compiler-visible four-byte POD VM handles at `+0x04/+0x08`, state and timer at `+0x0C/+0x10`, saved screen-fade color at `+0x1C`, a 48-byte XOR-`0xAA` decoded label at `+0x20`, and calc/draw Chain nodes at `+0x60/+0x64`. `Initialize @ 0x00408670` is exact: two dedicated source-local inline creators keep each zero `Float3` and four-byte CreateVm sret in its own 16-byte call-site allocation block, and POD whole-object assignment reproduces the target result-before-LHS chronology. Using the nontrivial shared `AnmVmId` as a member would incorrectly add constructor work to the already-exact CardInf constructor. | Ten canonical VC7.1 units totaling 1,671 bytes with all 90 VM creation, lookup, text, Chain, fade, and lifecycle relocations replayed |
| PHOTO-054 | exact / compiler-observed | `BulletInf` is a `0x27C5B8`-byte TH095 manager containing `0x641` inline `0x65C` bullets: `0x640` usable slots plus a state-five sentinel. It owns six intrusive draw-bucket head/tail pairs, calc/draw Chain nodes at `+0x27C5A8/+0x27C5AC`, slot-six `bullet.anm` at `+0x27C5B0`, and the active count at `+0x27C5B4`. The factory registers both callbacks at priority 14; drawing projects each bullet into its embedded VM, optionally aligns rotation to travel direction, and renders all six buckets. `OnUpdate @ 0x004059C0` is exact through the same source-local force-inline OR helper used by the ScreenEffect and PlayerInf gates. | Fourteen canonical lifecycle/rendering units total 1,476 bytes with the former callback residual closed; shared `AnmLoaded::InitializeVm @ 0x00404B80` is canonical exact for all 222 bytes as recorded in ANM-006. |
| PHOTO-055 | exact | The `BackgroundInf` singleton pointer at `0x004BDD90` owns TH095's photograph masking as well as spell backgrounds. Its state at `+0x175C`, photo color at `+0x1760`, active camera rectangle at `+0x1764..+0x177F`, three `AnmVm` borders at `+0x1780`, and dynamic spell VM ids at `+0x1FE4/+0x1FE8` are target-proven. `SetPhotoArea @ 0x00404950` publishes the camera bounds and lazily initializes scripts `0x25..0x27`; because it has no competing authored locals, one source-local inline body phase owns the target `0x84` compiler-storage lane and moves only the hidden receiver from shallow `-0x04` to `EBP-0x88`. | Canonical 209-byte SetPhotoArea with all six relocations, plus canonical 143/66-byte spell start/stop and exact TH08-ancestral Hermite basis. |
| PHOTO-056 | exact | `Background::RunStageScript @ 0x00403440` is the TH095-specific variable-record stage interpreter. Its fifteen opcodes own camera position/look-at and Hermite interpolation, three shared camera values, photo color plus two float offsets, four periodic camera-motion modes, screen-fade publication, and eight stage VMs. Four timer lanes occupy `+0x20..+0x7F`, their packed modes are at `+0x80`, and the current/initial/final photo tuples occupy `+0x1FEC..+0x200F`. The exact source keeps repeated `stageInstruction` member expressions, moves the real `stageTime` snapshot into a pointer-based value producer, gives case 14 a pointer-only `0x2C` disable-VM phase, and maps semantic interpolation-slot/color-channel indices through the two target-significant backing identifiers. Background-local timer initialization writes `current -> subFrame -> previous`, matching all ten inline reset expansions without changing the shared ANM timer source. | Canonical 5,129-byte authored unit with 5,229-byte compare extent, all 1,100 mnemonics, all 517 EBP-home references, and all 117 body/data relocations exact. |
| PHOTO-057 | exact | The BackgroundInf lifecycle has exact construction, destruction, stage-object VM execution, and stage-data loading. `LoadStageDataInner @ 0x00402C80` relocates cached stage pointers, loads slot-four ANM data, resolves object/script offsets, allocates the raw stage-VM pool, and initializes one VM per quad. Exact stock VC7.1 shape requires three independent pieces: integer-offset-first pointer expressions for serialized offsets, the target-proven backing bucket for the real `stageVmAllocationSize`, and a source-local force-inline allocation frontend attached only to the real stage-VM `malloc`. That frontend owns the target-observed unreferenced `0x2C` phase between shallow loader locals and hidden `this`; 0x28/0x30 widths and InitializeVm/member/free-owner placements are negative controls, so this is not a generic padding rule. | Canonical 523-byte loader with all 16 cache/file/ANM/allocator/error/InitializeVm relocations plus exact constructor, destructor, and 208-byte UpdateStageObjectVms; current Background object replay remains exact. |
| PHOTO-058 | exact / compiler-observed | The BackgroundInf draw spine is now exact end to end. `DrawHighPrio @ 0x00402750` programs depth/fog/viewport/mix state, `DrawLowPrio @ 0x00402990` owns the clamped photograph depth mask, `BackgroundStageObject::IsVisible @ 0x004031A0` performs camera-relative culling, and `RenderObjects @ 0x00402F60` traverses stage instances and draws opcode-0 ANM quads. RenderObjects restores the exact TH08 ancestral source class: a function-scope VM owner, direct `switch` case body, the ancestral stage-quad workspace, and switch-control `tv` after hidden `this`. TH08's exact object and an isolated patched-MSVC7.0 `/FAsc` rebuild independently prove that `this -> tv` ordering. TH095 additionally requires an eight-byte compiler reservation in the real opcode frontend; 4/12-byte same-site controls and eight-byte camera/outer placements fail, while the accepted phase gives `this @ -0xE8`, `tv @ -0xEC` and all 64 paired EBP operands. | Canonical 565-byte `background-render-objects` unit with 517/517 comparable bytes, all 160 mnemonics and all 12 relocations; together with DrawHighPrio, DrawLowPrio and IsVisible the exact draw/cull spine totals 2,279 authored bytes. Adding the helper renumbers only 28 private `$L` symbols in exact `RunStageScript`; all 117 relocation offsets/types/destinations remain unchanged before the manifest-only label refresh. |
| PHOTO-059 | exact | BackgroundInf construction/destruction is target-complete. The dtor proves three ownership call-site homes at `EBP-0x10/-0x14/-0x18` for stage data, cache, and stage-object VMs, with `this @ -0x1C`; these arise from `FreeBackgroundOwned(void*)`, not dummy locals. The resource branch must test reload-disabled state before choosing VM marking versus ANM release, preserving target block order. | Canonical 389-byte ctor and 366-byte dtor plus exact viewport helpers. Handwritten VM destructor loops expand the dtor to 445 bytes and are a negative oracle. |

| PHOTO-060 | exact | The target PhotoInf/photo-overlay owner at `0x0042A8A0..0x0042C448` is `0x25730` bytes. `Draw @ 0x0042C220` is exact: it finds the best of eleven slot scores, draws all eighty display VMs, then colors/draws six overlay VMs per slot from best-slot and persistent detail-score state. | Ten canonical units total 1,616 authored bytes. Exact Draw intentionally does not hoist an overlay `AnmVm*`; target recalculates the slot/VM indexed address in each color branch and again for Draw, keeping six scalar locals and a `0x1C` frame. Hoisting the pointer creates a seventh local and shrinks the body to 370 bytes. |
| PHOTO-061 | exact | `PhotoRuntimeView::CountPhotoTargets @ 0x004168D0` is the 128-enemy photograph-capture scan on the shared owner at `0x004BDDC0`. It requires flags1 bit 0 set, bit 1 clear, bits 8..9 clear, bits 4/5 clear, and flags2 bit 6 clear; fully intersecting enemy AABBs either restart the pending ECL subroutine or set state bits `0x100`, then emit five colored charge items. The exact source uses a gapless fully-live 0x3C local aggregate `{i, enemy, captureMaximum, enemyMaximum, enemyMinimum, captureMinimum, count}` and inlines the TH08 Float3 divide body at all three half-size sites. The final capture bound must be `captureMaximum = captureMaximum + *position`: replacing it with `+=` removes two anonymous 12-byte return/copy objects and shrinks both frame and code. | Canonical 1,274-byte VC7.1 unit with all 17 constant, ECL-context, item-manager, and spawn relocations replayed; the target 0x104-byte frame has no unused dword slot. |

## Reconstructed scene selection

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| SCENE-001 | exact | Twelve scene groups index 120 persistent `0x60`-byte score records. The five exact progression helpers count captures globally or per group, sum the preceding group's unlock scores, and accept score/capture alternatives when deciding whether a group is available. | Five canonical VC7.1 units totaling 586 bytes with thirteen relocations replayed |
| SCENE-002 | exact | The shared sixteen-entry `SceneValueQueue` is exact in both directions. `Push @ 0x00450DD0` clamps a full queue to its final slot, overwrites that slot, increments the count, and returns the new count. `Pop @ 0x00450F60` decrements a nonempty queue, shifts all sixteen dwords toward the front, and returns the new first value. The target Pop evaluates each four-byte source element before rematerializing the destination index/base; expressing the element move as one four-byte POD whole-value assignment makes stock VC7.1 use that RHS-first chronology and avoids the otherwise generated ESI save/restore. Ordinary scalar assignment, pointer/dereference spellings, fixed-size `memcpy`, inline getters, identifier sweeps, and `register` RHS locals are bounded negatives. | Canonical relocation-free 78-byte Push and 91-byte Pop units; Pop replays 91/91 bytes exactly and provides a reusable small-POD copy/evaluation-order oracle |
| SCENE-003 | exact | `SceneSelectControllerView::RefreshSceneSelection @ 0x0044BBD0` enters Supervisor critical section 4, queues packed group/scene selection and two preview values, derives locked/unattempted/attempted/score-gated display state from persistent scene data, interrupts the status VMs, and resets the preview timer. | Canonical 716-byte VC7.1 unit with all 32 relocations replayed. Seven dword/pointer locals use the established hash rank, the byte display state uses the target-proven `refreshDisplayStateLocal23` bucket at `EBP-1`, and a bounded `__forceinline` queue helper restores the target count-first LHS evaluation and register chronology. |
| SCENE-004 | exact | `SceneSelectControllerView::BuildScenePreviewText @ 0x0044BEA0` stages three script-backed text VMs at timer values 8, 12, and 16. Each column distinguishes locked, unattempted, attempted, below-score-requirement, and available scenes, selects the corresponding color/text source, and rebuilds the VM at a 19-by-19 glyph size. | Canonical 1,989-byte VC7.1 unit with all 141 relocations replayed; exact inline-return and four-int ABI compiler oracles |
| SCENE-005 | exact | `SceneSelectControllerView::ResolveSceneText @ 0x0044D020` decodes one 64-byte preview-text column into the shared buffer at `0x004CA308`. Its initial rolling key is `argument2 * 11 + argument1 * 7 + 58`, then advances by `(column + 1) * 23 + index` after each byte. | Canonical 128-byte VC7.1 unit with both buffer relocations replayed |
| SCENE-006 | exact | `SceneSelectControllerView::UpdateSelectedSceneDetails @ 0x0044C670` renders the selected scene record as month/day and hour/minute digits, a fixed-width six-digit detail score, and a second six-digit unlock score whose leading zero VMs are hidden. A zero capture time replaces the date, time, and detail-score digits with sprite `0x34`. | Four canonical VC7.1 units totaling 2,453 bytes with all 122 relocations replayed; exact `time_t`, `tm`, VM-flag, score-record, and controller-layout evidence |
| SCENE-007 | exact | `SceneSelectControllerView::UpdateSceneSelect @ 0x00447D00` is the complete TH095 scene selector. It coordinates twelve group cursors, unlock limits, persistent score/record flags, title/detail VMs, asynchronous best-shot and mission-preview queues, D3D texture uploads, persistent last-selection fields, Game Start, and return-to-title. The canonical stock-VC7.1 body is exactly 16,066 bytes with the complete 3,637-instruction topology and all 520 COFF relocation destinations replayed. | Canonical `scene-select-update` unit: 16,066/16,066 authored bytes and 520/520 relocations; target-attested dependency graph and compile-time layout assertions through `+0x63CC` |
| SCENE-014 | target / compiler-observed | The scene selector navigates groups horizontally (`LEFT/RIGHT`) and scenes vertically (`UP/DOWN`). VM visibility bit 1 is cleared on a false predicate and set on true, using the target's false-arm-first branch shape. The pending upload drain clears primary data, secondary data, primary size, then secondary size; a positive scene preview upload targets texture slot 2, not slot 3. Group indices 0..10 use VM `0x20` and index 11 uses VM `0x21`; the displayed group sprite folds index 11 by subtracting 11. | Exact-sized `UpdateSceneSelect` compiler probe; target-aligned input immediates, flag mutations, field offsets, branch directions, and D3D texture-entry displacement |
| SCENE-008 | exact / compiler-observed | The final scene-selector stack closure removes the private oracle's diagnostic 4+16-byte queue fields entirely. The key correction is that the early queue lane is **not** one 92-byte aggregate: it is 23 independently live scalar/pointer locals. A VC7.1 port of TH08's patched `var_order` was used only as an ordering oracle and exposed the target contiguous lane immediately below the 72-byte shallow aggregate. The target shallow-to-deep queue order begins `loadedGroupSize0, loadedGroupFrontQueue, loadedGroupFrontValue, loadedGroupSize1` and continues through the positive/negative preview queue/value pairs to `groupPreviewDataNegativeFreeValue`; six late loaded-scene locals independently occupy `-0xC0..-0xD4`. The canonical source does **not** require the patched compiler: semantic macro aliases map these 23+6 real locals onto the collision-free stock-VC7.1 identifier buckets already calibrated by exact `ResultScreen::Draw`. Stock build 3077 then emits 13,986/13,986 non-relocation bytes structurally and the configured unit replays all 16,066 bytes. The old 92-byte aggregate produced the misleading `-0x4C` alignment hole; the earlier uncalibrated 23-scalar negative is superseded. | Target-labelled `/FAsc` home map; build-3077 ordinary-local ordering oracle; exact ResultScreen identifier calibration; canonical stock `scene-select-update` 16,066-byte / 520-relocation replay |
| SCENE-009 | exact | `LoadSceneSelectionAssets @ 0x0044D0A0` is the selector's asynchronous producer. It prioritizes face/status work, then selected best shots, then positive or negative mission previews; the three wait loops observe both stop bits and the shared completion flag. A fully live `0x220` aggregate reproduces the queue/view/path/data lane. The remaining compiler allocation is split by owner: a four-byte phase wraps only the sibling `groupPreviewQueue.Size()` result, while an eight-byte phase wraps the final `g_HelpLoadActive/g_HelpLoadComplete` completion notification. An entry-phase placement is a negative control because it shifts the entire deep lane. | Canonical 3,070-byte VC7.1 unit with all 115 file, queue, synchronization, scene-data, and compiler-local relocation destinations replayed; exact 707-instruction topology |
| SCENE-010 | exact | `SceneSaveDataView::LoadBestShotForScene @ 0x00435E90` is canonical exact for the complete 1,034-byte selected-scene best-shot loader. It resets the staged record, opens normal/extra best-shot files, copies the 24-byte image header, preserves or clears the 80-byte comment by format, decompresses pixels, and validates the three-part checksum before setting `componentsLoaded @ +0x69`. The exact stock-VC7.1 source splits the old monolithic local aggregate into a real `{path[260], input}` pair plus scalar `recordIndex`/`fileSize`, maps those real locals through target-proven identifier buckets, and writes allocation size as `width * height * componentCount`. That rank makes VC7 preserve `recordIndex * 0x78` in ESI across `_malloc`; only the real pixel allocator owns the final eight-byte compiler phase. Current-source width controls 0/4/12 bytes all fall to 933/970 comparable bytes, while eight bytes replays 970/970. | Canonical `scene-best-shot-load` unit: 1,034/1,034 authored bytes with all 16 scene-group, filesystem, allocator, decompression, checksum, string, and debug relocations replayed; exact no/4/8/12 phase-width controls retained under `.analysis/gpt-web-bestshot-exact-width-controls-20260907`. |
| SCENE-011 | exact | The shared best-shot/replay/archive decoder at `DecompressData @ 0x00456220` consumes an MSB-first token stream. Literals are copied directly; back-references use a 13-bit nonzero history offset and a four-bit length extended by three against the 8 KiB ring at `0x004E24A8`, whose write cursor starts at one. A zero offset terminates the stream. TH095 retains TH08's otherwise discarded input-byte checksum and drains trailing bits through the same fetch/checksum path. | Canonical 852-byte VC7.1 unit with `_malloc` plus three ring relocations; exact TH08 `Lzss::Decode` source-shape oracle and target stack audit |
| SCENE-012 | exact | The scene selector and Help viewer share one exact ANM texture-entry path. `GetAnmFormat @ 0x00442B40` downgrades ARGB8888/unknown to ARGB4444 and RGB888 to RGB565 when forced 16-bit color is active. `LoadTexture @ 0x00442B90` and `LoadTextureRegion @ 0x00442CA0` handle encoded images or embedded `THTX` pixels, with the regional overload applying its caller-provided destination top. `ApplyTextureAlphaBleed @ 0x00442510` is exact for ARGB8888, ARGB1555, and ARGB4444: transparent pixels accumulate four alpha-bearing neighbors, divide RGB only when more than one contributes, and preserve alpha. The target's upper-neighbor source shape is `pixel + (-Pitch / divisor)`, and both 16-bit formats truncate averaged channels through `u8` before bitfield insertion. | Seven canonical VC7.1 units totaling 2,357 authored bytes and 36 relocations. The alpha unit compares its complete 1,335-byte COFF extent: 1,292 authored bytes plus a 16-byte jump table and 27-byte selector table. A fully semantic 0x80 local aggregate reproduces the exact 0x88 frame without padding; its three 0x1C case records precede `D3DSURFACE_DESC`, `D3DLOCKED_RECT`, and the surface pointer. |
| SCENE-013 | exact | `SceneSaveDataView::LoadScenePreviewTexture @ 0x004362A0` uploads the selected staged best-shot pixels into a scene-selection texture as RGB24 or ARGB4444, then normalizes the alpha byte across its 256-by-192 preview. When no decoded component is available, it clears the full surface using the destination texture's bytes-per-pixel value. | Canonical 578-byte VC7.1 unit with both relocations replayed; shared score/best-shot record layout and exact D3DX call contract |

## Reconstructed PBG archive and file core

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| PBG-001 | exact | Twenty-four ZUN-owned PBG helpers are canonical exact for 3,662 authored bytes, covering entry construction, archive construction/load/release/destruction, encrypted entry read/decompress, THA1 header parse, lookup/name ownership, and CPbgFile close/read/write/tell/size/seek/whole-file/path expansion. Generated deleting wrappers remain compiler-owned. | Twenty-four canonical VC7.1 units covering the same vtable, Win32, CRT, archive-helper, allocation, decrypt, checksum, and EH dependency spine. |
| PBG-002 | exact | The TH095 class source shape puts the empty `IPbgFile` constructor and virtual destructor in the class body. VC7.1 then inlines the empty base lifetime into `CPbgFile` as direct base/derived vtable stores while still emitting the standalone virtual destructor required by the vtable. Out-of-line empty TH08 definitions make the derived constructor/destructor one byte short and introduce non-target calls. | Exact 52-byte `CPbgFile` constructor, 82-byte destructor, and 20-byte `IPbgFile` destructor units. |
| PBG-003 | exact | `PbgArchive::CopyFileName @ 0x00455790` keeps the TH08 strlen/strcpy topology but TH095 allocates with CRT `malloc @ 0x00485780` rather than TH08's `MemAlloc`/`GlobalAlloc` macro. That single allocator-source change closes the exact 139-byte body. | Canonical copy-name unit plus solved `_malloc` relocation; the GlobalAlloc probe differs only at the allocation sequence. |
| PBG-004 | exact | `CPbgFile::ReadWholeFile @ 0x00455BF0`, `CPbgFile::Open @ 0x00455920`, and `PbgArchive::AllocEntries @ 0x00455580` are all canonical exact. ReadWholeFile uses TH095 CRT `malloc/free` plus `{oldLocation,dataLen,data}`. Open preserves the four independent TH08 locals and stock-VC7 backing rank so the MAX_PATH array keeps its allocation boundary. AllocEntries keeps the fully live `{buffer,i,entryData}` outer aggregate, restores TH08 `DeleteArray` safe-delete semantics, and puts the genuine `filenameSize` inside a bounded inline `SeekPastString` helper so it occupies the late allocation phase. | Canonical 199-, 330-, and 524-byte units with all allocator/file/LZSS/EH relocations replayed; no inert storage. |
| PBG-005 | exact | TH095 archive ownership now includes `PbgArchiveEntry::PbgArchiveEntry @ 0x00452E50`, the relocation-free 23-byte `filename=NULL` ctor used by AllocEntries vector construction. `Load` reopens the archive after parsing, `Release` uses a force-inline filename-free helper, and the entry dtor conditionally CRT-frees the filename without clearing it. | Canonical archive ctor/load/release plus entry ctor/dtor total 566 bytes; the entry ctor is credited once at its own target address. |
| PBG-006 | exact | `ReadDecompressEntry @ 0x004550F0` keeps the archive file open, computes compressed size from the next entry offset, optionally reads directly into the caller buffer, and selects one of eight 0x0C decrypt profiles at `0x004A42C8` from `CalculateByteChecksum(filename, strlen(filename)) % 8`. Profile fields are XOR value `+0`, increment `+1`, chunk size `+4`, and max bytes `+8`. When compressed and decompressed sizes differ it calls exact `DecompressData`; scratch storage is freed only when it is not the caller buffer. The negative structured buffer condition (`sizes differ || outBuffer == NULL`) is codegen-significant. | Canonical 464-byte unit with all 14 lookup, allocator, file-I/O, checksum/profile, decrypt, decoder, debug, and free relocations replayed; exact 71-byte filename checksum dependency. |
| PBG-007 | exact | TH095 archives use a 16-byte `THA1` header, replacing the stale TH08 12-byte layout. After in-place `1B/37/0x10/0x10` decryption, fields are magic `0x31414854`, encoded file-table decompressed size minus `123456789`, encoded compressed size minus `987654321`, and encoded entry count minus `135792468`. The compressed file table is the final `encodedCompressedSize` bytes of the archive, is decrypted with `3E/9B/0x80/compressedSize`, then exact-decompressed and passed to `AllocEntries`. A fully live 0x20 aggregate `{compressedBuffer, fileTableOffset, size, header, decompressedBuffer}` reproduces every target local home without padding. | Canonical 539-byte ParseHeader unit with all 13 PBG file, decrypt, allocator, decoder, entry-builder, cleanup, and Japanese error-string relocations replayed; `PbgArchiveHeader` layout assertions updated to 0x10. |
| PBG-008 | exact | `CalculateByteChecksum @ 0x0041BA30` is the PBG filename profile selector: a real cursor local walks exactly `size` bytes while an 8-bit accumulator wraps naturally. The target uses a `while (size--)` body rather than the superficially equivalent `for` form, which is two bytes longer under VC7.1. | Canonical relocation-free 71-byte unit; direct exact caller in `ReadDecompressEntry`. |

## Reconstructed compression and runtime math core

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| COMP-001 | exact | Seven TH095 LZSS encoder-tree helpers at `0x00456580..0x0045698E` are canonical exact: `InitTree`, `InitEncoderState`, `AddString`, `DeleteString`, `ContractNode`, `ReplaceNode`, and `FindNextNode`. `AddString @ 0x00456650` preserves the TH08 algorithm and becomes exact only when its five real locals form the fully-live 20-byte aggregate `{delta, matchLength, testNode, child, i}`. The dictionary is `0x004E24A8` and the 8,193-node tree is `0x004CA498`. | Seven canonical VC7.1 units totaling 983 authored bytes and 50 relocations. |
| COMP-002 | exact | `CompressData @ 0x00455E10` is the 1,035-byte TH095 LZSS encoder. TH08 supplies the token semantics; target-proven backing identifiers recover the long-lived locals and byte-sized `outBitMask`. The final source-shape key is one nested token-writing scope: outer `dictHead` stays at `EBP-0x34`, while the single real `bitfieldMask` is declared only for the main encode loop plus final terminator and therefore lands at `EBP-0x38`. Per-pack block locals grow the frame and paired identifier swapping alone is ineffective. | Canonical `compress-data` unit: 1,035/1,035 bytes, 315/315 mnemonics, 1,003/1,003 comparable bytes, and all eight allocator/LZSS/ring relocations replayed. |
| COMP-003 | exact | `DecompressData @ 0x00456220` becomes exact only after restoring TH08's discarded `checksum` accumulator and its full trailing-bit fetch path. TH08's patched `var_order(currByte, outCursor, matchOffset, i, inBitMask, inCursor, inBits, size, matchLength, checksum, dictValue, outBitMask, dictHead)` is reproduced with already-proven stock-VC7 identifier buckets; the real `size` and `matchLength` scalar declarations must be reversed relative to their obvious semantic order. No frame padding is present. | Canonical 852-byte unit with 836/836 comparable bytes and four relocations; isolated source-shape probes reduce 100 residual bytes to eight, then to zero. |
| MATH-001 | exact | `Rng::GetRandomU16/U32/F32/F32Signed @ 0x0041B410..0x0041B4FD` preserve the TH08 RNG recurrence and conversion source shape. `GetRandomU32` concatenates two sequential 16-bit draws; the float helpers divide by the rounded `UINT_MAX`/`INT_MAX` constants used by VC7.1. | Four canonical units totaling 214 bytes and seven relocations, plus many independent exact ECL/Bullet/ANM callers. |
| MATH-002 | exact | `AddNormalizeAngle @ 0x0041B500` is TH08-derived but TH095 raises the shared escape guard from `i++ > 16` to `i++ > 32` in both positive and negative wrap loops. That two-immediate semantic change is required for the exact 127-byte target. | Canonical 127-byte unit with four float-constant relocations; the TH08-16 probe differs at exactly the two guard immediates. |
| MATH-003 | exact / production | `Float3::FromAngleMagnitude @ 0x00441DA0` is the canonical 32-byte three-float receiver for angle/magnitude conversion, and `Rotate @ 0x0041B600` is the canonical 95-byte two-vector rotation helper. Production `ExtendedVector` and `PhotoBulletVector` are only layout views over the same `{x,y,z}` storage, so their calls must canonicalize the receiver to `Float3`; PhotoEffect/PhotoStage rotate proxies must canonicalize to `Rotate`. DIFFBUILD keeps each historical proxy symbol for exact relocation replay. The exact macro spelling is itself codegen-visible under VC7.1: redundant parentheses grew `photo-bullet-update-deceleration` from 145 to 148 bytes, while token-equivalent member-call expansion restores exact code. | Hash-attested Ghidra function bounds/signatures at `0x00441DA0/0x0041B600`; canonical exact math units; `PhotoBulletVector`/`Float3` layout assertion; 97/97 affected exact replay; fresh 88-TU link closure for both target addresses. |
| TIMER-001 | exact | `ZunTimer::Add @ 0x0041B830` first copies `current` to `previous`, then adds either the raw delta when `g_AnmGameSpeed > 0.99f` or the game-speed-scaled delta otherwise, and truncates `subFrame` back to integer `current`. The positive `> 0.99f` lexical branch is codegen-significant; the algebraically equivalent `<=` spelling produces a different VC7.1 block order. | Canonical 98-byte unit with four game-speed/float-conversion relocations and repeated exact callers in ECL, Bullet, and UI timing paths. |

## Reconstructed replay system

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| REPLAY-001 | target-observed | TH095's `ReplayManager` is `0x12C` bytes. Its mode is at `+0x00`, header/input/FPS allocations at `+0x04/+0x08/+0x0C`, stream cursors at `+0x10/+0x14`, sampled playback FPS at `+0x18`, frame counter at `+0x1C`, active input-data pointer at `+0x20`, and calc/draw chain elements at `+0x124/+0x128`. | Exact constructor, initializer, destructor, frame processor, and compile-time layout assertions |
| REPLAY-002 | exact | `ReplayManager::Create @ 0x004345B0` allocates and initializes the manager, then registers update and draw callbacks at priorities 7 and 3. `Load @ 0x00434700` uses mode 2 without chain registration, while both factory paths reproduce the original VC7.1 new/delete EH machinery. | Canonical 330-byte and 207-byte units with 28 relocations replayed |
| REPLAY-003 | exact | `ReplayManager::ProcessFrame @ 0x00434830` records three 16-bit input values per frame into a `0x69780`-byte stream, samples rounded FPS every 30 frames into a separate byte stream with saturation at 255, and restores the same values during playback. | Canonical 408-byte unit with all 24 relocations replayed |
| REPLAY-004 | exact | Playback FPS is drawn at `(485, 452)`; samples below 30 use `0xFF5050FF`, samples below 50 use `0xFFA0A0FF`, and higher samples use white. Both replay callbacks are suppressed by target global flag bit 2. | Canonical `replay-manager-draw-fps`, `replay-manager-on-update`, and `replay-manager-on-draw` units |
| REPLAY-005 | exact | `ReplayManager::Initialize @ 0x004342A0` builds a 36-byte `th95r` version-1 header, allocates `0x69780` input bytes and `0x11940` FPS bytes in record mode, and saves/restores the RNG seed, `0xC8`-byte runtime snapshot, and 48-byte player-configuration selection across record/playback. | Canonical 582-byte VC7.1 unit with all 16 relocations replayed |
| REPLAY-006 | exact | `ReplayManager::LoadReplay @ 0x00435130` supports loose `replay/%s` files and archive-backed data, applies `AA/E1/0x400` then `3D/7A/0x80` transforms, LZSS-decompresses the input stream, and locates FPS samples at `inputData + 0xF8 + inputStreamSize`. | Canonical 540-byte unit with all 15 relocations replayed |
| REPLAY-007 | exact | `ReplayManager::WriteReplay @ 0x00434A90` space-pads the replay name to eight bytes, finalizes the input and sampled-FPS stream lengths, LZSS-compresses their combined payload, applies the `3D/7A/0x80` then `AA/E1/0x400` transforms, writes the 36-byte header and payload, and appends two four-byte-aligned `USER` sections containing level, scene, time, score, slow-rate, and comment metadata. | Canonical 1,691-byte VC7.1 unit with all 44 relocations replayed |
| MATCH-006 | exact | Twelve canonical ReplayManager units reproduce 4,351 authored bytes and all 153 relocations, covering initialization, lifetime, allocation, chain integration, loading, writing, per-frame stream processing, and playback FPS output. | Pinned VC7.1 relocation replay for every listed ReplayManager unit |
| REPLAY-008 | exact | `ResultScreen::LoadReplays @ 0x0042A710` deletes and reloads all twenty `th95_%.2d.rpy` slots, then clamps the result-screen replay cursor to zero. The cursor is at `ResultScreen+0x4604` and the replay-pointer array begins at `+0x6CE8`. | Canonical 390-byte VC7.1 unit with all five relocations replayed and compile-time layout assertions |
| REPLAY-009 | exact | `ResultScreenReplayCursor::Move @ 0x0041B760` wraps or clamps the current selection, then rejects up to sixteen disabled entries at cursor `+0x90`; wrap mode is at `+0xD0` and the disabled-entry count is at `+0xD4`. `ResultScreen::UpdateCursor @ 0x00429AB0` uses this helper for timer-gated vertical selection, sends VM interrupts 2/3, and plays menu-move SFX 12. | Canonical 200-byte and 455-byte VC7.1 units with all eight update relocations replayed |
| MATCH-007 | exact | Twenty-two ResultScreen/replay-UI units reproduce 13,605 authored bytes. The 373-byte constructor is exact: the `ResultScreenTimer` constructor stores `current`, `previous`, then `subFrame` in target order; only the first `replayCursor @ +0x4604` selects a distinct 0x18 construction phase, placing its hidden receiver at `EBP-0x2C`, the otherwise-identical `photoCursor @ +0x6D48` at `-0x30`, and outer `this` at `-0x34`. The same cluster proves twenty-five `0x2CC`-byte VMs in constructor-visible `21+2+1+1` ownership groups, help text at `+0x46E4`, eleven groups of ten two-line scene labels at `+0x46E8`, twenty replay pointers at `+0x6CE8`, and calc/draw Chain ownership at `+0x6E24/+0x6E28`. | Pinned VC7.1 relocation replay for all twenty-two current `ResultScreen.cpp` units plus compile-time layout assertions |
| REPLAY-010 | exact | `ResultScreen::Update @ 0x00426BF0` is canonical exact for the complete sixteen-state TH095 flow. The target-authored body is `0x1947` (6,471) bytes and the adjacent sixteen-entry state table plus four-entry state-6 table extend comparison to `0x1997` (6,551) bytes. The former `0x18C` frame residual partitions into a 0xB0 phase owned by the state-13 photo-result `SetState(5)`/timer reset and a 0xDC phase owned by the following `replayCursor.Disable(1)`. After those deep phases are correct, only the first four early 21-VM interrupt loops belong to a shared source-local force-inline helper; the later three loops remain in the caller. That ownership interleaves seven real loop indices with nine compiler receiver temps exactly, reproducing all 1,313 body mnemonics, all 186 body/table relocations, and every stack home. | Canonical `result-screen-update` unit: 6,471 authored bytes, 6,551-byte compare extent, 186 relocations, and 5,807/5,807 non-relocation bytes replayed |
| REPLAY-011 | exact | Cursor `Push @ 0x0041B660` and `Pop @ 0x0041B6E0` preserve selection/count pairs in two sixteen-entry histories and clear disabled entries. `ResultScreenTimer::Tick @ 0x0041B8A0` accumulates fractional time only when game speed is at most `0.99f`; otherwise it advances by one. The timer helper has 27 target callers. | Three canonical VC7.1 units totaling 343 bytes with all five timer relocations replayed |
| REPLAY-012 | exact | `ResultPhotoDataView::FindBestShot @ 0x00429450` scans eleven `0x2214`-byte photo slots and returns the index with the largest signed score at slot `+0x21F4`, or `-1` when no nonnegative maximum exists. | Canonical 108-byte VC7.1 unit with no relocations |
| REPLAY-018 | exact | The target calls this `PauseInf` in its diagnostics, while the reconstructed `ResultScreen` type owns both TH095 pause/help and photo-result UI state. `ResultScreen::ResultScreen @ 0x004264B0` is canonical exact for all 373 bytes and fifteen relocations: target field-store order fixes the timer constructor, and a first-cursor-only construction overload selects the independently observed 0x18 frontend phase without changing object layout or runtime cursor values. `Initialize @ 0x00426630` loads slot-10 `pause.anm`, reads `sprt/help.txt`, parses `level:` blocks into the `11 x 10 x 0x58` scene-label table, and selects the active group. `LoadAnm/ReleaseAnm @ 0x00426820/0x00426860`, `Create @ 0x00426A50`, `Destroy @ 0x00426B90`, and `~ResultScreen @ 0x00426880` are also exact; teardown cuts both Chain nodes, deletes twenty replay managers, frees help text through its real ownership phase, retires loaded-pause VMs, clears the singleton, then destroys the twenty-five embedded VMs. | Seven canonical lifecycle units totaling 1,797 bytes with all 85 ANM, EH, vector-construction, filesystem, Chain, allocation, ownership, and cleanup relocations replayed |
| ANM-013 | exact | `AnmVmId::GetVm @ 0x004452F0` resolves a four-byte VM handle through `AnmManager` and clears stale handles; `SetInterrupt @ 0x00445330` forwards an interrupt through the same manager. The wrappers have seven and ten target callers. | Two canonical VC7.1 units totaling 85 bytes with all four relocations replayed |
| ANM-014 | exact | `AnmManager::GetVm @ 0x00445110` rejects the zero `AnmVmId`, walks the intrusive VM list through `AnmVm+0x00`, and compares the handle at `+0x10`; its 35 direct callers share exact interrupt, deferred-deletion, position set/get, and ANM-file-wide deletion helpers. `AnmVmId::SetSprite @ 0x00445360` resolves the same handle and dispatches through the VM's owning `AnmLoaded` at `+0x230`. | Seven canonical VC7.1 units totaling 480 bytes with seven relocations replayed; independent four-byte handle/operator oracle |
| ANM-015 | exact | The bounded `AnmVmColorInterpolationView::SetColor1Interpolation @ 0x00452D70` reproduces the `AnmVm` member behavior: it initializes interpolation timer slot 1, stores its mode, and copies the two packed RGB endpoints into `color1Initial` and `color1Final` without changing alpha. The isolated translation unit avoids perturbing compiler-local switch symbols in the established ANM object. | Canonical 222-byte VC7.1 unit with no relocations; fourteen pre-existing `AnmManager.cpp` units replayed |
| ANM-016 | exact | `AnmTextManagerView::DrawTextLeft @ 0x00443CE0` formats into a 128-byte buffer, forwards the loaded-sprite bounds/scales plus VM glyph size to `DrawTextInner`, and makes the VM visible. TH08 documents `#pragma var_order(buf, fontWidth)` for the same helper. Stock VC7.1 reproduces that patched order by backing the real buffer and glyph-width locals with the target-proven `restartCommandProcessingLocal05` and `averagedPanLocal12` identifier buckets. | Canonical 238-byte VC7.1 unit with all six `vsprintf`, `__ftol2`, and `DrawTextInner` relocations replayed; no assembly or inert locals |
| ANM-017 | exact | The complete ANM text alignment/render path is exact. `DrawTextRight @ 0x00443DD0` subtracts half a scaled glyph advance from the sprite right edge, while `DrawTextCentered @ 0x00443F80` subtracts a quarter advance from its midpoint; the unlisted real `x` local uses the `textXLocal00` bucket so stock VC7.1 matches TH08 patched `var_order(buf,fontWidth)` semantics. Shared `DrawTextInner @ 0x00443C70` has no TH08 small-font fallback. `RenderTextToTextureBold @ 0x0041C8F0` uses persistent 30/34/36/38-pixel fonts, renders shadow plus foreground into the `1024x64` CPU buffer, applies alpha/color bleed, then uploads directly with `D3DXLoadSurfaceFromMemory`. Its named upload/GDI locals naturally form one exact 0x40 semantic aggregate. | Five canonical VC7.1 units totaling 1,700 authored bytes with all 49 formatter, float-conversion, font, GDI, text-buffer, and D3DX relocations replayed |
| ANM-018 | exact | The TH095 text buffer's `InvertAlpha @ 0x0041C600` handles `A8R8G8B8`, `A1R5G5B5`, and `A4R4G4B4`; the 1555 path also clears RGB when inverted alpha becomes transparent. `ApplyAlphaBleed @ 0x0041C1E0` fills transparent RGB from up to four opaque neighbors for 8888 and 4444 buffers, averaging only when at least two neighbors contribute. Its two pixel-format scopes are truthfully represented by one 0x38 semantic aggregate of two 0x1C records; the remaining target-written scalar locals are restored through identifier-hash backing names, including the real but otherwise unused entry flag. | Canonical 373-byte inversion and 911-byte alpha-bleed units totaling 1,284 authored bytes; all eight neighbor-accumulator relocations replayed |
| ANM-019 | exact | TH095 owns a `0x124`-byte CPU text buffer whose first `0x100` bytes are initialized from the high RNG byte divided by two. The remaining fields own a bottom-up `1024x64` GDI DIB, with a seven-entry D3D-format table and 16-to-32-bit fallback policy. `TryAllocateBuffer @ 0x0041BF90` is canonical exact for all 490 bytes without TH08's unused `u32 padding`: a fully live shallow aggregate `{originalBitmapObj, deviceContext, imageWidthInBytes}`, the real `0x6C` bitmap-info object, and a fully live deep aggregate `{bitmapData, bitmapObj, formatInfo}` reproduce the patched-`var_order` physical lanes under stock VC7.1. The shallow aggregate uses the established `averagedPanLocal12` identifier bucket; all six aggregate fields participate in real GDI/allocation/publication operations. | Eight lifecycle canonical units totaling 1,331 bytes with 42 relocations; allocator unit is 490/490 with all five GDI/callee relocations replayed and TH08 padding retained only as a negative oracle |
| ANM-020 | exact | `AnmVm::AnmVm @ 0x00401EB0` naturally invokes the compiler-emitted `AnmVmBase` constructor and constructs the derived interrupt-return timer before clearing the complete `0x2CC`-byte VM and writing `-1` to `activeSpriteIndex @ +0x234`. The independently emitted `AnmVmBase::AnmVmBase @ 0x00401F10` initializes the current/wait timers at `+0x50/+0x5C`, seven interpolation timers at `+0x68`, and seven secondary timers at `+0xBC`; the derived constructor has twelve target callers. | Canonical 90-byte derived and 243-byte implicit-COMDAT base constructor units; one relocation replayed |
| ANM-021 | exact | `AnmManagerPreloadView::ServicePreloadedAnims @ 0x004438E0` scans thirteen `0x120`-byte TH095 preload slots. Each slot embeds the legacy `0x1C`-byte `AnmLoaded`, a release-pending latch, and a `0x100`-byte path. Pending release takes priority and clears its latch after `ReleaseAnm`; otherwise a nonzero entry count advances `PostloadAnmEntry` and aborts the frame on failure. This target-local 13-slot layout supersedes the adjacent TH08 256-entry shape. | Canonical 147-byte VC7.1 unit with both release/postload relocations; hash-attested target stride/count and independently exact Supervisor caller |
| ANM-022 | exact | `PostloadAnmEntry @ 0x00443480` walks the linked raw-entry offsets while accumulating sprite/script bases and uploads the scheduled entry through `LoadTextureData`; it clears pending state on completion or failure and advances it when more entries remain. `ReleaseAnm @ 0x00443980` validates one of thirteen slots, marks every owned VM for deletion, releases each `0x10`-byte texture entry, frees the texture/sprite/script/raw allocations, and zeroes the complete `0x120`-byte slot. | Canonical 284- and 305-byte VC7.1 units with all nine debug, upload, VM, entry, and allocator relocations; eleven target callers of the release hub |
| ANM-023 | exact | `LoadTextureData @ 0x004435A0` validates TH095 ANM version 4 entries, creates an external, embedded, or empty texture, applies D3D priority/preload, binds every raw sprite through the surface-to-source scale, and records each script offset. The serialized entry is `0x40` bytes, each runtime texture slot is `0x10` bytes, and the format is target-distinct from TH08's version-3 entry. `ReleaseAnmEntry @ 0x00443AC0` releases the owned COM texture and raw allocation. | Canonical 829- and 78-byte VC7.1 units with all seventeen error-string, texture-construction, sprite-binding, and allocator relocations replayed against the hash-attested target |
| ANM-024 | exact | The TH095 texture-construction family is target-complete. `CreateTextureFromFile @ 0x00442E10` normalizes the format, creates a managed texture with linear filtering, applies alpha bleed, and stores bytes-per-pixel at runtime slot `+0x0C`. `CreateTextureFromAnm @ 0x00442E90` directly uploads the `THTX+0x10` pixels through `D3DXLoadSurfaceFromMemory`, unlike TH08's intermediate surface and row-copy path. `CreateEmptyTexture @ 0x00442FC0` creates a managed blank texture and records the same stride metadata. `AnmLoaded::LoadSprite @ 0x00443B10` copies the complete `0x44`-byte sprite and derives normalized UVs plus unscaled pixel dimensions. | Four canonical VC7.1 units totaling 832 bytes with all eighteen format, D3D device, D3DX, alpha-bleed, and stride relocations replayed; the sprite geometry unit is relocation-free |
| ANM-025 | exact | The complete TH095 ANM load/preload spine is exact. `ReadAnmEntries @ 0x00443070` owns thirteen slots, defers replacement-slot release to the main-thread service, preserves the filename, counts linked version-4 entries/sprites/scripts, allocates `0x10`-byte texture slots and `0x44`-byte sprites, and stages external texture files. `LoadAnm @ 0x00443010` drains postload synchronously; `PreloadAnm @ 0x004432E0` reuses a loaded slot or waits for main-thread service with cancellation at `Supervisor+0x650`; `LoadExternalTextureData @ 0x004433A0` validates version 4 and owns external file bytes. | Four canonical VC7.1 units totaling 1,107 bytes with all 36 diagnostic, filesystem, wait, allocator, and loader relocations replayed |
| ANM-026 | exact | The TH095 surface-file ownership path is exact. `LoadSurface @ 0x004440F0` consumes either a loose file or deferred bytes, decodes it through a temporary `640x1024` image surface, creates primary and secondary D3D surfaces, and records `D3DXIMAGE_INFO`. `ReleaseSurface @ 0x00444390` releases both COM surfaces and any deferred file bytes. `CopySurfaceToBackbuffer @ 0x00444460` lazily rebuilds the primary surface from the secondary copy, then copies the requested rectangle to the backbuffer. The target layout is primary surfaces `+0x11DC`, secondary surfaces `+0x125C`, deferred data `+0x12DC`, sizes `+0x135C`, and 32 image-info records `+0x13DC`. | Three canonical VC7.1 units totaling 1,317 bytes with all 34 filesystem, diagnostic, D3D8, D3DX, and allocator relocations replayed; a private `AnmSurface.cpp` ABI view preserves all fourteen established `AnmManager.cpp` units without compiler-local label churn |
| ANM-027 | exact | The per-frame ANM capture dispatcher and both consumers are exact. `TakeScreenshots @ 0x00421D00` drains texture and surface requests at manager `+0x0C/+0x08`, then resets each index to `-1`. Texture rectangle state occupies `+0x3817D0..+0x3817EC`; TH095 adds `textureEntryIndex @ +0x3817F0`, so `CaptureToTexture @ 0x00444620` can select any `0x10`-byte entry in a loaded ANM rather than only entry zero as in TH08. Surface rectangle state occupies `+0x3817F4..+0x381810`; `CaptureToSurface @ 0x00444760` recreates primary and secondary surfaces at the requested destination size and copies the backbuffer region into both. | Three canonical VC7.1 units totaling 1,105 bytes with all 21 dispatcher, flush, release, D3D8, and D3DX relocations replayed; target-local caller fields and a pinned-compiler aggregate-local oracle establish the TH095-specific extra selector |
| ANM-028 | exact / compiler-observed | TH095 animation VMs are their own intrusive lifetime nodes: next and previous links are at VM `+0x00/+0x08`, the render-layer link is at `+0x04`, render mode is at `+0x0C`, and the nonzero handle is at `+0x10`. `AddVm @ 0x00444D10` is canonical exact for all 236 authored bytes. The decisive source shape keeps the original postfix-id semantics but gives the first increment a fully live `AnmVmLifecycleView::Id *incrementReceiver`; that real pointer naturally takes target `EBP-0x04`, while the untouched zero-valued `Id()` equality temporary moves to `-0x08`. No padding or inert local is used, and all thirteen pre-existing `AnmVmLifecycle.cpp` exact units cold-replay unchanged. `RemoveVm @ 0x00444E00` is exact for intrusive unlink/destruction. The three creation entries allocate a `0x2CC` VM and initialize render mode, direct screen position, or TH095 world position through `PhotoToScreen`; all three are now canonical exact. | Canonical `anm-add-vm` and removal units plus canonical 161-byte CreateVm, 180-byte CreateVmAtScreen, and 169-byte CreateVmAtWorld units |
| ANM-029 | exact | The ANM frame shell is target-complete. `OnUpdate @ 0x00444980` forwards into exact `UpdateVms @ 0x00444B10`; that core clears nine embedded draw-list sentinels, traverses the lifetime list, executes/removes eligible VMs, and rebuilds nine render lists. The target PhotoGameTask suppression gate keeps the positive `state != NULL && !flag28 && (...)` shape. Its final commutative OR is source-sensitive: a force-inlined `AnmUpdateEitherFlag(flag0, drawVms)` preserves target bit2-first/bit0-second loads while accumulating into `EDX`, reproducing `or edx,eax` without operand swapping. `DrawLayer @ 0x00444C80` separately requires the natural nested `if { } else if (!flag26)` gate. | Twelve canonical frame-shell/draw-layer units totaling 771 authored bytes with all 31 relocations replayed; the 358-byte UpdateVms unit is exact with 326/326 comparable bytes. |
| ANM-030 | exact / compiler-observed | `AnmManager::DrawInner @ 0x0043ECD0` is the shared seven-caller screen-space quad hub. It applies manager shake at `+0x20`, optionally rounds to nearest-even and subtracts the D3D8 half pixel, installs sprite UVs, culls against the active Supervisor viewport, caches the texture and diffuse vertex shader, applies manager color mixing at `+0x00/+0x04`, and appends the quad to the triangle-list batch. Its four target `FRNDINT` sites at `0x0043ED77/85/93/A1` require the user-authorized TH08 source-family inline-x87 sequence: stock build 3077 rejects all tested clean frontend spellings with C4163, while TH08 independently contains the same stack-preserving four-value form. The exception and its non-MSVC semantic fallback are documented inline and are not transferable to other functions. | Canonical `anm-draw-inner`: 1,497/1,497 bytes with all 93 relocations replayed; all 18 accepted `AnmDrawCore.cpp` units cold-replay exact |
| ANM-031 | exact | `DrawNoRotation @ 0x0043F4A0` and `DrawNoRotationNoRound @ 0x0043F760` implement TH095's three-way horizontal and vertical sprite alignment from `AnmVm+0x228`: centered, leading-edge, or trailing-edge. Both include `positionOffset`, publish a common Z to the four shared vertices, and enter `DrawInner`; only the first floors centered X/Y and requests its D3D8 half-pixel path. A natural four-field sprite-dimensions aggregate reproduces the target's VC7.1 local layout without padding or assembly. | Two canonical VC7.1 units totaling 1,354 bytes with all 98 constant, `_floor`, shared-vertex, and `DrawInner` relocations replayed |
| ANM-032 | exact / compiler-observed | `TranslateRotation @ 0x0043FA00` applies the shared `x*cos-y*sin+xOffset`, `x*sin+y*cos+yOffset` transform exactly. Its sole caller `Draw2D @ 0x0043FA40` implements TH095's full rotated-quad path: it creates four local X/Y pairs from the same three-way alignment fields as the axis-aligned helpers, adds `positionOffset` before rotating, writes `position.z` without the Z offset, and submits through `DrawInner` without rounding. Target `FSINCOS @ 0x0043FA7B` is expressed through the user-authorized TH08 `FLD/FSINCOS/FSTP/FSTP` source form because stock VC7.1 emits separate `FSIN`/`FCOS` for every tested clean C++ alternative. | Canonical relocation-free 58-byte helper and canonical `anm-draw-2d`: 542/542 bytes with all 16 relocations replayed |
| ANM-033 | exact / compiler-observed | `ProjectCameraFacingQuad @ 0x0043FC60` projects the VM's offset world position and the current background viewport's `cameraRight @ +0x30` through its view/projection/viewport fields at `+0x4C/+0x8C/+0xCC`. It rejects projected Z outside `[0,1]`, derives a screen-space sprite scale from the projected delta, applies TH095's three-way alignment, and writes `vm->position.z` to all four vertices rather than TH08's projected Z. Two fully live local aggregates reproduce the target `0xE0` frame and lifetime reuse without padding or inert storage. Target `FSINCOS` sites at `0x0043FC7B` and `0x0043FEB3` use the same narrowly authorized and inline-documented TH08 x87 source form. `DrawCameraFacingQuad @ 0x004400F0` is its exact failure-gated `DrawInner` wrapper. | Canonical `anm-project-camera-facing-quad`: 1,163/1,163 bytes with all 26 relocations replayed; canonical 48-byte wrapper with both relocations replayed |
| ANM-034 | exact | Render mode 6 is `AnmManager::DrawMode6 @ 0x00440120`, a TH095-specific camera-facing sprite path coupled to the background photo blend. After projection it measures the VM's offset world position from `g_BackgroundCameraPosition`, selects and optionally manager-mixes the VM color, and uses `Background+0x1FEC/+0x1FF0/+0x1FF4` as near distance, far distance, and blend target color. Beyond the near distance it interpolates RGB toward the background color while fading alpha; a normalized fade at or beyond one rejects the draw. | Canonical 798-byte VC7.1 unit with all 30 projection, background, camera, square-root, float-conversion, shared-vertex, and `DrawInner` relocations replayed; the fully live `AnmPhotoBlendDrawLocals` aggregate reproduces the target stack layout |
| ANM-035 | exact | Render mode 7 is `AnmManager::DrawMode7 @ 0x00440950`, TH095's projected-3D photo-blend path. It first runs `Project3DQuad`, transforms each manager-local untextured vertex through the cached world matrix at `AnmManager+0xECC`, and measures that vertex independently from `g_BackgroundCameraPosition`. Within the near distance it preserves the selected VM color; through the blend range it interpolates RGB toward `Background+0x1FF4` while preserving VM alpha; at or beyond the far side it takes the background color but restores VM alpha. Unlike mode 6 it neither applies manager mix color nor rejects the fully blended quad. It submits with `DrawInner` flag 2 and restores all four shared RHW values. | Canonical 691-byte VC7.1 unit with all 33 projection, vector-transform, camera, square-root, float-conversion, background, shared-vertex, and `DrawInner` relocations replayed; a fully live 32-byte semantic aggregate reproduces the target local-slot order, and the function ends at `0x00440C02` before thirteen alignment bytes |
| ANM-036 | exact | The direct generated-geometry lane at `0x00440C10..0x00441592` is canonical. `Draw3D` updates the VM scale/rotation matrix, applies TH095 three-way X/Y alignment plus `positionOffset`, preserves the target's duplicated `uvScrollPos.x` cache test, installs world/texture transforms, and submits the manager quad vertex buffer as a triangle strip. `InitializeHorizontalTextureStrip` alternates top/bottom UV rows across generated vertices. `DrawVertices` is the visible/alpha-gated triangle-strip submitter, while `DrawTriangleFan` is the distinct Z-write-disabled fan path used by the pulsing radial trail. | Four canonical VC7.1 units totaling 2,415 bytes with all 59 relocations replayed. Target-proven identifier-hash backing names restore the original patched `var_order` live-local layout for `Draw3D` and the relocation-free strip initializer without assembly, padding, or inert locals. |
| ANM-037 | exact | `AnmManager::SetupVertexBuffer @ 0x00442260` initializes the four manager-local 0x14-byte source vertices at `+0x1774`, publishes their XYZ plus W/diffuse bit patterns into four 0x18-byte software-fallback vertices at `g_BackgroundQuadVertices @ 0x004CA290`, then creates, locks, fills, unlocks, and binds one 80-byte managed D3D8 vertex buffer. TH095 always executes this managed-buffer path; unlike the TH08 source oracle, the target has no vertex-buffer-disabled branch. | Canonical 682-byte VC7.1 unit with all 25 global/device relocations. Direct typed reinterpret expressions reproduce the target's copy sequence; introducing helper pointer locals changes stock VC7.1 code generation and is therefore a useful negative source-shape oracle. |

## Reconstructed ASCII text manager

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| ASCII-001 | exact | TH095's global `AsciiManager @ 0x004A9F80` is `0x13DAC` bytes and is structurally different from the stale TH08 pause/retry layout. It owns four `0x2CC`-byte ANM VMs at `+0x000/+0x2CC/+0x598/+0x80B8`, 256 regular strings at `+0x864`, 64 GUI strings at `+0x6864`, string counts and drawing state at `+0x8064..+0x8098`, `ascii.anm`/`capture.anm` handles at `+0x80A0/+0x80A4`, 723 score popups at `+0x8384`, and 128 time popups at `+0x121AC`. | Compile-time layout assertions and target field accesses across `0x00401280..0x00401920` |
| ASCII-002 | exact | `AsciiManager::AsciiManager @ 0x00401D30` constructs the four inline VMs, while `~AsciiManager @ 0x00402010` releases their four optional generated-vertex payloads. The two natural empty C++ member bodies reproduce all 552 authored bytes and all twelve relocations through VC7.1 member construction/destruction. | Canonical constructor/destructor units plus exact `AnmVm` construction foundation |
| ASCII-003 | exact | The complete ASCII chain lifecycle is exact. `RegisterChain @ 0x00401280` installs calc priority 1 plus draw priorities 28 and 21; `AddedCallback` clears the full manager, loads `ascii.anm` and `capture.anm`, resets its state, and calls the target-empty `InitializeVms @ 0x00423430`; `DeletedCallback` releases both files. `CutChain` intentionally unlinks only the calc and low-priority draw nodes, preserving the target's high-priority omission. | Seven canonical VC7.1 units totaling 512 bytes with all 51 relocations replayed |
| ASCII-004 | exact | The regular and GUI text writers snapshot position, color, scale, GUI mode, and selection mode into independent queues capped at 256 and 64 entries. Both variadic writers format through a 512-byte stack buffer; the GUI form returns the resulting byte length. | Four canonical VC7.1 units totaling 659 bytes with all four relocations replayed |
| ASCII-005 | exact | `OnUpdate @ 0x00401000`, `Reset @ 0x004010F0`, `DrawStrings @ 0x00401700`, and `DrawGuiStrings @ 0x00401920` are canonical exact. The draw pair preserves the direct TH08 ancestor's otherwise-unused `Float3 vector`: TH095 independently exposes the same 12-byte deep compiler home, while target-proven identifier buckets place the five live locals ahead of that vector in the `0x24` frame. `Reset` adds a separate compiler-phase oracle: both real `InitializeAndSetSprite` call sites must expand through the same source-local `__forceinline` phase carrying the target-attested eight-byte reservation; wrapping only the second call leaves the frame eight bytes short, while wrapping both reproduces all 394 bytes without changing the VM initialization semantics. `DrawGuiStrings` additionally must spell the unit-scale test as `scale.x != 1.0f`, with `DrawNoRotationNoRound` in the true arm. | Canonical 135/394/536/577-byte units with all 37 relocations replayed; direct TH08 source ancestry for the draw vector plus exact repeated-callsite phase evidence for Reset. |
| MATCH-008 | exact | Sixteen canonical ASCII manager units reproduce 2,971 authored bytes, covering construction/destruction, chain ownership, resource lifetime, the target-empty VM initializer, the update gate, both bounded text queues, both variadic entrypoints, and both complete draw paths. | Pinned VC7.1 relocation replay for every listed ASCII unit |
| REPLAY-013 | exact | `ResultScreen::PrepareBestShot @ 0x004292D0` selects the highest-scoring photo, runs preview script `best*2+1` in VM 23, derives display size from the loaded sprite, interrupts the selected photo handle, initializes the photo cursor, and exposes every available photo VM through flag `0x10000000`. Only the real `photoCursor.Set(bestShot)` expression owns the target `0x38` allocation phase; wrapping the subsequent photo-count/visibility loop is a bounded negative oracle. | Canonical 380-byte VC7.1 unit with all thirteen relocations replayed; exact `FindBestShot` and `AnmVmId` dependencies. |
| REPLAY-014 | exact | `UpdatePhotoResultScreen @ 0x004294C0` implements the TH095-specific result photo browser and best-shot save path. Its former `0x2C` stack residual is two distinct semantic phases, not one anonymous hole: the previous-photo `SetInterrupt(3)` expression owns a `0x24` source-local phase around `photoCursor.GetPrevious()`, and the immediately following current-photo `SetInterrupt(2)` expression owns an `0x08` phase. Those two phases reproduce every target stack home while preserving the 1,323-byte/304-instruction topology. The last three byte differences exposed a shared ABI correction: the `0x60` `ResultBestShotImageView` overlays the `ResultScoreEntryView` at save-data `+0x460`, so its live fields begin at image `+0x10` (`score +0x10`, `metadata +0x18`, `replayValue +0x3C`, `slowRate +0x48`, `stageValue +0x4C`). With that overlay fixed, the entire browser/save body is exact. | Canonical 1,323-byte VC7.1 unit with all 65 cursor, ANM, score-data, sound, and persistence relocations replayed; ScoreData writer/parser and ResultScreen lifecycle guards remain exact |
| REPLAY-015 | exact | `ResultSaveDataView::UpdateBestShotRecord @ 0x004299F0` frees two owned component buffers at record offsets `+0x70/+0x74`, clears those pointers, then clears component-loaded and valid bytes at `+0x69/+0x68`. The authored body is 187 bytes; the canonical unit also enforces two compiler-owned trailing `int3` bytes and both `_free` relocations. | Canonical 189-byte VC7.1 comparison with 187 authored bytes and two relocations replayed |
| REPLAY-016 | exact | `ResultScreen::Draw @ 0x00429C80` is canonical exact for the complete TH095 result renderer: best-shot/current/high/total/slow-rate text, overwrite notification, twenty replay rows with timestamps and scene metadata, and the 6x16 animated replay-name keyboard. TH08 patched `#pragma var_order` is used only to observe the target local order; the canonical stock VC7.1 source maps the 28 real locals through calibrated identifier buckets. The target-attested twelve-byte best-shot frontend phase is bounded by 0/4/8/16-byte and wrong-phase negative controls. | Canonical 2,573-byte authored unit with a 2,605-byte body-plus-two-switch-table compare extent; all 2,605 bytes and all 118 relocations replay exactly. The Draw source change only renumbers twenty-two compiler-private `$L` identities in exact `ResultScreen::Update`; all 186 Update relocation offsets/types/targets remain unchanged. |
| REPLAY-017 | exact | The complete ResultScreen entry-initializer trio is canonical exact: `InitializeGameResultScreen @ 0x00428590` (788 bytes), `InitializeReplayResultScreen @ 0x004288B0` (1,489), and `InitializePhotoResultScreen @ 0x00428E90` (1,073). Game and Photo keep their previously proven capture/cursor phases. Replay requires mixed constant-index ownership: primary ANM VMs `1/7/9/10/8/19/20` retain indexed `GetResultVm` lowering, while aux text VMs `21/22` use direct member addresses and direct glyph stores; selected group/scene values are rematerialized rather than cached. Its normal timestamp/score tail owns a `0xA0` phase and its extra cursor/aux-text tail owns `0x58`, yielding target cursor homes `-0x0C/-0xB0`, outer `resultScreen @ -0x10C`, and Set-result homes `-0x110/-0x114`. | Three canonical initializers totaling 3,350 authored bytes and 94 relocations; wrapping Replay's Set calls directly with the same storage is a negative control because both cursor homes move to the wrong slots |
| REPLAY-021 | exact | `ReplayBrowserView::Update @ 0x0044DCA0` implements the complete TH095 4-by-20 replay browser: it starts the asynchronous scan, restores the previous grid selection, drives two reusable cursors after a 30-frame gate, validates loaded replay input, copies the selected path, and performs all entry/exit ANM transitions. A source-local `__forceinline` CreateVm helper moves each anonymous return-object temporary into the inline expansion at its actual call site; the six sret homes therefore interleave naturally with timer/cursor homes instead of being front-loaded by the outer function. `GetCurrent()` restores the selected-row/column value temporaries. | Canonical 2,054-byte VC7.1 unit with all 77 relocations replayed; exact layout assertions through `ReplayBrowserView+0x6110` |
| REPLAY-022 | exact | `ReadResultHelpLine @ 0x0041B920` clears the caller-provided destination, copies one logical line, preserves the second byte of Shift-JIS characters whose lead byte is `0x81..0x9F` or `0xE0..0xFC`, then skips all CR/LF bytes before returning the next source cursor. Newline tests use signed `i8`, while the lead-byte ranges use unsigned `u8`; preserving those lexical types reproduces the target branches. | Canonical relocation-free 264-byte VC7.1 unit; exact ResultScreen caller and byte-for-byte parser replay. |
| REPLAY-023 | exact | `ReplayInputSource::Update @ 0x004353B0` exposes the full target-backed input object at `0x004BE218`: ReplayManager consumes `currentInput +0x00` and `pressedInput +0x06`, while the internal history block lives at `+0x2C..+0x58`. Update scans sixteen history bits, increments held counters, repeats after 26 frames by subtracting eight, and derives pressed/released masks. The target internal repeat word at `+0x30` receives constant bit 0 rather than Controller::GetInput's independently shifted repeat mask. | Canonical relocation-free 247-byte VC7.1 unit. A fully live `{u16 currentBits; i32 bitIndex}` aggregate uses only natural alignment yet pins the target homes at `EBP-0x08/-0x04`; all twelve pre-existing ReplayManager units replay unchanged after moving the shared view into `ReplayInputSource.hpp`. |
| REPLAY-019 | exact | The replay-browser scan examines twenty numbered `th95_%.2d.rpy` files and then up to sixty `replay/th95_ud????.rpy` user files while the outer browser state remains 3. The exact 403-byte callback keeps the second scan-finished snapshot as a function-scope scalar and expresses user-file enumeration as a bounded `while` loop with an explicit `FindNextFileA == 0` break. `ReplayBrowserView::LoadReplaySlot @ 0x00450E20` replaces each of the eighty pointers under Supervisor critical section 4; the exit signal at `0x0041BB00` sets its request word at `+0x08`. | Canonical 403-byte scan callback with 23 relocations, 306-byte slot loader with 19 relocations, and canonical 21-byte exit setter |
| REPLAY-020 | exact | The replay scan uses a dedicated `0x18`-byte worker at `SceneSupervisorView+0x648`. `Start` first joins any old worker and launches `_beginthreadex`; `Stop` repeatedly asserts `stopRequested=1` and `active=0` while waiting in 200-ms intervals, then closes the handle. The Supervisor launch wrapper serializes through critical section 6 and its byte lock count at `+0x712`. | Five canonical VC7.1 units totaling 396 bytes with twelve relocations replayed; compile-time worker/Supervisor layout assertions |
| OPTIONS-001 | exact | `OptionsMenuView::Update @ 0x0044E4B0` is the complete seven-row TH095 option/key-configuration page. Rows 0-2 bind the first 32 joystick buttons without duplicates, row 3 toggles windowed mode, rows 4-5 adjust live BGM/SFX volume in steps of five, and row 6 exits. A changed display mode returns result 5. Its exact source keeps the initial VM producer in a source-local force-inlined helper: the fixed VM sites and the loop pass the real script expression into that helper, preserving the target allocation chronology of two inline timer/cursor `this` homes, four `CreateVm` sret homes, then the loop script-index home. | Canonical `options-menu-update` replay covers all 10,103 authored bytes and 372 relocations under pinned VC7.1 build 3077. The target frame is filled only by live semantic values; named helper-side index temporaries, nested return helpers, and direct call-site producers are negative compiler oracles. |

## Reconstructed ECL VM

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| ECL-001 | exact | `EclManager::RunEcl` at `0x00408E70` reproduces its complete 0x69D3-byte authored body. The canonical comparison extends to 0x6C63 and reproduces all 647 relocation fields. | `ecl-manager-run-ecl`; pinned VC7.1 build 3077 |
| ECL-002 | target-observed | The main ECL dispatch table has 158 entries and the shared interpolation tail has a six-entry easing table. Both tables are compiler-owned by the RunEcl COFF symbol. | Target extents `0x0040F843..0x0040FAD2`; exact DIR32 replay |
| ECL-003 | compiler-observed | Exact source requires integer-bit raw float transfers, native bitfield assignments for enemy/effect flags, and whole-structure assignment for 4-byte animation handles. These source forms determine register order without changing semantics. | Zero-difference target comparison and isolated VC7.1 source-shape iterations |
| ECL-004 | target-observed | TH095 extends the inherited ECL VM with photography session, camera, photo-effect, animation-handle, and stage-state opcodes. Adjacent TH08 source establishes ancestry but not these TH095-specific cases. | Exact target handlers and target-local call/field evidence |
| ECL-005 | exact | `EclOperands::ResolveInt @ 0x0040FAE0` and `Enemy::ResolveFloat @ 0x004105A0` decode the target's complete operand selector ranges. Beyond context variables and RNG values, TH095 exposes player/world coordinates, shared runtime parameters, movement state, photo index/count, and active-boss coordinates directly to ECL. | Canonical 1,731- and 1,708-byte bodies plus their 85- and 86-entry jump tables; all 279 relocations replayed |
| ECL-006 | compiler-observed | The photo index and photos-taken selectors require a four-byte value wrapper with an inline integer conversion. Modeling them as plain integers preserves values but removes two conversion temporaries, shrinking each resolver by 12 bytes and changing its vector-temporary stack slots. | Independent pinned-VC7.1 source-shape oracle followed by zero-difference comparison of both resolver extents |
| ECL-007 | exact | `ResolveIntLValue @ 0x00410300` and `ResolveFloatLValue @ 0x00410DB0` implement the writable selector subsets after applying each instruction's operand-flag gate. Float selectors `0x272A..0x272C` point at the enemy's local `position @ +0x28A0`, not the derived `worldPosition @ +0x28F4` returned by the read resolver. | Canonical 487- and 686-byte bodies plus 410 bytes of compressed switch tables; all 81 relocations replayed |
| ECL-008 | exact / compiler-observed | `EclRunHigh::DispatchShotInstruction @ 0x00412670` is the TH095 high-opcode shot descriptor frontend. It preserves the TH08 ancestry but removes the human/youkai gate and TH08 rank scaling, writes the target-local `0x210` descriptor at enemy `+0x298C`, and gates shots by squared XY distance from player `+0x1E30` against enemy `minimumPlayerDistanceSquared @ +0x2C4C`. The exact source uses the ancestral nested early return `if (minimum > 0) { if (distanceSquared < minimum) return; }`. With the target-local Enemy/descriptor overlay, stock VC7.1 naturally keeps the accumulated distance in x87 and emits the target memory-form `FCOMP [enemy+0x2C4C]`; no assembly is needed. Decompiler-style positive OR / De Morgan forms produce a 755-byte body, while empty-arm/goto forms produce 758, proving the former one-byte residual was CFG/source-shape, not an unavailable x87 backend feature. | Canonical 756-byte `enemy-shot-dispatch` unit with 696/696 non-relocation bytes and all 15 zero/player/operand-resolver/bullet-manager relocations replayed. |
| ECL-009 | exact / ancestry-observed | `Enemy::UpdateMovement @ 0x00412970` is the complete six-mode TH095 movement core: velocity, polar, orbit, and three interpolation modes, with target-local movement flags at `+0x2BF4`. The exact source preserves the direct TH08 ancestral `Float3 legacyWork` declaration inside the ORBIT case even though no runtime expression consumes it. This is not arbitrary padding: the TH08 source contains the same otherwise-unused 12-byte local, and omitting it leaves every real movement home in the wrong VC7.1 allocation rank. The earlier 1719-versus-1695 "oversize" diagnosis was also wrong: the 1695-byte authored body is immediately followed at `0x0041300F` by the target's six-entry, 24-byte compiler switch table. With `legacyWork` restored, stock VC7.1 reproduces the entire 1719-byte body+table extent and all six table destinations. | Canonical `enemy-update-movement` unit with 1695 authored coverage, 1719 compared bytes, 1607/1607 non-relocation bytes, and 28 relocations. Direct TH08 source ancestry distinguishes the real unused local from invented frame padding. |
| MATCH-003 | exact | `EclManager::RunEcl` contributes 27,091 authored exact bytes; 27,747 total bytes are compared so the 656 compiler-owned table bytes cannot be omitted or credited as authored code. | `size`/`compare_size` manifest contract and canonical replay |

## Reconstructed screen effects

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| SCREEN-001 | exact | All sixteen target-owned `ScreenEffect` functions at `0x00436760..0x0043778F` are canonical exact for 3,998 authored bytes. The complete lane covers D3D clear/viewport/quad draw, fade-in/out/hold, arcade pulse, linear/envelope shake, Chain registration, timer initialization, and teardown. The target object is `0x34` bytes; overlay alpha/duration/raw parameters are at `+0x10/+0x14/+0x18`, release state at `+0x24`, and the 12-byte timer at `+0x28`. | Sixteen canonical VC7.1 units; `RegisterChain` additionally compares its 32-byte compiler-owned switch table without authored-byte credit. `SetViewport` remains independently anchored by the exact Main D3D initializer. |
| SCREEN-002 | exact | TH095 keeps the TH08 fade source ancestry but two lexical details are target-visible. `CalcFadeIn` must multiply `timer * 255.0f`, not `255.0f * timer`, or VC7.1 reverses the x87 load order by four bytes. `DrawArcadePulse` uses rectangle `(128,16)..(512,464)`, while TH08 uses `(32,16)..(416,464)`. | Isolated build-3077 source-shape probes followed by zero-difference canonical replay. |
| SCREEN-003 | exact | `DrawSquare @ 0x00436920` preserves TH08's four `Float3` corner temporaries, 20-byte diffuse vertices, fixed-function texture-stage setup, triangle-strip draw, and six ANM cache clears, but TH095 removes TH08's conditional ZWRITE-disable device call. The six cache-clear helpers must live in an isolated bounded translation unit: in the crowded ScreenEffect TU stock VC7.1 folds them directly into stores and emits an 815-byte body, while isolation preserves one real `this` home per inline helper and reproduces the 887-byte target frame/body exactly. | Canonical 887-byte `screen-effect-draw-square` unit with all 30 Supervisor, D3D8, ANM, and constant relocations replayed; isolated-TU negative/positive compiler oracle. |
| SCREEN-007 | exact | `RegisterChain @ 0x00436DD0` naturally constructs the timer member inside `new ScreenEffect`, immediately clears the whole `0x34` object, selects calc/draw callbacks through an eight-case switch, registers calc priority 2 and caller-supplied draw priority, and installs exact lifetime callbacks. TH095 swaps enum values 3 and 4 relative to TH08: value 3 is full fade-out and value 4 is arcade pulse. The authored body is 598 bytes; VC7.1 owns the adjacent 32-byte switch table. | Canonical unit with `size=598`, `compare_size=630`, and all 58 body/table relocations replayed. Before the enum correction all non-relocation bytes were already identical, but callback relocations for cases 3/4 resolved to the wrong canonical targets; relocation review therefore caught a real semantic error that byte-only comparison would miss. |
| SCREEN-004 | exact | TH095 `CalcFadeOut @ 0x00436CA0` removes TH08's menu/retry gate and timer increment; it only honors the global cancel counter, computes overlay alpha, and tests the timer. `CalcArcadePulse @ 0x00437190` resets a completed repeat with the real `timer = 0` assignment source shape. Expanding that assignment manually preserves semantics and size but rotates two compiler temporaries. | Canonical 119- and 231-byte units; exact timer comparison/assignment and `Tick` relocations. |
| SCREEN-005 | exact | `CalcShake @ 0x004372D0` and `CalcShakeEnvelope @ 0x004374B0` are gated by photography state at `0x004BDEC8`: bits 0,1,2,4,5,6 must all be clear. Both publish frame shake to floats `g_ScreenEffectShakeX/Y @ 0x004C493C/40`; exact `AnmManager::DrawLayer6` clears those same floats each frame. The gate must keep the lexical `if (state != NULL) { if (blocked) return 1; } else { return 1; }` shape. The bit0/bit2 OR uses a force-inlined two-argument helper: VC7 evaluates `(flag0, flag2)` right-to-left, preserving target bit2-then-bit0 loads while returning the OR in EAX. | Canonical 473- and 592-byte shake units plus unchanged 66-byte `anm-draw-layer-6`; nineteen relocations per shake callback establish photo state, RNG, timer, and X/Y globals. |
| SCREEN-006 | exact | `InitializeTimer @ 0x00437700` initializes all twelve timer bytes as current 0, subFrame 0.0, previous -999999. `DeleteScreenEffect @ 0x00437740` disables its calc deletion callback, cuts the draw Chain node, clears the draw pointer, deletes the effect, then clears the fastcall argument home. The source-level `delete screenEffect; screenEffect = NULL;` is seven bytes longer than delete alone and exactly matches the target. | Canonical 53- and 80-byte lifecycle callbacks with all three Chain/delete relocations replayed. |

## Reconstructed score data

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| SCORE-001 | exact | `ResultSaveDataView::WriteBestShotData @ 0x00435910` commits a pending `BSTS` photograph to its per-scene best-shot file, clears the staged record, updates the persistent best-shot view, then rebuilds `scoreth095.dat`. The rebuild serializes every valid `SC` scene record plus the `0x458`-byte profile, refreshes their additive checksums, compresses the payload, encrypts it with the `AC/35/0x10` transform, and writes a 24-byte header followed by the transformed data. | Canonical 1,407-byte VC7.1 unit with all 37 relocations replayed; exact checksum, compression, encryption, file-writer, and record-reset dependencies |
| SCORE-002 | exact | Persistent score data begins with a `ScoreFileHeader *` at `+0x00`, profile bytes at `+0x08`, 120 overlapping `0x60`-byte scene score/best-shot image views at `+0x460`, and 120 staged `0x78`-byte best-shot records at `+0x3160`. The overlapping image view has a target-proven `0x10` prefix before its live fields: `score +0x10`, `metadata +0x18`, `replayValue +0x3C`, `slowRate +0x48`, and `stageValue +0x4C`; this agrees with the scene-score union member's `score @ +0x10`. The profile checksum is at object `+0x10`, and each staged record owns pixel data at record `+0x74`. | Exact member displacements in `UpdatePhotoResultScreen` and the score writer, compile-time layout assertions, and unchanged exact replay of ScoreData/ScoreLoad/ResultScreen/FrontEndLifecycle consumers after the shared-header correction |
| SCORE-003 | exact / compiler-observed | The persistent save-data owner is `0x69A0` bytes. `ScoreProfileView::Initialize @ 0x00435500` builds the `0x458`-byte profile with `ST` magic, version 0, size `0x458`, an eight-space name, and 512 sequential `g_Rng.GetRandomU16()` words at profile `+0x22`. `InitializeScoreData @ 0x004355F0` allocates/constructs the complete owner and publishes it at `0x004C4E78`; `ReleaseScoreData @ 0x00435660` performs guarded destruction/operator-delete and clears the global. The former lifecycle stack-home residuals are superseded by the exact operation-local producer and cleanup-phase forms recorded in SCORE-LIFE-001/002. | Three canonical owner/profile units totaling 338 bytes with all 18 relocations, plus canonical 69-byte constructor and 109-byte destructor units |
| SCORE-004 | exact / compiler-observed | `ResultSaveDataView::ParseScoreFile @ 0x004356D0` closes the score-file read side. The raw file owner is at `+0x00` and the decompressed payload owner at `+0x04`; valid headers use magic `TH95`, u16 version 2, and format value `0x102`. The payload is decrypted with `AC/35/0x10`, decompressed through the exact LZSS decoder, then scanned as version-1 `SC` records or version-0 `ST` profile records. Unknown record magic or size underflow resets the file, while a known record with bad version/checksum is skipped. The decoder allocation deliberately reserves `uncompressedSize * 4` bytes while passing the unscaled `uncompressedSize` to `DecompressData`, matching the target. | Canonical 568-byte VC7.1 unit with all fifteen logger, decrypt, allocator, decoder, checksum, and constant relocations replayed. A gapless 24-byte all-live parser-state aggregate reproduces the complete target frame; POD whole-structure assignment is required for both `SC` and `ST` copies so VC7 materializes the RHS cursor into ESI before the destination EDI. `memcpy` and force-inline copy wrappers are negative oracles. The adjacent constructor and destructor are now canonical exact as recorded in SCORE-LIFE-001/002. |

| SCORE-005 | exact | `LoadBestShotForScene` proves staged best-shot record bytes `+0x68/+0x69` are distinct: `+0x68` is the persistent valid/write-pending flag and `+0x69` is `componentsLoaded`, consumed by `LoadScenePreviewTexture`. The exact loader also supplies a reusable VC7.1 evaluation-order oracle: keeping `recordIndex` as a calibrated scalar causes the `recordIndex * 0x78` destination stride to enter ESI before `_malloc` and survive the call; hiding the index inside the old 0x110 aggregate loses that cross-call preservation. The final compiler-home class belongs specifically to the inlined pixel-allocation frontend and is target-strict at eight bytes. | Exact `scene-best-shot-load` 1,034-byte replay; exact `UpdateBestShotRecord`/preview consumers; current-source 0/4/12-byte negative controls versus eight-byte exact control. |

## Reconstructed MIDI subsystem

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| MIDI-001 | exact | The TH095 WinMM MIDI spine at `0x00421E00..0x00423422` is canonical for twenty-eight functions / 5,122 authored bytes. Twenty-four device/timer/ownership/playback helpers transfer directly from TH08 once compiled against TH095's `Midi.hpp`; TH095-specific source shapes close `ParseFile`, `OnTimerElapsed`, `ProcessMsg`, and `FadeOutSetVolume`. | Twenty-eight canonical VC7.1 units; all 125 relocation fields and every compared non-relocation byte replay against the attested 1.02a target. |
| MIDI-002 | exact | `MidiOutput` is `0x300` bytes and owns thirty-two file-data slots, thirty-two pending long-message headers, `0x20`-byte track records, the output device, sixteen channel records, and fade/loop timing state. `ReadFileData @ 0x004221B0` proves the target CP932 error string at `0x00495D7C` and routes file ownership through exact `FileSystem::OpenFile`; `StopPlayback @ 0x00422660` unprepares all pending headers before stopping the timer and closing the device. | Exact layout assertions in `Midi.hpp`, canonical read/stop/unprepare units, and solved `FileSystem`, WinMM, allocator, error-context, vtable, and timer relocations. |
| MIDI-003 | exact / compiler-observed | `MidiOutput::ProcessMsg @ 0x00422A70` is canonical exact for all 1,866 authored bytes. Its COFF function symbol is `0x89E` bytes because VC7.1 owns an adjacent `0x154` switch-table region; treating that auxiliary extent as authored code caused the obsolete “2,196 versus 1,866” diagnosis. TH095 keeps the TH08 event semantics but uses direct `malloc/free`, retains target-observed `beatsPerMinute` and `nextEventDeltaTicks`, snapshots the 64-byte `MIDIHDR` allocation size, and snapshots `lpData` before failure-path `free`. Stock VC7.1 reproduces the TH08 var-order without a patched pragma: calibrated real-local identifier buckets pin the shallow/deep scalars, while one fully live 16-byte middle aggregate lays out `{longMessageHeader,eventDataLength,eventData1,statusByte,messageType,channel,adjustedChannelVolume}` exactly. | Canonical `midi-output-process-msg`: 1,866/1,866 authored bytes, 2,206/2,206 body-plus-switch-table bytes, 520/520 body mnemonics, 221/221 EBP operands, and all 37 relocations replay. The final probe remains structural-exact after removing `#pragma var_order`. |
| MIDI-004 | exact | `ParseFile @ 0x00422300` inlines the 32-bit endian swap twice and reproduces TH08's documented parser var-order as a 0x24 source-order record. `OnTimerElapsed @ 0x00422800` uses the target-proven live-BOOL bucket so the generated signed 64-bit event-tick comparison temporary lands at `EBP-0x20/-0x1C`. `FadeOutSetVolume @ 0x00423310` uses a gapless fully live five-field aggregate. The parser's initial `fileData` snapshot is store-only but is retained only because both TH08 source ancestry and TH095 machine code independently prove that exact store. | Canonical 547-, 615-, and 183-byte units totaling 1,345 authored bytes with 26 relocations; all three replay exact from maintained `src/Midi.cpp`. |

## Reconstructed sound system

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| SND-001 | exact | The target-proven `SoundPlayer` size is `0x52D0`. Worker handles are at `+0x5218/+0x521C`, stop state at `+0x5224`, worker window at `+0x5228`, and 37 producer-owned file pointers at `+0x5230`. | Compile-time assertions and canonical SoundPlayer units |
| SND-002 | exact | TH095 owns 47 `SoundBufferIdxVolume` entries. `InitSoundBuffers` joins both workers before duplicating and configuring all 47 DirectSound buffers. | `sound-player-init-sound-buffers` and exact `ProcessQueues` relocations |
| SND-003 | exact | Positioned SFX pan uses `(pan * 1000) / 192`; it does not use TH08's inherited `(pan - 192)` shape. | Canonical `sound-player-play-sound-positioned-by-idx` unit |
| SND-004 | exact | Serialized option bit 4 controls BGM preloading. `StartBGM` and `LoadBGM` create a notification worker; `BGMPlayerThread` services the streaming event and window-message queue. | Canonical preload/load/thread units and exact option-field DIR32 addends |
| SND-005 | exact | `SoundPlayer::UpdateFades @ 0x00426300` is the live-frame streaming maintenance hook. When BGM exists it updates full fade-out, full fade-in, partial fade-out, and partial fade-in in that target order. The relocations prove `UpdatePartialFadeOut @ 0x00454450` is fade type 4 and `UpdatePartialFadeIn @ 0x004543E0` is fade type 3, correcting the former semantic name reversal. | Canonical 79-byte VC7.1 unit with all four `CStreamingSound` relocations; exact Supervisor calc-callback caller |
| SND-006 | exact / origin-reviewed | The ZUN-modified `CSoundManager`, `CSound`, `CStreamingSound`, and `CWaveFile` layer at `0x00452E70..0x00454E42` is exact for twenty-two authored functions and 5,942 bytes. It covers file/memory streaming creation, persistent play/fade state, notification-buffer creation, nonlinear volume, stop/pause/resume, four fade modes, the 998-byte refill/loop hub, raw-stream reset, and the `ThBgmFormat` memory/file open/read/seek/close path. Thirteen unchanged Microsoft DSUtil scaffold functions totaling 1,849 bytes and two 44-byte VC7.1 scalar deleting destructors are separately classified as exclusions and receive no authored credit. | Twenty-two canonical `zwave-*` units with all 92 relocations replayed; Microsoft copyright/source ancestry plus explicit ZUN modification markers in `zwave.hpp`, checked against TH095-local target behavior |
| SND-007 | exact / compiler-observed | `CSoundManager::CreateStreaming @ 0x00453050` and `CreateStreamingFromMemory @ 0x004533B0` share a fully live local aggregate ordered as notify/result/descriptor/buffer/wave/notifications/size, reproducing the target stack homes without inert storage. The file-open failure path deletes and then clears its `CWaveFile` owner; that observable ownership reset is the final seven bytes required by the 862-byte target. `CSound::Play @ 0x00453EE0` similarly needs a fully live result/restored/buffer aggregate and then publishes the target's fade state, priority, flags, auxiliary word, and play flag before DirectSound playback. | Four canonical units totaling 2,131 bytes with all 40 EH, allocation, DirectSound, string, and internal-call relocations replayed; attested Ghidra/target control flow plus pinned VC7.1 build 3077 |
| MATCH-004 | exact | Twenty-seven canonical SoundPlayer units reproduce 8,212 authored bytes, including the 2,525-byte queue hub, 881-byte DirectSound initializer, 849-byte asynchronous WAV consumer, and all surrounding request/worker helpers. | Pinned VC7.1 relocation replay for every SoundPlayer unit |

## Open architecture questions

- The original object partition outside the proven `Main.cpp` Render unit is
  not established.
- ECL helper ownership and friendly names outside the exact `RunEcl` unit are
  still provisional even though every dispatcher case and VM offset in the
  canonical body is target-validated.
- Source-level class names for the large TH095 photography, gameplay, and
  asynchronous resource hubs remain unknown.

## Reference provenance

The initial repository model was compared against local ignored clones at:

- `N0zoM1z0/th105` commit `4afbe8430973571b78b06a97ca351de2a9412994`;
- `N0zoM1z0/th08` commit `b6d8877ccbff7de534c02ab2c6a811345496412e`.

These revisions explain workflow provenance only. They do not establish a
single TH095 code, layout, or behavior claim.

### TH095 low-ECL movement dependency lane (`0x00412200..0x00412666`)

- `EclRunLow::BeginBoundaryAwareMove` at `0x00412200` is canonically exact at 645 bytes. The exact RunEcl relocation manifest supplied the function identity; TH08 `0x00422020` supplied source shape; TH095 target evidence pins the player-position overlay to `g_Th095Player + 0x1E30`, movement flags to `Enemy + 0x2BF4`, and movement bounds to `Enemy + 0x2C3C`.
- `EclRunLow::StartTimedPolarDisplacement` at `0x00412490` is canonically exact at 471 bytes. A real C++ bitfield view at `Enemy + 0x2BF4` is required for VC7.1 to reproduce the target's movement easing (bits 12..14) and movement mode (bits 10..11) stores without mask-shape drift.
- The adjacent `EclRunHigh::DispatchShotInstruction` at `0x00412670` is canonical exact for 756 bytes. The former one-byte x87 comparison residual was a CFG/source-shape error: the ancestral nested early-return form naturally keeps the squared distance in x87 and emits the target memory-form compare without assembly. ECL-008 records the exact source and 15-relocation replay.
- `Enemy::UpdateShotAndAnm @ 0x00413030` is canonical exact for all 837 bytes and thirteen relocations. TH08 supplies the ancestral cadence/direction state machine, while TH095 target-local views pin life `+0x2958`, deferred instruction `+0x2B9C`, interval/timer `+0x2BC8/+0x2BCC`, movement flags `+0x2BF4`, ANM direction `+0x2C0A`, and five scripts at `+0x2C0E..+0x2C16`. Flags1 bit 31 selects runtime ANM banks `+0x4DF8/+0x4DFC`. The target 0x40 frame is naturally filled by the two real locals plus inlined timer and script-setup temporaries; no padding is required.


## TH095 extended ECL callback table

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| ECLX-001 | exact | `g_Th095ExInsn @ 0x004A4270` is the TH095-specific 22-entry extended-ECL callback table. Entries 0..21 target `0x00413380, 0x00413410, 0x004134A0, 0x00413620, 0x00413750, 0x00413990, 0x00413AA0, 0x00413B90, 0x00413CF0, 0x00413DA0, 0x00413DF0, 0x00413F90, 0x00413FC0, 0x00414020, 0x00414090, 0x00414230, 0x00414260, 0x00414290, 0x00414430, 0x004144E0, 0x00414580, 0x00414930`. All entries use the target-proven `void (__fastcall *)(Enemy *, EclRawInstruction *)` ABI even where EDX is semantically unused. | Two exact `EclManager::RunEcl` DIR32 relocations to `0x004A4270`, verified target table contents, and twenty-two independently canonical callback units. |
| ECLX-002 | exact | All twenty-two extended entries 0..21 are canonical exact for 5,570 authored bytes and 181 relocations. Entries 10/14/17 are the three PhotoEffect-spawn siblings with `field38 = 40/300/120`; entries 1..4 close the bullet/enemy transformation family described in ECLX-011; the other entry-specific semantics remain described below. | Twenty-two replayable `ecl-extended-*` units under pinned VC7.1 build 3077. |
| ECLX-003 | exact | Extended entry 6 measures squared XY distance from player `+0x1E30` to enemy `+0x28A0`. Below 1024 it publishes `0.25` to player `+0x2A18`; from 1024 through 4096 it linearly maps to `1.0`, then projects the enemy position into the VM referenced by enemy handle `+0x2D4`. | Canonical 237-byte `ecl-extended-update-player-proximity` unit with twelve relocations, including exact `GetVm` and `PhotoToScreen`. |
| ECLX-004 | exact | Extended entries 12/13 and 18/19 control the two Background dynamic VM handles at `Background+0x1FE4/+0x1FE8`. States 2/3 are written through `AnmVm+0x22E`; entries 18/19 additionally toggle photo-global flag `0x400`, execute both VMs immediately, and play sounds `0x26/0x0F`. Entry 13 and entry 18 restore `g_AnmGameSpeed` to `1.0`. Photo-global flag `0x200` at `+0xFC` is independently toggled by entries 15/16. | Seven canonical units totaling 600 authored bytes and 55 relocations. |
| ECLX-005 | exact | Extended entry 21 scans exactly `0x640` active bullets in the target-proven `0x65C` pool at BulletManager `+0x4C`. Ownership is `bullet+0x330 == activeEclContext->extraIntVariables[2]`. Matching bullets clear dwords `+0x348/+0x34C`, set speed `+0x2F4` to `4.5`, and rebuild velocity `+0x2DC` from angle `+0x300` through exact `Float3::FromAngleMagnitude`. | Canonical 183-byte `ecl-extended-reset-owned-bullet-motion` unit plus the exact BulletManager and Float3 dependency lanes. |
| ECLX-006 | exact | Extended entry 5 scans 1,600 active bullets, filters ownership through `activeEclContext->extraIntVariables[2]` and capture flag bit 4, then initializes ANM interpolation slot 2 for a 30-frame alpha transition from `0x40` to `0xFF`. The interpolation mode is a real 32-bit local and is truncated only at the final byte store. | Canonical 264-byte `ecl-extended-fade-owned-captured-bullets` unit with one global relocation; a fully live six-field 0x18 semantic aggregate reproduces the target frame. |
| ECLX-007 | exact | Extended entry 8 creates a marker VM through the ANM source at runtime `+0x4DF8`, using `activeEclContext->extraIntVariables[2]` as script index and enemy `worldPosition` as the world coordinate. It resolves the returned handle, sets the sprite to the enemy VM active sprite, then copies the two-bit flip field and complete rotation. | Canonical 164-byte `ecl-extended-spawn-enemy-marker-vm` unit with four relocations. Exact source writes the small-struct return directly into a live aggregate handle through the target-observed sret out-parameter ABI, avoiding an extra assignment copy. |
| ECLX-008 | exact | Extended entry 20 is the complete photography transition coordinator. It owns the 120-frame start and 60-frame release countdown, photo flag `0x400`, both Background dynamic VMs, sounds `0x26/0x0F`, game-speed restoration, camera-target gating, random Y displacement, viewfinder-dependent X displacement, and a 60-frame enemy movement interpolation with easing mode 4. | Canonical 938-byte `ecl-extended-run-photo-transition` unit with 44 relocations. Every dword of the target 0x50 frame is live; a 0x48 semantic aggregate plus ECX/EDX homes reproduces the frame exactly without padding. |
| ECLX-009 | exact | Extended entry 7 projects a 24-pixel movement-angle offset into the first enemy marker VM, converts it through `PhotoToScreen`, shifts it by `(-128,-16)`, mirrors the movement angle into VM rotation, then copies the projected position and rotation to the second marker VM. One real 12-byte vector local is reused for both phases. | Canonical 341-byte `ecl-extended-update-enemy-marker-vms` unit with eight relocations; the semantic `firstVm` identifier lands in the target VC7.1 local bucket without dummy storage. |
| ECLX-010 | exact / compiler-observed | Extended entries 10/14/17 each build one `0x48` PhotoEffect argument record, store the `Spawn` result, resolve the spawned effect by id, initialize its VM from `activeEclContext+0x60`, and clear effect flag bit 1. The exact finder publishes the match and returns immediately; loop exhaustion falls through to an unconditional NULL publication, removing the old target-absent `cmp/jne`. A fully-live `0x50` `{args,effect,spawnId}` record pins every shallow home through `cursor @ EBP-0x74`. The final real flags publication is a source-local member frontend owning a target-strict `0x2C` tail phase: it moves only the two fastcall argument homes from `-0x78/-0x7C` to `-0xA4/-0xA8`. `0x28` and `0x30` controls leave the same seven references one dword shallow/deep; the canonical `0x2C` form is 380/380 comparable bytes in all three 404-byte targets. | Three independent stock-VC7.1 units, 18 solved relocations, 108/108 mnemonics per target, 59/59 paired EBP operands for the closing entry-10 oracle. |
| ECLX-011 | exact / compiler-observed | Extended entries 1..4 close the TH095 bullet-to-enemy / bullet-script family. Entry 1 scans all `0x640` bullets and uses the target-strict skip form `if (state == 0 || loadedSprite->widthPx < 64.0f) continue;`, then spawns a life-1 enemy at the bullet position; spelling the positive compound condition collapses the shared false block and produces a 138-byte body instead of target 140. Entries 2/3 reinitialize matching/active bullet VMs through shifted/direct script banks, rebuild velocity, and publish Background VM states 2/3. Their real `InitializeVm` operation owns a repeated `0x2C` source-local phase; background-handle publications use bounded inline helpers. Entry 4 extends entry 2 with context-angle/speed selection, `ExecuteScript`, and interpolation-finalization. Its last four homes are target-sensitive: the two timer-generated `$T` values must occupy `-0x3C/-0x40`, while the real VM/interpolation values occupy `-0x44/-0x48`. A force-inline tail helper with parameter order `(AnmVm *vm, i32 interpolationMode)` reproduces all 57 EBP operands and all 145 mnemonics exactly; reversing the two parameters is a strong negative oracle. | Canonical callback units at `0x00413410/0x004134A0/0x00413620/0x00413750` totaling 1,386 bytes with 41 relocations; cold tracked-source replay is 140/140, 378/378, 293/293, and 575/575, and all 22 `EclExtended.cpp` units remain exact. |
- VC7.1 lifecycle closure: an empty child destructor defined class-inline can still emit the callable COMDAT target while expanding automatic member destruction inside the owning destructor. `PhotoCameraState::~PhotoCameraState` remains a 35-byte exact body, while `PhotoGameUpdateView::~PhotoGameUpdateView` expands its four `AnmVm` viewfinder destructions. If an owned pointer is target-homed only at the free site, introduce the real ownership local in that phase rather than hoisting it or adding padding.
- Dummy MIDI lifetime classification: `0x00424700` is not authored source. Removing the user-declared `DummyMidiTimer::~DummyMidiTimer` makes pinned VC7.1 emit an aux-less implicit destructor symbol with the target's exact 19-byte instruction body; masking its sole `REL32` leaves every byte equal, and the relocation resolves to exact `MidiTimer::~MidiTimer @ 0x00421F90`. The startup new-expression separately calls the base constructor and writes the derived vptr, while the shutdown delete-expression calls this implicit destructor then `operator delete`. Classify the 19-byte body as compiler-generated/excluded rather than forcing an authored destructor to suppress VC7.1's vptr store.
### CPbgFile open/local-array allocation oracle

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| PBG-OPEN-001 | exact | `CPbgFile::Open @ 0x00455920` is exact for 330 authored bytes. TH08's `r/w/a` semantics transfer directly, while stock VC7.1 needs only the four live locals mapped through the established identifier-hash rank in target order `curMode / goToEnd / filePathBuffer / creationDisposition`. Keep the 260-byte path array independent: combining it with `creationDisposition` into a semantic aggregate collapses a genuine four-byte array allocation boundary and leaves a `0x114` frame instead of target `0x118`. | Canonical `pbg-file-open` unit, 89/89 target mnemonics, four reviewed relocations, target/local-home crosswalk, and TH08 `CPbgFile::Open` ancestry |
| PBG-OPEN-002 | compiler-owned | `0x00455890` and `0x00455DE0` are scalar deleting destructors for `CPbgFile` and `IPbgFile`, respectively, not authored methods. | Attested Ghidra/target control flow: underlying destructor/vtable publication followed by flag-bit-0 conditional `operator delete` |
### Small authored leaf ownership oracles

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| LEAF-001 | exact | `ReleaseResultAnm @ 0x0042AAD0` is a 20-byte authored slot-nine ANM release wrapper. It calls `AnmManager::ReleaseAnm(9)` and returns zero; the unique accepted caller is `Supervisor::DeletedCallback`. | Canonical `photo-release-result-anm` unit with both relocations plus exact caller relocation |
| LEAF-002 | exact | `utils::DebugPrint @ 0x00412180` is an authored empty variadic sink in the release build, not a CRT thunk. | Canonical relocation-free 5-byte unit, 119 accepted caller relocations, and TH08 source where the body is empty without `DEBUG` |
| LEAF-003 | compiler-owned | `PbgArchiveEntry::vector deleting destructor @ 0x00455080` selects vector/scalar destruction and conditional deallocation from compiler flags. | Attested Ghidra/target control flow; excluded from authored denominator |
### PbgArchive inline-allocation-phase oracle

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| PBG-ALLOC-001 | exact | `PbgArchive::AllocEntries @ 0x00455580` is exact for 524 bytes. Keep `{buffer,i,entryData}` as one fully-live outer aggregate. Restore TH08 `DeleteArray(buffer)` as `if (buffer) { delete[] buffer; buffer = NULL; }`, and restore `SeekPastString(&entryData)` as a bounded source-local inline helper. Its real `filenameSize` local is thereby allocated in the later inline phase at `EBP-0x30`; this rotates all surrounding new[]/delete[] compiler homes into target order without padding. | Canonical `pbg-archive-alloc-entries` unit: 164/164 mnemonics, 484/484 comparable bytes, ten reviewed relocations, plus full maintained-TU replay |

### VC7 stack-home crosswalk and hard-lane oracles

This table preserves bounded negative-oracle results, not the current blocker
list. `PhotoStageDisplayView::Build`, `UpdateSceneSelect`, `WinMain`,
`PhotoEnemyManagerView::PhotoEnemyManagerView`, and the three `CreateVm`
variants were subsequently promoted by the exact entries elsewhere in this
knowledge base and the canonical ledgers. Only the `Controller::GetInput`
rows below still describe an active authored non-exact function.

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| VC7-HOME-001 | tooling / compiler-observed | `scripts/compare-stack-homes.py` pairs EBP-relative operands instruction-by-instruction for equal-extent/equal-mnemonic VC7 COFF and target functions. With `/FAsc`, it labels object homes by source/compiler symbols and reports source→target home mappings, reference counts, and first sites. It is diagnostic only and never grants exact credit. | Attested target reader reused from `compare-coff-function.py`; SceneSelect 3,637-instruction smoke test |
| SCENE-ALLOC-001 | exact / compiler-observed | `UpdateSceneSelect` demonstrates the distinction between semantic aggregation and VC7 debug-local allocation. Grouping the 23 queue pointer/value locals into a single 92-byte record makes stock VC7.1 align the object at `-0xA8`, creating a non-target dword hole before `shallow @ -0x48`; the target instead lays the 23 real four-byte locals contiguously at `-0x4C..-0xA4`. A patched build-3077 `var_order` oracle established their physical rank, after which the exact ResultScreen hash buckets reproduced that rank under the unmodified compiler. This supersedes the former conclusion that the 92-byte aggregate was required. | Exact `scene-select-update`; 23-scalar target/source home crosswalk; patched-order diagnostic and stock-bucket replay |
| PHOTOSTAGE-ALLOC-001 | compiler-observed | In the exact-size `PhotoStageDisplayView::Build` private best, all 92 real per-emission `AnmVm *` homes can be made target-exact while outer hidden `this` remains at `-0x2B4` instead of target `-0x140`. Calibrated hash names, helper scope spellings, two-level wrappers, static/member owners, and a member-array `operator[]` returning a fully live local pointer all converge to the same 7,245/7,288 replay once the 92 homes exist; the last variant keeps all 2,234 mnemonics and leaves only the 21-use outer receiver mismatched. Conversely, wrapper forms that move the receiver near `-0x140` remove the 92 pointer-home family. The residual is hidden-this/inline-scope allocation, not identifier rank, and the two effects are not additive in tested stock-3077 source shapes. | Target/source stack-home crosswalk; 20-name hash sweep; helper/owner families; member-wrapper plus live-pointer negative oracle |
| CONTROLLER-ALLOC-001 | compiler-observed | `Controller::GetInput` local-home rank and scratch-register scheduling are independent: the rank-correct probe has every observed EBP home exact while preserving the canonical EAX/ECX/EDX rotation. Comma-expression and equivalent ternary AST variants do not change it; `u16` casts expand code. | Pinned VC7.1 operand crosswalk and bounded expression-shape probes |
| CONTROLLER-ALLOC-002 | compiler-observed | `Controller::GetInput @ 0x00419AE0` is a compiler temporary/value-class residual, not an ordinary-local rank problem. The source/target have 579 instructions and 212/212 paired semantic EBP homes, while target alone reserves two unreferenced dwords below `inputIndex @ -0x11C`. All 720 declaration permutations are byte-family identical; 43 different identifiers with exact physical homes produce identical function bytes; 168 historical `0x124`-frame probes top out at 205/212 homes, and no deeper-frame historical probe preserves 212/212. The strongest phase-only diagnostic is byte-exact except for the single frame-allocation immediate (2,573/2,574 comparable); the strongest target-frame diagnostic misses exactly the seven `inputIndex` displacement bytes (2,567/2,574). Their inert phase expressions and dead hidden-return storage are non-composable and forbidden as canonical source. Function/block-scope dwords, 8-byte scalars/alignment, TU isolation, macro ownership, and topology-preserving stock-3077 profile variants are negative controls. | Full 6! declaration oracle; complete historical frame/home census and `p2-*` rescore; raw target stack audit; pinned build-3077 profile controls |
| CONTROLLER-ALLOC-003 | compiler-observed | The target-frame `GetInput` UDT diagnostic keeps the fastcall parameter in one fixed compiler allocation class: sixteen ordinary parameter names and plain, `const`, `register`, `const register`, and `__w64` spellings all store ECX at `EBP-0x124`. Every variant retains the target 2,662-byte/579-instruction register shape but misses the same seven target `inputIndex @ EBP-0x11C` operands. ABI-equivalent parameter spelling cannot move the hidden eight-byte reservation behind the real parameter. | Bounded stock-3077 cross-oracle and representative object hashes in `.analysis/final-four-oracles-20260908.md` |
| CAMERA-ALLOC-002 | exact / compiler-observed | The old camera allocation-barrier diagnosis is superseded. `IsZero` correctly reclassifies both zero-equality RHS objects into the deep chronology, but the historical eight-byte reservation was attached too late to the effect-VM start and merely realigned temps after the comparisons. Removing that late reservation makes the entire deep family, including outer `camera`, uniformly eight bytes shallow. The first member of that family is generated by the real tracking `Float3` subtraction; moving the same target-strict eight-byte phase to a no-extra-operation subtraction value producer closes every stack home. A no-storage producer is byte-identical to the phase-free source, and direct effect-call plus class-inline converting-constructor controls remain exact, so those historical wrappers are unnecessary. | Authored-body `/FAsc` chronology and 613-operand EBP audit; exact PhotoGame equality sibling; phase-size controls 4/8/12 = 5,976/6,415, 6,415/6,415, 5,976/6,415; canonical 219-relocation replay |
| WINMAIN-ALLOC-001 | compiler-observed | `WinMain @ 0x00420240` has 347/347 mnemonics, all 134 relocations, and 782/790 comparable bytes. All six ordinary `$T` pointer homes are exact. Source then has `tv148=-0x44, tv231=-0x48, tv284=-0x4C`; target inserts one instruction-unreferenced dword at `-0x44` after the last `$T`, then places those three new/delete merge-result temporaries at `-0x48/-0x4C/-0x50`. The source frame is `0x4C`, target `0x50`. Publication helpers/member proxies are byte-identical; real delete-owner helpers change extent; TH08 WinMain has no fifth explicit local. Treat this as a compiler-class boundary hole, not a missing semantic local. | `compare-stack-homes.py` 51-operand crosswalk; TH08 WinMain source; bounded publish/delete-owner probes |
| ENEMYCTOR-ALLOC-001 | compiler-observed | `PhotoEnemyManagerView::PhotoEnemyManagerView @ 0x00414B90` is exact-sized with 269/269 mnemonics and all eleven relocations. Its 32 non-exact EBP references are exactly seven homes, every one shifted by target-source `-0x28`: three compiler values for the sixteen-entry `PhotoEnemyTimelineView` construction loop, three later real `ZunTimer = 0` receiver temporaries, and outer `this`. All 95 shallow EBP references are exact. The 40-byte target-only phase begins specifically after spawn-template construction and before the sixteen-timeline constructor loop. Exact `PhotoEnemyEclContextView` and `PhotoEnemyView` constructor loops do not repeat a generic 0x28 phase; explicit empty timeline constructors and timer-constructor surface probes are byte-identical negatives. | 127-operand source-labelled crosswalk; exact child-constructor `/FAsc` oracles; TH08 `EclTimeline` ancestry |
| CREATEVM-ALLOC-001 | compiler-observed | `AnmLoaded::CreateVm`, `CreateVmAtScreen`, and `CreateVmAtWorld` share one repeated new-expression residual. Each has the complete target mnemonic sequence; 16/21 EBP operands are exact, while only outer `this` and the constructor merge-result temporary are 20 bytes shallow (`-0x40/-0x44` versus target `-0x54/-0x58`). The two `new AnmVm` allocation/EH `$T` homes and real `vm` owner are already exact. A 0x14 caller scope preserves extent and improves each by one comparable byte but incorrectly pushes the two `$T` homes 20 bytes deep; a 0x14 tagged derived-construction oracle instead makes frame, `this`, and merge-result homes exact but adds one three-byte `mov eax,allocationTemp`, growing each function by three. Pointer identity and InitializeVm frontends are byte-identical; trivial/non-trivial 20-byte phase objects do not close the class boundary. The three independent targets therefore prove a common 0x14 construction-result phase, but the exact stock-C++ source surface remains unresolved. | Three 21-operand crosswalks; target disassembly; derived/scope/identity/InitializeVm/class-object construction probes |
| ENEMYCTOR-ALLOC-002 | exact / compiler-observed | `PhotoEnemyManagerView::PhotoEnemyManagerView @ 0x00414B90` is canonical exact for 1,196 authored bytes. The target's formerly unexplained `0x28` deep-home shift belongs to the real sixteen-entry `PhotoEnemyTimelineView` generated-construction frontier: placing one stock-VC7.1 `compilerStorage[0x28]` phase in the timeline constructor moves exactly the seven deep compiler homes while preserving all 95 shallow EBP references and the 269-instruction topology. `0x24` and `0x2C` controls leave the same family one dword shallow/deep, and an empty constructor is byte-identical to the old source. | Canonical `enemy-manager-constructor` unit: 1,196/1,196 bytes and eleven relocations; 0x24/0x28/0x2C bounded phase matrix; all 22 `EnemyManagerUpdate.obj` units cold-replay exact, with only destination-audited private-label renumbering in `enemy-timeline-run` |

## Authored-boundary and ScoreData lifecycle audit (2026-09-07)

| ID | Class | Durable fact | Evidence |
| --- | --- | --- | --- |
| TRACK-AUTH-001 | tracking / target-observed | The authored denominator must include eleven previously owner-empty functions inside the established code region: `0x00412670`, `0x00412970`, `0x00413410`, `0x004134A0`, `0x00413620`, `0x00413750`, `0x00413DF0`, `0x00414090`, `0x00414290`, `0x004354B0`, and `0x00435580`. The seven `0x00413xxx/0x00414xxx` entries are slots 1/2/3/4/10/14/17 of the exact 22-entry `g_Th095ExInsn` authored callback table. `0x00401BE0`, the only other owner-empty body below the prior upper boundary, is MSVC `vector_constructor_iterator` and remains compiler-owned. | Exact callback table contents, neighboring canonical callback units, direct ECL/caller edges, existing semantic reconstructions, and function-origin re-audit |
| TRACK-AUTH-002 | boundary / compiler-observed | `Lzss::FindNextNode @ 0x00456950` is still the upper authored-code boundary. The raw gap `0x0045698F..0x004583CF` contains repeating compiler/data records and no common x86 function prologues; `0x004583D0` begins consecutive DINPUT8/DSOUND/D3D8 import thunks followed by CRT/compiler helpers. Do not extend the authored denominator into that tail merely because a disassembler later invents function labels there. | Direct PE-byte scan independent of Ghidra/IDA function recognition, plus runtime-thunk sequence |
| SCORE-LIFE-001 | exact / compiler-observed | `ResultSaveDataLifecycleView::ResultSaveDataLifecycleView @ 0x004354B0` is canonical exact for all 69 bytes. Its real `OpenFile` producer owns one four-byte compiler phase adjacent to the live file-size output; representing those as one eight-byte operation-local record puts `fileSize @ EBP-0x04`, the phase at `-0x08`, and hidden `this @ -0x0C`. 0/4/8-byte controls fail at 46/53, 52/53, and 45/53 comparable bytes. | Canonical `score-data-lifecycle-constructor`, 53/53 non-relocation bytes and four replayed relocations |
| SCORE-LIFE-002 | exact / compiler-observed | `ResultSaveDataLifecycleView::~ResultSaveDataLifecycleView @ 0x00435580` is canonical exact for all 109 bytes. The target explicitly materializes the raw-file and decompressed-buffer free arguments into two real ownership snapshots. Each free frontend independently owns one four-byte cleanup phase; either one-sided control stays 90/97, while both give target `index/raw/decompressed @ -0x04/-0x08/-0x0C`, the eight-byte compiler interval, hidden `this @ -0x18`, and 97/97. | Canonical `score-data-lifecycle-destructor`, both `_free` destinations and `UpdateBestShotRecord` relocation replayed; sibling ScoreLifecycle units cold-replay exact |
